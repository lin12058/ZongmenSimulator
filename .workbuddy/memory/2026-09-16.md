
---

## 2026-09-16（另一任务 · W 世界种子 / 齿轮设置组件 / 灵脉签色）——与上面 A2 任务并行

用户四项：① seed 改由服务端产生并落 sqlite ② 右上「另启一世」随之修 ③ 右上收成一枚齿轮 ⚙，
点开弹窗（显示开关）+ 存 localStorage，要一个**全局存储组件** ④ 灵脉牌匾敷对应（五行）色，淡一点叠在纸签上。

- **① 服务端种子台账**：新增 `Server/Zongmen/Storage/WorldLedger.cs` —— **独立表**
  `World(Round,Seed,BornAt)`（⚠ 不能塞 `Data` 表：`PruneExcept` 会静默删）。`MapWorldService` 加
  `WorldCurrentJson/WorldNextJson/WorldListJson` + stats 报 `worldRounds`；`MapEndpoints` 加
  `GET /api/world/current`（幂等）/ `POST /api/world/next` / `GET /api/world/list`。CSPRNG 8 位种子。
- **②③ 前端**：新增 `web/js/store.js`（`window.ZMStore`，键 `zongmen.settings.v1`，schema
  `{veins,nameSettle,nameVein,nameRegion,clouds}`，白名单/200ms 节流/坏 JSON 降级/`pagehide` 强写 +
  通用 `read/write/clear(ns)`）。`index.html` 把 7 个按钮收成 `#btnGear` + `#settingsWrap` 弹窗
  （5 个 `data-zm` 复选框 + 轮次/种子/来历 + 另启一世 + 山河 stats）；`main.js` 加
  `applySettings/syncSettingInputs/openSettings/worldFetch/setWorldInfo`；boot 改
  `await worldFetch('/api/world/current')`；「另启一世」= POST next；`?seed=` 仍为调试覆盖。
- **④ 灵脉签色**：`drawNameBanner` tint 分支改**两道** —— 同路径再敷「向纸色提亮 0.40」渐变
  （`A0/A1=0.30/0.46`），描边 `mixRGB(tint,[72,58,40],0.52)`，印章本色 0.86。
- **判据**：新增 `verify/check_settings_store.mjs`（offline，真跑 store.js）+ `verify/check_world_ledger.mjs`
  （live，会真实开一世）；均入 `run_regression`。`verify/cdp_feat.mjs` 的 HUD 量测改指 `btnGear/settingsWrap`。
- **回归 4 条真红的归因**（细则 → skill §39）：
  · `check_fish_skin` G5/G9 —— **瞬态**（并行任务正改 `mapgen.js`/`check_fish_skin`，跑动中读到中间态）；
    改完复跑 **全绿**。
  · `frontend_smoke` ×2 —— **我引入的真红**：(a) 注释里写了完整字面量 `classList.toggle('off')` 被
    raw-source 守卫命中；(b) 设置值局部变量取名 `st` 撞「解码字段契约」。改名 `sv` + 改注释 ⇒ **全绿**。
  · `check_mm_ui` ×1 —— **我引入的真红**：`statVeinBanner` 漏 `var` ⇒ strict 模式 ReferenceError
    （纯源码守卫查不出，只有"真页面无 JS 异常"抓得到）。补 `var` ⇒ **全绿**。
  · `check_calc_local` ×5 —— **既有隐患（非本轮引入）**：`armCalc` 的 3s 看门狗抢在引擎脚本到货前
    `settle`，之后 `load().then(settle)` 因 `CALC.settled` 已成 no-op ⇒ `CALC.local` 恒 false。
    证据：`--wait=30000` 拉长观察窗后 `ready:true / hashStale:false / 逐位比对 ✔` 而 `local:false` 依旧；
    服务端脚本下发经独立 WS 探针确认正常（`name:"engine"` 53KB gz / 124KB 文本）。`armCalc` 不在本轮
    diff 内 ⇒ **未改**，已记 skill §39.4 待用户拍板。
- **环境**：`check_calc_local` / `check_mm_ui` **不认 `--base=`**（只认位置参数 URL）；本轮用隔离实例
  8150 + 构建目录 `verify/_vmsrv`。⚠ 并行 Edit 同批 2 条只落 1 条，复现 2 次 ⇒ 一次改一处 + grep 复核。
�)。`densestRef` 已改成同容差语义 (但计数/并集/质心走另一条代码路)。
- T3 内联预览已同步 (`sync_preview_inline --check` 归零); T4 离线全量见下; T7 记忆: 本文件 + 
  `MEMORY.md` (「最密格」四修口径) + `DETAILS-引擎与表现层.md` (新增四节, 并把 MEMORY.md 的
  「前端自算地形」「小地图 R12/R13」「构建验证坑」整段迁入以压缩其体积);
  T8 skill 增补 §32 (参照系不能自证) / §32.1 (同容差语义) / §33 (离群免疫要恒等式) / §34 (数值探针优先)。
- 交接单遗留待拍板项未动: 灵脉名叠字 (是否砍「灵脉/岩峰」后缀) —— 等用户。

## T6 清理 + T9 收尾

- **T6 已执行**: 白名单删 41 个未跟踪临时件 (`_t13_diag*.mjs` ×14 / `_t13_out*.txt` ×14 /
  `_pa_out.txt` `_reg_out.txt` `_sync_*.txt` ×4 `_plaq_final.txt` `_plaq_live_log.txt` `_vmbuild.txt`
  `_vmsrv.log` `_net.txt` `_tracked.txt` `capture.png`) + 递归删 `verify/_vmsrv/`。
  姿势: Node `fs.unlinkSync` 绝对路径 + **先 `git -c core.quotepath=false ls-files` 过滤** +
  basename/数量断言 (`41/41`)，`_vmsrv` 删前验 `ZongMen.dll` 哨兵。**保留** `anc_fixture.json`
  `check_plaque_align.mjs` (被跟踪) 与 `_scratch_diag.mjs` (被跟踪)。
- 独立实例 (:8157) 已 kill (按 `netstat -ano` 取 PID→`tasklist` 验 `dotnet.exe`→`taskkill /F`)。
  ⚠ WMIC 已进本机程序黑名单 (security policy 拦截) ⇒ 查进程改走 `netstat -ano` + `tasklist`。
  ⚠ 本会话 PowerShell 工具**输出被吞** (exit 0 但无 stdout) ⇒ 诊断一律走 Node。
- 清完复跑 `check_plaque_align` = **68/68 全绿**、`run_regression --offline-only` 全绿。
- 文档计数订正: `MEMORY.md` / 本文件 / `SKILL.md:549` 的 39/66 条 → **68 条** (A6b + A13c1/A13c2 计入)。
- ⚠ **发现文档-磁盘漂移**: `verify/_vv_crop.mjs` 与 `verify/_vv_pick.mjs` **磁盘上不存在**，
  但被 4 处当「常驻工具」引用 (`.gitignore` 注释「别按前缀删」、`渔村贴图…规划.md` ×4、
  MEMORY/DETAILS/skill)。排除本次误删 (不在 T6 候选名单、会话开始前即缺) ⇒ 疑似更早会话误删
  或只在另一台机。**待用户确认**是否重建 (接口已在文档里: `_vv_crop.mjs <in> <out> x0 y0 x1 y1 [scale]`
  自包含 PNG 编解码 / `_vv_pick.mjs <seed> --R=10 --D=8`)。

## 五修 · 口径订正 (用户拍板「要中心点」，推翻前四修思路)

用户原话: 「要和当前的城市的中心点, 还有灵山的中心点位置一样, 而不是什么所谓的平均值
或者什么参照物」。⇒ 上面那一整轮 T1~T9 的方向是错的: 我 (以及前四修) 都在**用统计量去猜
中心**, 而中心在数据里**本来就存在**。统计量不是错, 是**解错了题**。

- **权威定义找到了 (就在明面上)**: `mapgen.js growTownFootprint` 把**核心建筑** (祠堂/村口/
  宗祠/集市/官衙/祖师殿…) 恒定放在**中心格** (`cell.d === 0` 那一支; 建筑记录带 `terrain:'core'`)。
  离线跑引擎实测 (契约 E1): seed42/777 共 **465 座聚落**, `core.q,r === st.q,r` **全部成立**,
  且中心格恒在 buildings 清单里 ⇒ 「点 = 聚落中心格」**恒落在真建筑上**。
  回头看原始病灶: "点悬在村里空地上"的真因是初版**合成点** (中心格 x + 建筑格 y 的 p25),
  **不是**"中心格不够中间" —— 前四修每一修都在给上一修的副作用打补丁。
- **改动 (web/js/main.js, 都很小)**:
  · `ANC_GEO` 缺省 → `'center'`; 旧 `densest|box|col|med|sum` 全留为 A/B 档位。
  · `bldgAnchor` 默认分支 = `{ x: st.x, y: st.y, real: 中心格上有无建筑 }` —— **不调求解器**;
    求解器只在历史档位分支; 清单未到货**不落缓存** (否则 `real` 永久 false)。
  · 灵脉签新增 `VEIN_PT` (缺省 `'center'`): 落点 = `w2s(v.x, v.y)` (灵脉格心 = 地盘色环/
    灵脉花所在处); 旧 `'apex'` 档保留。**两档签位逐像素相同** —— 中心点档靠 `gap = topU*hexR*z`
    把签抬到峰上 (点不动只抬签) ⇒ 视觉差别只有"引线终点从峰尖下移到峰体中心"。
  · 探针加 `anc/veinpt/coreOn/ctrX,ctrY` (可逐点核 `dotX == ctrX`)。
- **契约**: `check_plaque_align` 68 → **79 条全绿**。D6/D6b/D7 重写为五修结构守卫; 新增 **E 段**:
  E1/E2 = 引擎交叉源 (465 座); E3~E7 = 抽 main.js **真实源码**
  (`new Function('geo','BI','location', …)`) 跑行为 (缺省档落点==实体坐标 / 加离群点**逐位不变**
  / 中心格无建筑 real=false / 不落缓存 / 历史档位仍复现); E8/E9 = 灵脉签结构守卫。
  另订正 `bldg_ink.js` 两处**注释与实现不一致**: `centerPiece` 头注释仍写"截断核分 Σmin(d,2R)",
  而实现早已是"最密束 2R 邻域并集质心" (那份截断核分正是本轮 T1 实测**否决**的方案)。
- **实机** (临时实例 8141, 仓库外构建 + 三个显式 override): `anc=center veinpt=center`,
  载入 12 座聚落 **coreOn 12/12**; 在屏 2 座的 `dotX/dotY` 与中心点投影 **偏差 0.000px**;
  灵脉 48 条 `topU∈[2.273,6.186]` (签位公式未动)。
- **回归**: `run_regression --offline-only` **红 0/18** (唯 warn `w3_bfs_road` 墙钟门槛 ——
  本次机器同时在跑 Chrome+dotnet, 读数涨到 1603/2319ms, 性质仍是机器绝对速度, 非回归)。
- **两个环境坑 (→ skill §35.2/§35.3)**: ① 后台进程**活不过一个回合** (同一回合内探针正常、
  下一回合全超时且端口 CLOSED —— 看着像产品回归); ② 仓库外实例的 `/api/debug/snap` 跟着
  `FindRoot` 落到**实例自己目录**旁 ⇒ `live_cap` 必超时、但探针其实已写出。

## A2 (2026-09-16 用户定案) · 水面建筑一律走渔家皮肤 + 地盘环水陆都画

用户原话: 「所有的建筑都改成这样的, 而不是只有渔村才有这些建筑, 只是改一下统计显示贴图的方式;
你把中心格子这个判定去掉, 只要是水里的建筑必须有这些; 其次是地盘环都要画; 测试环境不用清库」。

- **引擎** `Engine/js/mapgen.js` (地皮判定段, 原 R5b): 「渔家」地皮从 `type==='fishing'` **放开到任何聚落
  的水面格** (`lu === '水岸'` → `'渔家'`)。⇒ 沿海集镇/村落的水上格不再只有码头, 出民居/仓库。
- **前端** `web/js/main.js`:
  · `1399 var WATER_KIND` 白名单 (民房/仓库/码头/渔船坞/渔亭) + `1403 var WATER_OLD` A/B 档位 (`?water=old`)。
  · `1546 bridge = onWater && !isFish && (WATER_OLD || !WATER_KIND[b.kind])` ⇒ 栈桥降级为**白名单兜底**。
  · `1552 fishVillage: WATER_OLD ? isFish : (isFish || onWater)` ⇒ 水面格走 KINDS_FISH 渔家皮肤。
  · `1563 if (!bridge || !WATER_OLD) {` ⇒ **地盘环无差别画** (旧口径 `if(!bridge)` 跳过水面格 = 用户报的
    "下面没有正六边形的框框")。
  · `tools/bldg_town.mjs` 镜像判据同步 (旧注释还写"生产代码同一判据 = isFish", 会漂)。
- **契约**: `check_fish_skin` (G5/G9 重写) ✔ / `check_sea_village` (新增 A2 段) ✔ /
  `sync_preview_inline --check` ✔。离线实测: 水面格建筑 4367 座 (民居 61.8% / 水工 36.7% / 核心 65),
  含水面格的聚落中 **787 座是非渔聚落**。
- **取证**: ① 离线确定性平面图 `tools/bldg_town.mjs a2test --q=25 --r=-73 --R=46 --nolabel` —— 水里
  8 民房 + 5 仓库 均为**带茅顶干栏吊脚楼**, 不再是光板桥; ② 实机同机位 A/B (`verify/_a2_new*.png` vs
  `_a2_old*.png`, 临时实例 8157 + seed `a2test` 的乌衣镇 11 水面格)。
- **⚠ 归因边界**: `frontend_smoke` 唯一红项 (`st.clouds/veins/nameRegion/…` 读点) 来自**另一个任务**
  的设置重构 (`web/js/store.js` ZMStore + `S.settings.all()` → `main.js:68-92 applySettings`),
  **与本改动无关** —— 用户已明示「另一个任务再加新东西你不用管」。



---

## 2026-09-16（另一任务 · W 世界种子 / 齿轮设置组件 / 灵脉签色）——与上面 A2 任务并行

用户四项：① seed 改由服务端产生并落 sqlite ② 右上「另启一世」随之修 ③ 右上收成一枚齿轮 ⚙，
点开弹窗（显示开关）+ 存 localStorage，要一个**全局存储组件** ④ 灵脉牌匾敷对应（五行）色，淡一点叠在纸签上。

- **① 服务端种子台账**：新增 `Server/Zongmen/Storage/WorldLedger.cs` —— **独立表**
  `World(Round,Seed,BornAt)`（⚠ 不能塞 `Data` 表：`PruneExcept` 会静默删）。`MapWorldService` 加
  `WorldCurrentJson/WorldNextJson/WorldListJson` + stats 报 `worldRounds`；`MapEndpoints` 加
  `GET /api/world/current`（幂等）/ `POST /api/world/next` / `GET /api/world/list`。CSPRNG 8 位种子。
- **②③ 前端**：新增 `web/js/store.js`（`window.ZMStore`，键 `zongmen.settings.v1`，schema
  `{veins,nameSettle,nameVein,nameRegion,clouds}`，白名单/200ms 节流/坏 JSON 降级/`pagehide` 强写 +
  通用 `read/write/clear(ns)`）。`index.html` 把 7 个按钮收成 `#btnGear` + `#settingsWrap` 弹窗
  （5 个 `data-zm` 复选框 + 轮次/种子/来历 + 另启一世 + 山河 stats）；`main.js` 加
  `applySettings/syncSettingInputs/openSettings/worldFetch/setWorldInfo`；boot 改
  `await worldFetch('/api/world/current')`；「另启一世」= POST next；`?seed=` 仍为调试覆盖。
- **④ 灵脉签色**：`drawNameBanner` tint 分支改**两道** —— 同路径再敷「向纸色提亮 0.40」渐变
  （`A0/A1=0.30/0.46`），描边 `mixRGB(tint,[72,58,40],0.52)`，印章本色 0.86。
- **判据**：新增 `verify/check_settings_store.mjs`（offline，真跑 store.js）+ `verify/check_world_ledger.mjs`
  （live，会真实开一世）；均入 `run_regression`。`verify/cdp_feat.mjs` 的 HUD 量测改指 `btnGear/settingsWrap`。
- **回归 4 条真红的归因**（细则 → skill §39）：
  · `check_fish_skin` G5/G9 —— **瞬态**（并行任务正改 `mapgen.js`/`check_fish_skin`，跑动中读到中间态）；
    改完复跑 **全绿**。
  · `frontend_smoke` ×2 —— **我引入的真红**：(a) 注释里写了完整字面量 `classList.toggle('off')` 被
    raw-source 守卫命中；(b) 设置值局部变量取名 `st` 撞「解码字段契约」。改名 `sv` + 改注释 ⇒ **全绿**。
  · `check_mm_ui` ×1 —— **我引入的真红**：`statVeinBanner` 漏 `var` ⇒ strict 模式 ReferenceError
    （纯源码守卫查不出，只有「真页面无 JS 异常」抓得到）。补 `var` ⇒ **全绿**。
  · `check_calc_local` ×5 —— **既有隐患（非本轮引入）**：`armCalc` 的 3s 看门狗抢在引擎脚本到货前
    `settle`，之后 `load().then(settle)` 因 `CALC.settled` 已成 no-op ⇒ `CALC.local` 恒 false。
    证据：`--wait=30000` 拉长观察窗后 `ready:true / hashStale:false / 逐位比对 ✔` 而 `local:false` 依旧；
    服务端脚本下发经独立 WS 探针确认正常（`name:"engine"` 53KB gz / 124KB 文本）。`armCalc` 不在本轮
    diff 内 ⇒ **未改**，已记 skill §39.4 待用户拍板。
- **环境**：`check_calc_local` / `check_mm_ui` **不认 `--base=`**（只认位置参数 URL）；本轮用隔离实例
  8150 + 构建目录 `verify/_vmsrv`。⚠ 并行 Edit 同批 2 条只落 1 条，复现 2 次 ⇒ 一次改一处 + grep 复核。
  ⚠ bash heredoc 追加中文进本文件**静默写 0 字节**（改用字节级 appendFileSync 才落盘）。

## 十四版 · 灵脉「地盘彩环」只垫了中心 1 格（用户报障，当天修复）

**用户原话**：「大灵脉 1 格外面 6 格，中的是 1 格下面 2 格 —— 但是在显示地上的地盘彩环的时候
没有对应的另外 6 格和 2 格 格子显示 你现在要修复他」。

**根因**：十一版（2026-09-15）只把 **峰体** 的占地改成按档 7/3/1（引擎 `mapgen.js veinFootKeep`），
而 `main.js` 画**地盘色环**的那段（R8）**只 `hexPath(ctx, v.x, v.y, …)` 垫了中心 1 格** ——
同一条「占地」语义在表现层有**第二处拷贝**，改引擎时漏跟。

**改法（单一真源优先）**：
- `web/js/vein-skin.js`：新增 `FOOT_OFF`（偏移池 = 本格 + 六邻，序同 `mapgen.NEIGH_SLOTS`）、
  `FOOT_MIRROR`（档→下标：大 `[0..6]` / 中 `[0,2,3]` / 小 `[0]`）、`footOffsets(level, MG)`。
  ⚠ **优先问引擎**（传了 MG 就直接用 `MG.veinFootKeep` 判），镜像表只在引擎缺席
  （`EngineLocal.load` 失败 ⇒ 主视图降级服务端下发）时兜底。导出 `footOffsets` / `footOff`。
- `web/js/main.js`：地盘环改成**铺满该档占地**；先铺满全部格再统一描边（免得后一格半透明填充
  盖住前一格的边）；本格恒用封包的 `(v.x, v.y)`（与灵脉花/名牌同点的契约不变）。
  新增**归属裁决** `veinOwnerOf`：引擎侧 `veinNear(q,r)` 是在 9 宫格群落内取 hexDist 最近的
  灵脉 ⇒ 前端就**已加载**群落集合用同一条规则；`veinAll` 先按 (q,r) 排序 ⇒ 结果与 comm 包
  到达顺序无关（「幻影灵脉」撞格恒取 (q,r) 小者）。另加海域过滤：`elevAtTile` 已知
  `< geo.seaLevel` 的从属格**不铺**（引擎在海里不长峰），海拔未到货（-1）时**照画**
  （宁可多铺，不能因视口边缘没数据把环吃掉）。
- 验数：`statVeinRing` / `statVeinRingLv[4]`（本帧格数，按档）+ DEBUG 下 `statVeinRingRows`
  （逐根 `{name,lv,n}`）进 `__feat()`；新增 `?veinprobe=1` 出口（走 plaqprobe 同一条
  「页面自回传」通道 POST `/api/debug/snap`）。

**证据**：
- 离线：`check_vein_cluster` 新增 **§D 14 条**（镜像表 vs 引擎 `veinFootKeep` 跨源逐值 ×4 档、
  偏移池序 == NEIGH_SLOTS、中档 == `[[0,0],[0,1],[-1,1]]`、源码守卫「已接线 + 旧写法不得复活 +
  探针齐备」）⇒ 该判据 **12 → 26 条**，全绿。
- 实机（**用户的 8140，只读，`?seed=42` 调试覆盖不入账** + `live_cap.mjs` + `?veinprobe=1`）
  机位 `qt=-248&rt=-178&zm=2.4`（正对 `check_vein_cluster` 打印的那根大灵脉「岩层断崖」）：
  39 根在场灵脉 → **大 9 根全 7 格 / 中 10 根全 3 格 / 小 20 根全 1 格**，合计 113 = 63+30+20；
  页面无 JS 异常（strict 模式下漏 `var` 这类问题只有真页面才抓得到，探针正常回传即证）。
- 离线全量回归 **红 0 / 19**（唯一 warn = `w3_bfs_road` 墙钟慢机门槛，非回归）；
  `frontend_smoke` 79 条全绿。

**顺带（MEMORY.md 超限被截断）**：按注入提示二次压缩 MEMORY.md（13.5KB → 约 8.8KB），
把「渔村 A2 / 归属势力 B / 匾额 C / 世界种子 W」四节的细则整段迁进
`DETAILS-引擎与表现层.md`，MEMORY 只留契约与指针。

<!-- ⚠ 教训: `node -e "…"` 里含**反引号**的中文会被 bash 当命令替换吃掉 ⇒ 中文长文一律
     写脚本文件再 `node <file>`（本轮已踩一次，切回 15050 字节重写）。 -->

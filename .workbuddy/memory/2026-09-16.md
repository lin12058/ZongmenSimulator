# 2026-09-16

- 用户再报「村庄名字下竖线/点与村庄、灵脉对不上」。定位并修复三处根因:
  1. **灵脉签垂直落点**: `VS.tipU` 旧返回方框顶 `H-propBottomU`，可见峰尖低 `apexV*H`(≈1.1uR/27px) ⇒ 悬空。新增 `apexV/apexJx/apexOf`，`tipU(l,e,hash)` 改 3 参随精灵真 hash。⚠ apexV 第一版推导有符号错，最终以 textures.js veinPeakPts 实际多边形为准。
  2. **灵脉签水平**: 峰体 shader 横抖 `jx=±0.9uR` 未计入 ⇒ `propHashAt`(纯海 chunk propCenters/propSprites/propHashes 为 null 必须判空) + `veinJxU`，画签 x 加 `jxU*hexR`。
  3. **聚落锚点三修**: 列最近被偏心选址格拉到边缘孤屋(归元宗 3.00R)→ 中位格 → **最密格**(2·hexR 邻域邻居数最多，决序「离截尾质心近→更北→更西」)，两套种子 fixture 评比最优且半径 1.2~2.5R 不敏感。样本 `verify/anc_fixture.json`。
- 契约 `check_plaque_align.mjs` 重写 (A13/B/C/D)，挂 `?plaqprobe=1` 数值探针 (含原始建筑格 bldgs 字段)；?ancgeo=box|col|med|sum A/B 档位保留。
- **未完**: A6 红灯——最密格平手参照 `trimmedCentroid` 被离群均值污染 (加一块远农田翻转 (1,0)↔(1,2))；修法 = 平手第一键改截断核分 `Σ min(d,2R)` (恒离群免疫)。bldg_ink.js 需再 sync 预览、离线回归需重跑。
- **交接**: 待办事项/交接-2026-09-16-匾额灵脉对齐收尾.md (现状/红灯根因/T1~T9 步骤/符号速查)。

# 2026-09-07 山河图无限区块地图 — 续修完成

## 接手状态
前一Session已重写 mapgen.js / renderer.js / main.js 为「我的世界式」无限流式区块（六边形区块、地块直径16px、渐入丝滑加载），但存在黑楔形空洞。

## 根因与修复（均已验证）
1. **区块楔形黑洞**：四候选最近中心归属本身正确（全局最近、无重叠），但胞腔在斜向延伸至 14 格，而 `buildChunk` 只扫描 `CHUNK_R+0.5=10.5` 格 → 13~14 格处的楔形地带无人认领。修复：`CHUNK_SCAN=15`（node 脚本验证胞腔最大半径=14，平均每区块 441 格）。
2. **dpr 坐标系错位（最隐蔽）**：GL 渲染以设备像素为 uRes，main.js 相机/视野边界/流式需求集全按 CSS 像素 → dpr=1.5 时 GL 实际渲染世界范围比加载范围大一圈，外环永远黑（只有右上角明显，因左边有预载环遮掩）。修复：renderer 增加 `this.dpr`，hex/post 两 pass 的 uRes 一律传 `fboW/dpr`（NDC 与分辨率无关，FBO 仍设备分辨率高清渲染），同时顺带修正了 overlay 标注与 GL 地图的 dpr 缩放错位。
3. **流式需求集**：旧「4角各自3x3邻域」在低缩放漏屏幕中部区块。改为：视野四角色坐标包围盒 + 外扩 `CHUNK_SCAN+CHUNK_S`（胞腔半径+预载一圈）→ 区块索引范围覆盖式枚举；队列积压>6 时每帧构建 2 个。
4. **小地图**：点击换算错误（误用 backing 尺寸 432 且 y 也误用 132）→ 修正为 132×88 基准 × SCALE 6；刷新节流 `(dirty && >0.4s) || >1.5s`，避免区块生成期间逐帧 1.2 万格采样卡顿。
5. **内容**：灵脉海拔阈值 0.76→0.72（初始视野灵脉 0→3）；道路 z≥0.8、灵脉 z≥0.7 才绘制（远景无意义且避免 A* 批量首算卡帧）。

## 验证手段
agent-browser 实测（注意：**daemon 在多条 bash 命令之间会重启丢页面，open/eval/screenshot 必须串在同一条命令里**；截图易被 SIGTERM 杀，用页面内 `__snap()` canvas 导出 dataURL 最稳）。zoom 0.7 全屏 100 探针 0 空点；换种子+远距平移全覆盖；点击信息面板正常。

## 二轮验证（续）
- buildChunk 实测 0.5ms/区块（热缓存），流式构建无卡顿风险。
- 聚落图标/名号与地块对齐（望舒城居中验证），道路虚线 z≥0.8 正常显示。
- 另起一界（种子切换重生成）、注记开关、小地图点击跳转均通过；小地图跳转落点 314 ≈ 理论 317（132×88×SCALE6 换算修复的数值级验证）。
- 临时快照文件已清理，项目目录保持 assets/index.html/js/server.js/方案.md。

## 项目约定（新增）
- 相机与世界坐标一律 CSS 像素；GL 侧 uRes 必须除以 dpr（本项目 dpr=1.5 环境）。
- 区块归属=四候选最近中心（六边形距离），构建扫描半径必须 ≥ 胞腔最大半径 14（现取 15）。

## 灵脉驱动地图重设计（决策已锁定）
产出文档 `灵脉地图设定.md`，核心原则「先定灵脉，后造山河」：
- **群落结构**：1 大灵脉 + 中[0~3] + 小[0~7]，向心密度（越靠中心越密）。
- **五行相生相克只在群落内部**作用，群落之间互不干涉（推翻旧版跨群落相克压制）。
- **地貌不做硬绑定**：同灵根候选池随机组合（水=潭/瀑/海眼；火=熔岩/火山/温泉…），种子确定性。
- 明确待定：异灵根具体种子函数、大中小密度随图幅曲线、七星贴图六触手美术。
- 下一步候选：据此重构 mapgen.js 的灵脉/区域生成管线。

## 二轮设定增补（已成稿 `灵脉地图设定.md` §九~十三）
- **灵气场中心衰减**：原点(0,0)灵气最足，直线距离到 1000 处灵气归零，参考《无尽的拉格朗日》；密度曲线可配置。
- **群落半径默认约 40**；生物群系与建筑分开分层：①灵气场→②群落生态→③灵脉布局→④建筑/聚落选址→⑤道路连接。
- 建筑群(城市/市场/聚落)生成依据：灵脉亲和、资源吸引、地形适配、互斥间距、灵气梯度逐级降繁华。
- 所有数值抽入配置文件，不写死。本次会话只做设定不下代码。

## 灵脉驱动地图重构落地（本轮实施，均已浏览器验证）
- mapgen.js：删除旧"高山发源下山入海"点线灵脉，改为灵脉驱动——
  ① 灵气场 spiritAt：原点 (0,0)，欧氏像素距离衰减，半径 1000 格（16000px）归零，指数 0.8；
  ② 群落 communityOf：晶格 COMM_CL=150 确定性播种大灵脉，存在概率 0.16+0.62*spirit，群内 1大+[0~3]中+[0~7]小 向心聚敛（pow(u,0.55~0.65)），间距 D_L_M=18/D_L_S=12/D_M=14/D_SMALL=8；
  ③ 五行：SHENG=[2,3,1,4,0]，KE=[1,4,3,0,2]，DUAL={'0|3':'雷','1|2':'风','0|2':'冰','3|4':'暗'}，发育 roll：<0.44 主根 / <0.78 相生(30%派生异灵根) / <0.90 远相生 / 其余相克(55%异变)；地貌候选池 LANDFORM/VARIANT_LANDFORM 按种子取名；
  ④ 地形迁就 elevAt：灵脉中心抬 0.80/0.75/0.70（大/中/小），d=1 从属 -0.05，d=2/3 部分抬升过渡；基础海拔<海平面则抬成岛（中心海面+0.16）——「灵脉必是山/岛」；
  ⑤ 灵脉格 disp biome 8..12（金木水火土）进 GL 图集；veinNear per-tile 缓存。
- textures.js：图集 8 列×5 行——0~3 行群系×4 变体，第 4 行新增 5 灵脉格贴图（暗岩底+灵根色晕+符纹：金剑/木脉/水浪/火焰/土台）；导出 drawVeinFlower（七星花）。
- renderer.js：uAvg[13]；灵脉格寻址 cell=(biome-8,4)、uv 分母 vec2(8,5)；灵脉格跳过邻居晕染。
- main.js：overlay 灵脉点线 → 群落灵气晕圈 + drawVeinFlower 七星花 + 灵脉名牌(z≥1.0)；信息面板灵脉/灵根/位份行；统计 countVeins；小地图用 disp。
- **关键 bug 修复**：HEX_FS `floor(vTile/4.0+0.5)` 解码偏移——variant 2/3 被解码成 biome+1（旧版群系同样中招），土灵脉 variant2/3 落到图集未绘制格→纯黑六角。改 `floor(vTile/4.0+0.001)`。
- 聚落耦合：存在概率 0.20+0.38*spirit；灵脉域内(dist<COMM_R*1.4) 宗门概率 0.24、人口 ×1.3。
- 验证：seed=lingmai 实测 56 灵脉；spirit(0,0)=1.00/900=0.30/1200=0.00；海上土灵脉群落抬出七星岛+花蕊+名牌+晕圈全部正常。截图存 assets/snap_vein_island.png、snap_vein_overview.png。

## 环境注意（新增）
- **8137 端口被 D:\codes\宗门模拟器demo3 的服务占用**（demo3 为此前迭代副本，已有另一版灵脉驱动实现）；demo2 验证用 `node server.js 8138`。
- agent-browser 须一条 bash 命令串完 open/eval/screenshot/close，且需 dangerouslyDisableSandbox（沙箱 SIGTERM 杀 daemon 链）；eval 用 Promise 轮询等 window.__MapGen 就绪；无头 Chrome --screenshot 在本机不落盘，用 agent-browser screenshot。

## 二轮增补（灵根生态 + CFG 配置化，已验证）
- **§十二 CFG**：群落/间距/灵气场/抬升/生态系数全部收进 `MapGen.CFG`，`MapGen.configure(patch)` 覆写并清 commCache/veinNearCache/elevCache/fieldCache；main.js 用 MG.CFG.COMM_CL/COMM_R。
- **§六 灵根生态呼应**：fields() 中灵脉 d≤3 权重 ew=1/0.55/0.25，水+湿0.25、木+湿0.18、火-湿0.22+热0.15、金-湿0.08 → 灵脉周边自然长出对应群系（水灵脉绿林环、火灵脉焦土感）。Node shim 冒烟测试：5×5 晶格 17 群落 100 灵脉。
- 浏览器验收：水灵脉「深潭」七星 + 蓝色水灵贴图 + 湿润林环 + 金灵脉格 + 道路/聚落全正常；截图存 assets/snap_vein_eco.png。
- Node 冒烟测试 shim：须先 require noise.js 再用 window.MapGen（require 返回值是空 module.exports，勿覆盖 window.NoiseLib）。

## 道路 A* 帧内卡顿修复（二轮）
- 根因确认：roadsNear 中 astar 失败不缓存（临海聚落每帧重跑全图探索）+ 首算发生在 drawOverlay 绘制帧；次要：三大缓存满 15 万条整体 clear() 造成周期性重算尖峰、虚线描边开销随 zoom² 增长。
- 修复（mapgen.js + main.js）：①roadFail Set 失败缓存（判定一次终身跳过，init 时清空）；②roadsNear(i,j,maxNew) 预算制，绘制帧传 0 纯读缓存；③新增 warmRoadsStep(camQ,camR) 主循环渐进预热，每帧至多 1 条 A*（z≥0.8 才跑，5×5 区域格旋转扫描）；④elev/field/veinNear 满额改 evictHalf 淘汰旧一半；⑤z>2.4 道路虚线改实线；⑥信息面板点击 roadsNear 预算 1。
- Node 实测：4 遍收敛，收敛后绘制帧调用 1000 次=0ms；warmRoadsStep 11 帧补齐。残留：单条病态失败 A*（海岛聚落 60000 节点 guard 跑满）单次仍可达 ~550ms，因 roadFail 终身只发生一次。

## 美术升级：立体精灵渲染（Battle Brothers 式压格 + 水墨整合）
参考图①觅长生式柔和山水晕染 ②战场兄弟式山体/树木超出格子。方案：保留水墨六边形底图 + 新增 Pass1.5 立体精灵遍。
- **图集扩展**（textures.js）：7 行制——第 5 行精灵：0/1 山 2/3 雪 4..7 林（4 变体）；第 6 行：0 沙 1 草丛 2..6 金木水火土灵脉峰。新画师 propShadow（椭圆软墨接地投影）/drawPropPeak（透明底渐变山+脊线+披麻皴+雪帽）/propTree（枯笔干+椿点冠）。山地/雪峰/灵脉格底图改为纯地表质感（碎石点/丘影横皴），山峰全交精灵承担；灵脉符纹+山峰移入灵脉峰精灵。
- **mapgen.js**：propSpriteFor(f) 按格分配（灵脉 b+42；山 40+hash2；雪 42+；林 44+hash4，55% 概率；沙 48@30%；草丛 49@10%）；buildChunk 产出 propCenters/propSprites/propHashes（r 外层递增=天然 y 升序遮挡序）。
- **renderer.js**：新增 progProp（PROP_VS/PROP_FS）：精灵 quad 宽≈六边宽×1.4~1.8、高≈2.3~3R，底部锚 center+0.78R 压向下一格；hash 控制翻转/抖动/缩放；独立 propFbo + alpha 混合（精灵层不污染底图 alpha 编码的 biome id）；atlas 另建 LINEAR 拷贝 texAtlasLin 供精灵柔化采样。POST_FS 合成顺序：底图晕染外渗 → 精灵合成 → 底图墨线/海岸线 → 精灵剪影梯度勾边(propInk) → 剪影下移采样接地投影(只落陆地) → 宣纸/暗角。
- **工具**：agent-browser screenshot 正确语法是**位置参数**路径（不是 --path，会被当 selector 报 Element not found）；jpeg 格式 (--screenshot-format jpeg) 不易被 SIGTERM；png 大截图常被 SIGTERM 杀。最终验证改用 present_files 开 localhost 内置浏览器预览（用户指定）。

## 视觉二轮：参考图对齐（本轮完成，headless Chrome 截图验证）
用户给参考图（奇幻地图风）要求改五点，均已落地：
1. **山高随 noise**：mapgen buildChunk 新增 propElevs 数组；renderer PROP_VS location=4 iElev，山 mix(0.55,1.30,(e-0.70)/0.14)、雪 mix(0.95,1.55,(e-0.84)/0.12)、林沙草 hash 微变、灵脉峰 1.05 固定；低山 e<0.76&&hash<0.30 无精灵（山谷留白）。
2. **图集 7→8 行**：第 7 行 56/57 山B、58/59 雪B（三峰横岭构图，打破两变体壁纸感）；HEX_FS uv 分母改 vec2(8.0,8.0)、atlasRows=8。
3. **树木**：TREE_PAL 四调色板（阔叶/松/花树粉点缀/秋色橙），propTree 多层球冠（暗底冠+主冠+受光高光+花色）；drawPropPeak 增受光面/背光面/岩层横裂/锯齿雪帽/挂雪沟槽；林地格贴图改双色冠+高光。
4. **道路**：overlay 双色土路（深路缘+米黄路面，实线，宽随 zoom 1.5/2.1）。
5. **海浪静态**（用户二次明确：不要动画，要参考图白描浪线）：main.js overlay 新增静态浪线层 z≥1.0——开阔海面 hash 驱动长浪线+短回笔（深海 prob 0.30/浅海 0.46），近岸浅海画白沫弧+沫点；POST_FS 近岸白沫全部去 uTime（ring 1x/3.4x/7.5x 三级采样）；HEX_FS 波纹去 uTime。POST_FS uTime uniform 已移除。
- 另：页面新增 URL 定点参数 qt/rt/zm/nofade（headless 验证用）。
- 验证工具变更：agent-browser 连单独 screenshot 都 SIGTERM（彻底不可用），改用系统 Chrome headless（webgl-headless-verify skill）+ nofade=1，截图 Read 验证后即删；项目临时文件删除用 PowerShell（bash rm/cmd del 会被 SIGTERM）。
- 端口：本项目 8139（server 常驻）。

## 三轮微调（同日续）
- 水面基色向参考图青绿调校：深海 #95a5ad→#8fa6a9、浅海 #b4c2c4→#aec7c3（textures.js painters 0/1 + mapgen BIOME_META 同步，图例/小地图一致）。
- 浪线密度上调：深海 prob 0.24→0.30、浅海 0.38→0.46。
- 回归验证：出生点默认视野（seed=42）树木花色/秋色、山体海拔层次、土路、灵脉花、小地图新水色全部正常，无回归。

## 视觉三轮微调（18:20 前后，参考图驱动，均已截图验证）
- 水色改碧蓝：深海 #8fa6a9→#6d9aab、浅海 #aec7c3→#9dc2c9（textures.js painters[0/1] + mapgen BIOME_META 同步）。
- 道路去「国道感」：删深色路缘+恒定宽双色，改为无路缘浅土黄（rgba(224,212,172,0.48)）逐段变宽（每路确定性 wBase 0.9~2.0 × 每段 sin 噪声 0.55~1.45）+ 宽淡柔光底层融入草地（main.js 道路层重写）。
- 树变小变密：propForest 树高 54~92→34~58、每精灵 2→3 棵；林地精灵生成率 0.55→0.75（mapgen propSpriteFor，注意变体映射系数同步改 5.34）；着色器林高 hs 0.80+0.45→0.62+0.34（renderer）。
- 草地微草坪：painters[3] 加 9~14 个 2~6px 椭圆亮/暗斑（alpha 0.05~0.09，几乎不可见只留肌理）。

## 视觉四轮：提亮 + 树压路 + 去 LOD（用户指令，均已截图验证）
- **全局提亮**：POST_FS 加 `col = col*1.08 + 0.022`；宣纸叠底 paper*1.12@0.48 → paper*1.28@0.38；暗角 0.20→0.10；HEX_FS 深海深度下限 0.76→0.84。
- **树压路（渲染顺序）**：道路从 overlay 2D 移入 GL——renderer 新增 progRoad（ROAD_VS/FS 世界坐标纯色）+ setRoads(halo,core) 动态缓冲，render() 在 Pass1 底图后、精灵 FBO 前绘制；关键：`blendFuncSeparate(SRC_ALPHA, ONE_MINUS_SRC_ALPHA, ZERO, ONE)` 保住 scene FBO alpha 通道的 biome 编码（墨线/海岸检测依赖）。main.js drawOverlay 只负责 CPU 展宽 segment 成三角面 push。overlay 里其他元素（浪线/标注）仍在精灵之上。
- **去 LOD/去高度取消**：mapgen 删低山 30% 跳过（山峰全量）；main.js 删浪线 z≥1.0、道路 z≥0.8、灵脉 z≥0.55、道路预热 z≥0.8 四处缩放门控（标注文字 z≥1.0 保留）。
- 用户观点：shader 计算量与精灵大小/缩放无关，不要为省性能砍特性。

## 视觉五轮：路加粗 + 水墨浪线 + 山脚黑线根因 + 草坪可见（均已截图验证）
- 道路加粗：wBase 0.9~2.0→1.4~2.9，段内 0.60+0.8*wob，core 下限 1.0。
- 浪线重做水墨风：白高光改淡墨（开阔海 rgba(64,94,104,0.16~0.26) 线宽 0.75，一波两段二次曲线+回锋短笔 0.6）；近岸沫弧改灰墨绿细笔 0.85；后处理白沫脊 pow3→4.5、mix 上限 0.45→0.28。
- **山脚下黑点线根因（两处叠加）**：① HEX_FS 邻域晕染把草地格边缘 mix 向深色山色 uAvg → 修为 `nb<5.5 && biome<5.5`（山/雪不参与晕染）；② POST_FS 底图墨线在山/雪群系边界照画 → e1 循环里记 mtnEdge（本格或邻格 6/7），inkLine 乘 (1-mtnEdge) 全抑制（山峰精灵自带勾边）。另有 propNear ±2.6px 精灵近邻衰减兜底林缘。
- 草地微草坪可见化：alpha 0.05~0.09→0.10~0.17，半径 2.5~7px，数量 12~18。
- **黑点线真根因（五轮补充）**：用户圈出的密排黑点线是山/雪精灵自带的「山脚碎石点」（5 颗/精灵，y≈102~107）+ 山脚晕染椭圆，密排时连成线；已从 propMountain/propSnow 删除。前一轮的晕染排除+墨线抑制是另一层叠加因素，两者都修才干净。

## 黑线终局（七轮）：真凶是 propShadow 接地椭圆
- 用户放大截图确认「黑线」= 山精灵脚下 3 个圆形渲染 → 即 propShadow(64,108,rx≈40,ry≈13, rgba(48,42,34,0.30)) 软墨椭圆，一排三精灵三圆，缩小连成黑线。
- 修复：propMountain/propSnow/propMountainB/propSnowB 四个山雪精灵全部移除 propShadow 调用（B 变体残留的 5/4 颗碎石点一并删）；树/草/沙/灵脉精灵的 propShadow 保留（树的接地正常）。
- 经验：报「线」的穿帮先让用户放大定位形状——圆/点/线对应完全不同的代码源；本项目山脚下装饰已累计删过三层（晕染 wash、碎石点、接地投影），都在 textures.js 山雪画师内。

## 无贴图模式：移除外部 paper.png 加载
- 用户反馈 http 打开比 file:// 打开亮很多 → 根因：main.js boot 异步加载 assets/textures/paper.png（扫描宣纸，平均亮度 242）替换程序化纸（#eee5d2，~229），POST_FS 的 paper*1.28 放大亮度差；file:// 下加载失败保留暗纸，http 下成功替换成亮纸。
- 修复：删除 main.js 中 new Image()/img.onload/img.src='assets/textures/paper.png' 整块，只保留启动时的 IT.buildPaper() 程序化纸。现项目 grep assets//new Image 无任何外部资源引用，file:// 与 http 渲染完全一致。
- node --check js/main.js 语法通过。

## 2026-09-07 修复: 打开页面 CPU 持续 100%
根因: 主循环每帧做三件昂贵梳理级工作, 无空闲出口:
1. updateStreaming 每帧全量重扫"需要的区块集合"+排序, 相机静止也照做。
2. drawOverlay 每帧重画整层世界锚定静态内容(浪线/道路带/区域名/灵脉花/聚落图标)。
3. warmRoadsStep 每帧触发 A* 道路预热, 相机不动时反复空转。

修复(main.js):
- updateStreaming: 相机位移<阈值(18/缩放因子)且 genQueue 为空 -> 直接 return(streamIdle)。
- drawOverlay 拆成 renderStaticInto(离屏 staticLayer, 相机超阈值才重绘) + 每帧 blit staticLayer + 动态 hover/selected 高亮。
- warmRoadsStep 节流为每 3 帧一次(warmFrame++%warmGap)。
道路坐标本就存 roadCache, 不可达进 roadFail, 均不再重算(本质是"建一次存坐标")。
语法 node --check 通过, headless 冒烟: stats 更新, fatal 未触发。

## 2026-09-07 修复: 拖动时城市/标注"一卡一卡"跳变(非 CPU, 是 cache 跳变)
根因: staticLayer 离屏缓存只按相机位移阈值整层重绘, 两次重绘之间 blit 原图不动,
底层 GL 却每帧平滑滚动 → 图标相对底图反向滑动再整体跳回, 视觉上"一格一格"。
用户感受为"卡在整型上": 其实是阈值跳变, 不是坐标取整。

修复(main.js drawOverlay):
- blit staticLayer 时按 (cam - staticCam)*zoom*dpr 做平移补偿 dx/dy,
  让缓存内容跟随相机逐帧平滑 (仅当 |zoom-staticCam.zoom|<0.02 才平移,
  否则内容缩放不匹配直接原位 blit)。重绘仍按阈值触发, 内容刷新后 staticCam
  更新, 平移归零继续累积, 全程无跳变。

## 21:53 出生点印章删除 + 详情坐标
- 删除 main.js overlay 绘制中的「出生点印章」朱红山河方块（原 444-454 行，世界坐标 150,90 处）。
- 山川志详情面板（showInfo）在「地貌」下新增「位次」行：轴向坐标 q/r 以 东/西·南/北 显示（负值自动换方位）。

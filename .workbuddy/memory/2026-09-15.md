# 2026-09-15

- 深夜收尾轮：九版三项（云集群九版-2 / 点选朱砂标记 / 灵脉山地底座）全部完成并通过验证——探针 6/6+7/7+12 断言、check_vein_skin 32/32、离线回归 10/10 绿；证据图 fx26/27/28 已归档。
- 应用户要求把**未完成收尾事项**整理成交接单：`待办事项/交接-九版收尾未完成事项.md`（T1 清临时件 / T2 停 8177·8178 / T3 台账 §O / T4 记忆更新 / T5 skill 增补 / T6 commit+8140 重启需用户确认）。
- 用户反馈：**8177/8178 已手动停掉**（T2 完成）；且接手 agent 在**另一台机器**上干活 ⇒ 交接单已补「跨机器须知」：改动未 commit 只在 A 机工作区，B 机需先同步代码、重跑验证；%TEMP% 临时件清单与 offline9.log 仅 A 机存在。
- **接手轮（机器 B = D://codes//ZongmenSimulator / i7-6700HQ / 15.8G / Win10 1903）**：完成交接单 T1/T3/T4/T5，T6 待用户确认。
  核实过：九版三项改动**已在 HEAD**（A 机已 commit+push 7f8486e，本机 pull 后 git diff 干净）；本机无 8140/8177/8178 监听，未起任何实例。
  - T1：清单 18 件在本机全不存在（符合「只在 A 机」）；另清上轮残留 9 件（`_expA/_expB/_vmap/_run_w1/_run_w2.log`、`_ordtest.mjs`、`server_run.log`、`_ord_head/`、`_ord_pre/`），保留被跟踪的 `_scratch_diag.mjs`。`%TEMP%/wb/` 在本机是 WorkBuddy 自己的目录。
  - T3：台账新增 **§O（O1~O6）**，469→549 行。T4：09-14 日志补「九版」节（680→726 行）+ MEMORY.md 改 6 处。
  - T5：交接单点名的 `zongmen-regression` / `webgl-headless-verify` 在本机**不存在**（用户级 skills 为空）⇒ 四条经验写进**入库的** `.workbuddy/skills/zongmen-verify-pipeline/SKILL.md`（§3 w3 ⑦ 跨机门槛 / §5 CDP 挂起替代 + 别硬编码屏幕坐标 / 新增 §12 u16 复合打包 + CRLF 定点替换）。
  - ⚠ **重跑回归 9/10**：`w3_bfs_road` 只有 ⑦ 两条**墙钟阈值**超限（第 3 慢 550/658ms>400、最慢 936/962ms>500；两次独立运行同量级、建路率 95.6% 逐字相同）⇒ **机器绝对速度门槛，非回归**；其余 9 项（含 check_vein_skin 32/32、preview_draw 32:0、w6 23:0）全绿。
  - ⚠ 证据图 `待办事项/img/fx26~28*.png` 本机不存在（git 全历史无该路径）⇒ 只能靠数字复现。⚠ `.gitignore` 的 verify 段不覆盖 `_*.md` / `_*.json` / `_*/`，临时件会污染 git status。
- ⚠ 文本文件**全是 CRLF**（memory/待办事项/skill）⇒ 用工具追加会混入裸 LF；本次一律走 Node「normalize 成 LF → 拼/替换 → 写回 CRLF」并断言裸 LF=0。
- 十版（用户两条反馈）：① 云"没跟着屏幕大小变化" ⇒ `web/js/main.js` 云尺寸改**世界锁定**：新增 `CLOUD_W0=59`（世界像素基准朵宽），上屏 `base=CLOUD_W0*cam.zoom`（旧律 `92*(0.70+0.32*min(z,3.2))` 是屏幕 px、弱耦合且 z>3.2 冻结）；默认档 zm=2.2 ⇒ 59*2.2≈130px ≈ 旧观感不变。② 灵脉要"又高又瘦" ⇒ 九版底座把**宽度**也叠加导致总框 W/H≈1.24（宽>高）；十版新增 `vein-skin.js shape.terrainBaseW=0`（默认只加高不加宽）、`renderer.js` 的 `vbaseW` 再乘 `VBASEW` ⇒ 总框 3.06×6.75~10.39 uR，W/H 0.45。`check_vein_skin` 加 §C 第15/16 条（terrainBaseW 有效 + 大档"又高又瘦"）**34/34**。
- 工具修复：`tools/prop_sheet.mjs` / `tools/bldg_sheet.mjs` 的 Chrome 路径硬编码 `C:/Users/Administrator` 改 `os.homedir()` + `CHROME_PATH` 覆盖（本机 ENOENT）；prop_sheet 灵脉分支补底座 `tb/tbw` + 新增 `--e=` 海拔参数（默认 0.80=LIFT_CORE[0]）。
- ⚠ 发现并修了一个 **pre-existing 门禁缺口**：`frontend_smoke.mjs`「解码字段契约」报 `main.js:st._anc`。`st._anc` 是聚落匾额水平锚点的**客户端 memo**（main.js 首次绘制算好缓存，非服务端下发），与 `cm.elementRGB` 同类，但 `st` 白名单一直空缺 —— **HEAD 早已含 `_anc`，与本轮改动无关**。修法 `st: new Set(['_anc'])` + 注释，复跑 **100/100**。
- 回归（本机重跑）：离线 `check_vein_skin 34/34` + `check_preview_vein_marker/edge_falloff/preview_draw/preview_terrain/settle_road/w5/w6(23:0)/frontend_smoke/sync_preview_inline` 全绿；服务端 `verify_map/w1/w2/w4` 全绿；`w3_bfs_road` ⑦ 两条墙钟阈值按本机基线红（536/806ms vs 400/500）⇒ **非回归**（本轮未动引擎）。
- 临时探针 `verify/_cloudzoom.mjs` 提升为**入库判据** `verify/check_cloud_zoom.mjs`（云跨 zoom 覆盖率/平均段长；实测 zm1→zm3 覆盖率 10.74%→6.52%、段长 16.9→30.5px）；台账新增 **§P（P1~P5）**；清理 40 件临时件（保留被跟踪的 `_scratch_diag.mjs`）。
- T6 结项：十版代码 + 九版收尾文档已由用户一次性提交推送（`db33466`，14 文件）；本轮再补 `.gitignore` 覆盖 `verify/_*.md` / `_*.json` / `_*/` + 交接单 T6 改✅ ⇒ `d4da8a9` 已 push（HEAD == origin/main，工作区干净）。
- ⚠ 踩坑固化：**bash 双引号里跑 `node -e` 且脚本内含反引号/反斜杠会被 shell 吞掉** —— 反引号触发命令替换（曾把台账 §P 的 `code` 段整段替换成命令输出、文件损坏需回滚重写），`\s`→`s`、`\r\n`→字面量也都被改写。⇒ 凡多行或含特殊字符的 JS，一律**先写临时脚本文件再 `node <file>`**（本仓用 `.tmp_*.js`，已被 `.gitignore` 的 `.tmp_*` 覆盖）。

- 用户七条新需求（灵脉七星 / 灵脉禁建 / 海上渔村 / 城镇地盘与间距）⇒ 按要求**只整理不动代码**，产出 `待办事项/十一版-灵脉七星与海上渔村.md`（R1~R7 + D1~D8 待拍板 + P1~P6 批次）。
  核查结论（均带行号锚点）：① 「七星没了」的真源是 `mapgen.js:117 veinFootKeep` 恒只占本格（六版因密排连成尖顶山簇而砍掉 7/3/1），而海拔侧 `elevAtVN` 的 d=0/1/2/3 抬升仍在，`main.js:1604` 详情文案还写着「大灵脉·七星」⇒ 文案与实现已不符；② 「小灵脉偏低」的根因候选 = 底座公式 `ve > 0.70` 是严格大于（`renderer.js:206-207`）而 `LIFT_CORE[2]=0.70` 恰在边界，再加 `(等级+海拔)/3` 的 u16 量化 ⇒ **小灵脉底座恒为 0**（大 4.2uR / 中 3.2uR / 小 0uR，总高比 ≈ 7.5 : 6.0 : 2.1）；③ 「建筑建在灵脉上」的现状 = `landuseOf` 把 vein 格判成「灵枢 / 高阶灵地」可建地皮，且 `siteScore` 的 spir 项对灵脉格 +60 主动吸引城镇中心；④ 前端 `main.js:1049-1053` 把水上建筑一律改画「栈桥」且 `plate:!onWater`（水上没有地盘）；⑤ `bldg_ink.js:1790` 的 `spriteOf` 缓存 key **不含 tint** ⇒ 城镇色必须把地盘从精灵缓存里拆出来（否则串色 / 撞 SPR_CAP=512）；⑥ 聚落只有同格去重（B2 `usedCenters`），**没有任何最小中心距参数**。
  关键设计口径：从属峰走「中心档 + 1」复用现有三档（零契约改动，iElev 除数 3 不变、max 仍 1.0），且**从属格只进 `fields().vein`、绝不进 `comm.veins[]`**（否则多出 6 张名牌 + 统计虚高 + 污染群落五行发育与 `veinNear` 最小距判定）。

- 用户拍板 D1~D8（2026-09-15 09:50）+ 明确「测试环境会自动删库，不用清库」⇒ 更新 `待办事项/十一版-灵脉七星与海上渔村.md`（§3 决策记录、§0 已拍板列、§4 批次重排 P1~P7、新增 §9 落地细节）。**代码仍未动。**
  拍板口径：D1=**新增第 4 档「从属」**（原 B 方案）⇒ `vein-skin.js levels` 3→4、`main.js` `iElev=(level+elev)/4`、shader `clamp(iElev,0,1)*4` + `min(floor(vz),3.0)`，max=(3+1)/4=1.0 仍恰不溢出；从属格写 `vein.level=3`（**不是**"中心档+1"），且**不进 `comm.veins[]`**。D2=b（`terrainBaseMin` + `LEVELS[2].hScale 1.22→1.45`，**不动** `LIFT_CORE[2]`）。D3=**中心+从属一律禁建**（`siteScore`/`growTownFootprint`/`landuseOf` 三处 `f.vein` 全排除，原「灵枢/高阶灵地」地皮作废）。D4=灵枢殿/聚灵阵/祭坛/炼丹殿/炼器殿 5 种改为**宗门附属建筑**（挂 `type='sect'` 聚落，细节待 P4）。D5=a 新增 `type='fishing'`（CORE_KIND/TOWN_BUILD_MAX/genName/tier 1）。D6=`SETTLE_MIN_DIST=7` 格（足迹半径 3 的"贴边下限"，非保守 12）。D7=**否**（名牌不染城镇色），但追加 **R8 灵脉本体要带颜色**（色源已就绪：`vein-skin` 的 `element.glow`/`variant.glow`；推荐"峰下色环 + 地名签描边"）。D8=**是** ⇒ 追加 **R9 灵脉格禁路、道路绕行**（`bfsRoad` 回退路径也不许穿；必查副作用=灵脉 7 格群会不会"围死"聚落，否则放宽为高代价；`w3_bfs_road` 只加功能断言、不动墙钟阈值）。
  ⚠ 发现 `待办事项/交接-九版收尾未完成事项.md`、`待办事项/前端表现升级-匾额山体云气.md` 在工作区**已被删除**（HEAD 仍跟踪，非本轮动作；两份旧台账内容已被十一版文档吸收）⇒ 未擅自 `git restore`，已向用户报告等其确认。
- 小地图重构规划轮（用户两条需求）⇒ 产出/更新 `待办事项/小地图灵脉化重构-独立模块与WS数据.md`（R1~R10 / D1~D10 / T1~T11 / Open Q1~Q6）。**代码未动。**
  根因核对：`main.js:587 requestMinimap` → `MC.fieldGrid` → HTTP `/api/map/fields`（132×88 后端采样），由 `minimapTimer`(2172-2184) + `minimapWindowStale`(651) 反复触发 ⇒ 撞 `ApiRateLimitMiddleware`(`Program.cs:36`)；旧小地图全部落点 = main.js 585-698 / 105-108 / 344 / 1681-1682 / 2172-2190 / 2051 / 2205 + index.html 190-196/212/261-264。
  用户拍板（本轮 AskUserQuestion）：① **地形在前端按 seed 算**（`MapGen.init` + `MapGen.fields(q,r)`），**除地形外全走 WS**——理由是"世界是动态的"（灵脉/聚落/道路/农商不可前端自算，会与服务端不一致）；② 小地图移到**屏幕左下角**、默认**跟随视野**（不可拖），点击 → **全屏窗可拖动 + 缩放**，且**可隐藏（隐藏即模块停摆）**；③ 引擎 js 用 **WebSocket 下发到前端执行**（不新增静态挂载、不拷副本、不内联）；④ `mapclient.js:315 fieldGrid` **删**。
  关键事实：引擎 `mapgen.js:1887` 结尾 `})(window)` 且导出 `global.MapGen{init,fields,BIOME_META,spiritEdgeWorld,…}`（1811-1886）⇒ 浏览器可直接跑（`灵脉预览.html` 一直这么干，但它是**内联副本**，正是要弃掉的做法）；bundle 序真源 `JsEngineHost.cs:126` = [noise, mapgen-config, mapgen, mapgen-server]，服务端已前置 `'use strict'` ⇒ 严格模式下已跑通；`StaticWebMiddleware` **只托管 web/**（不托管 Engine/js）；`#info`(山川志) 现占 left:16 bottom:18 ⇒ 与小地图左下角冲突待让位。
  ⚠ 关键约束：地形层只读 `fields().biome/.e`，**不读 onRoad**（道路语义依赖 `roadCache` 冷热，`mapgen.js:222-224`）；全屏档大范围抽样必须「每帧预算 + 缓存 + 未探测纹理占位」，否则卡死。

- **十一版实施轮（用户「完成所有待办工作」）**：P1~P7 全部落地 + 全量验证。
  - P1/P2（前端四档）：`vein-skin.js` levels 3→4（大1.90/中1.58/小1.45/从属1.05，hScale 严格递减）+ `shape.terrainBaseMin=0.30`；`renderer.js` `VEIN_SHAPE.tbm` + `VBMIN` 兜底 + `clamp(iElev,0,1)*4` + `min(floor(vz),3.0)`；`main.js` `iElev=(level+elev)/4` + `VEIN_LV_NAME=[大,中,小,从属]` + `veinTopU` 同步 tbMin；`tools/prop_sheet.mjs` 同步。`check_vein_skin` **35 项全绿**。
  - P3（引擎七星占地）：`veinFootKeep` 改 7/3/1（大 hexDist≤1 / 中 本格+(-1,1)+(0,1) / 小 仅本格）；`fields()` 从属格写 `vein.level=CFG.VEIN_SAT_LEVEL(=3)`；`mapgen-config.js` 新增 `VEIN_SAT_LEVEL:3`。新 `verify/check_vein_cluster.mjs` **7 项全绿**。
  - P4（禁建/宗门附属/R9）：`landuseOf` 灵脉格→`vein`；`siteScore` 灵脉格→`-1e18`；`growTownFootprint` 灵脉格 `continue`；「灵枢/高阶灵地」地皮作废→新增「宗门附属」(5 种建筑, 只在 sect 外环)；`bfsRoad` 增 `allowVein` 两遍（严格绕行优先, 无路才兜底穿一次）。新 `verify/check_no_build_on_vein.mjs` 5 项绿 + `w3_bfs_road` 加 §⑧ 三条全绿；实测路网灵脉格 **0/13073 = 0.00%**。
  - P5（间距7）：`settlementsFor` 拆 `rawSettlementsFor`(无抑制) + 抑制层（只与字典序更小 1 环邻格比 ⇒ 无环确定）；`CFG.SETTLE_MIN_DIST=7`。新 `verify/check_settle_spacing.mjs` 6 项绿（实测最小中心距 7、抑制剔除 140/848）。
  - P6（海上渔村）：`landuseOf` 浅海/沙岸→水岸、`nearLandAt` 近岸闸、`siteScore` 浅海需近陆+灵气、中心落水强制 `type=fishing`、`CORE_KIND.fishing`/`TOWN_BUILD_MAX.fishing`/`genName`渔村/`STYLE_NAME.fishing`。新 `verify/check_sea_village.mjs` 8 项绿（实测 30 渔村）。
  - P7（表现层）：**R6 城镇地盘色** —— `bldg_ink.js` 新增 `plateAt(ctx,{cx,cy,R,tint,a,..})` 轻量出口（直画 2D 上下文, **不进 `_spr` 缓存**）；`spriteOf` 缓存 key 补 plate 位(`|p/n`) 防串色；`main.js` 新增 `townColor(st)`（由 q,r,type 派生确定性 HSL, 色相按 type 分带 城22/镇46/村142/宗268/渔196 ±14 抖动）并在贴精灵前逐格现画地盘、精灵侧改 `plate:false`。**R8 灵脉带色** 本已存在（元素色群晕 + 七星花 + 名牌 tint）；本轮补一圈**峰下元素色六边地环**（`hexPath`+rgb, fill0.16/ring0.55）。另按 R5 §4：`onWater` 分支改为「渔村聚落按原 kind 画，只有非渔聚落的水上格才画栈桥」。bldg_ink 全 27 kind paint 冒烟 + plateAt 冒烟 OK；`sync_preview_inline` 写回 `灵脉预览.html` 并复核同步；`frontend_smoke` 全绿。
  - 验证：离线 11 脚本全绿（含 check_vein_skin35 / check_vein_cluster7 / check_no_build_on_vein5 / check_settle_spacing6 / check_sea_village8 / w6 23:0 / frontend_smoke）；`w3_bfs_road` 仅 ⑦ 两条墙钟阈值红（592/903ms vs 400/500）＝本机绝对速度门槛 **非回归**。服务端：用户 8140 实例跑的是旧 bundle（`verify_map` 曾 154 项红）⇒ 另起**临时 8141 实例**（独立 DB `db/zongmen.verify.sqlite`、jsDir 仍指源码树、**不动用户实例**）⇒ `verify_map / w1 / w2 / w4` 全部全绿 ⇒ 无回归。用毕停该实例 + 删临时 DB + 清 12 件临时件。
  - ⚠ **已知非缺陷**：跨群落中心撞格（相邻两群落各自把次级灵脉落在同一整数格 ⇒ `veinNear` 全局最近裁其一，落败者成「幻影灵脉」= 名牌叠字，**不破 7/3/1 占地契约**）。seed 42 抽样 1 处；`check_vein_cluster` 已列为**信息项**（撞格遮蔽跳过，不判失败）。
  - ⚠ **收尾待办**：用户 8140 实例仍跑旧 bundle ⇒ 前端(磁盘新)/后端(旧) **当前不一致**，需**重启 8140**；载荷已变 ⇒ 按铁律需**清 `db/zongmen.sqlite*`**（用户说过测试环境会自动删库，故未擅动）。wmic 被安全策略禁用（拿不到父进程），未擅自 kill 用户进程。
- 收尾：把本轮 4 个新离线契约脚本（`check_vein_cluster` / `check_no_build_on_vein` / `check_settle_spacing` / `check_sea_village`）+ R9 判据补进 `.workbuddy/skills/zongmen-verify-pipeline/SKILL.md`：新增 **§13**（五条契约表 + R9 严格参照替换式必须同步 + 跨群落撞格的已知非缺陷说明），并**修正 §12 第 1 条**（u16 复合通道除数 **3 → 4**，因 D1 新增第 4 档「从属」；上界 (3+1)/4=1.0 仍恰好不溢出）。复核 shader `renderer.js:203-205` 确为 `clamp(iElev,0,1)*4` / `min(floor(vz),3.0)` ⇒ 文档与实现一致。
- **R10/R5b 轮（用户两条反馈）**：① 「背景用中空的设计，而不是整个背景色都一样的填色块的，大概距离边界 0.8-0.9格子的范围的正六边形，海上的也要」② 「渔村是渔村不是一个桥梁，你要有渔村的样子」。
  - 诊断（先看真实渲染，别猜）：`node tools/bldg_town.mjs 42 --type=fishing` 直接出生产代码渲染的聚落平面 —— 改前 **古井渔村 = 码头×3 + 渔船坞×2 + 渔亭×2 + 祠堂×1**，一座民居都没有；根因在引擎 `landuseOf`：浅海/沙岸一律归 `水岸`，而 `BUILDINGS.水岸` 池只有 码头/渔船坞/渔亭 ⇒ 渔村 8 格全是水工 = 一片栈桥。另用 `.tmp_probe_fish.mjs` 对运行中的 8140 取真实 WS 帧，确认 `st.type` 确实是 `fishing`（前端 `isFish` 判定没问题，问题在**建筑配比**，不是渲染分支）。
  - **R10 中空地盘环**：`bldg_ink.js` 的 `hexPlate`（精灵内/看板/预览）与 `plateAt`（main.js 现画·城镇色）双改 —— 只画半径带 **[0.80R, 0.90R]** 的环带（环内/环外全透空），实现用「以 mid=0.85R 的六边形 + 描边宽 = rOut-rIn」（两后端只有单环 poly/line，无 even-odd 填充）；`solid:true` 保留七版实心路径做对照。0.90R<1.0R 且邻格中心距 √3R ⇒ 密排环永不相接。海上渔村走同一函数 ⇒ 一并生效。
  - **R5b「渔村的样子」**：`mapgen.js` 新增地皮 **`渔家`**（`LANDUSE_PRI.渔家=2`）与池 `[民房×3, 仓库×2, 码头, 渔船坞, 渔亭]`（条目重复即权重，逐格 hash 抽 ⇒ 确定）；`growTownFootprint` 在「内环改判村落」**之后**加 `if (type==='fishing' && lu==='水岸') lu = '渔家';`（放后面才不会被那句抢走）。改后古井渔村 = **民房×3 + 仓库×2 + 祠堂 + 渔船坞×2**。
  - **水上木台**：`bldg_ink.js` 新增 `waterDeck(S)`，`paint()` 在 `spec.onWater` 时调用（桩脚 + 板缝 + 极淡暖晕，**刻意不铺实色**，与 R10 中空审美一致）；`spriteOf` 缓存 key 补 `|W/-`，main.js 传 `onWater`（= `it.bio<=1`，即浅海/深海）；`tools/bldg_town.mjs` 同步取引擎 biome 传该位。
  - 顺带补齐 R5 遗留：`main.js` 的 `ICON_FN` / `TYPE_NAME` 补 `fishing`（远缩放原本没有图标；面板原本直接显示英文 `fishing`）。
  - 新契约：`check_sea_village.mjs` 8 → **11 项**，新增「以水上民居为主（民房/仓库 ≥40%）」「水工 ≤45%」「无一村零民居」——钉死「渔村 ≠ 一片栈桥」。（实测 240 格：民居 141 = 58.8%、水工 69 = 28.7%、核心 30；每村民居占比 min 0.25 / 中位 0.63；零民居 0 座。）
  - 回归：离线 `check_sea_village 11/11`、`check_vein_skin 35/35`、`check_vein_cluster 7/7`、`check_no_build_on_vein 5/5`、`check_settle_spacing 6/6`、`check_preview_vein_marker`、`check_preview_draw`、`check_preview_settle_road`、`check_preview_terrain`、`check_edge_falloff`、`w5`、`w6 23:0` 全绿；`sync_preview_inline` 写回 `灵脉预览.html`（mapgen 80901→81686 / bldg_ink 88665→92477）。服务端：另起**临时 8141**（独立 DB、jsDir 指源码树）⇒ `verify_map` **0 FAIL** + `frontend_smoke` 全绿（107 处属性读取）。`w3_bfs_road` 仅 ⑦ 两条墙钟阈值红（515/815ms vs 400/500）＝本机绝对速度门槛 **非回归**（R9 全过，路网灵脉格 0/13073）。
  - 实机证据：`verify/_live_fish_close.png`（8141 近景，zm4.6）可见每座建筑脚下是**中空六边形环**、水上民居 + 渔船 + 两座码头。
  - ⚠ 收尾：用户 8140（PID 3988，12:52 起）在**我改 mapgen 之前**启动 ⇒ 引擎仍是旧 bundle，需再重启一次才能看到新渔村配比（前端磁盘已是新的，刷新即生效 R10 环）。

## 十二版 R11：聚落与灵脉的间距 + 点选标记缩放（2026-09-15 午后）
- 用户两条：「点击后的红色圈圈特效太大了，改为原来的 1/3」「有些城镇距离灵脉太近了，你看一下怎么改，再来改」。
- **根因（先查真实数据再动手）**：`siteScore` 把「灵脉邻近」当**加分**（`vn.d<=1` +30 / `d<=3` +10）⇒ 主动把城镇中心吸到灵脉脚下。改前实测（seed 42/7/check 共 382 座）：**32 座中心距灵脉中心仅 1 格**（8.4%），最近建筑同样贴到 1 格。
- **R11 两层避让**：
  · `mapgen-config.js` 新增 `SETTLE_VEIN_CENTER_PEN=40`（d<=1 扣分，旧是 +30）/ `SETTLE_VEIN_CENTER_PEN2=20`（d=2）/ `SETTLE_VEIN_FOOT_PAD=2`（足迹缓冲格距）。
  · `mapgen.js siteScore`：`spir = vpen + spiritAt*20`（vpen 按 `veinNear().d` 逐级扣分）；顺手删掉死分支 `vn.d === 0 ? 60`（灵脉格早已 `f.vein` → -1e18 提前 return）。
  · `mapgen.js growTownFootprint`：在 `cell.d === 0`（核心格）**之后**加缓冲 `veinNear(cell).d <= FOOT_PAD ⇒ continue`（核心格豁免，否则灵脉密布处会出「有卫星无主殿」的残缺聚落）。
  · 实测：中心 d<=1 **32 → 0**、最近中心距 1→2、最近建筑距 1→2、建筑 d<=2 **353(7.4%) → 16(0.3%)**、聚落 382→381、建筑 4790→4717（-1.5%）。
  · ⚠ **最易踩的坑：分级扣分必须单调 `PEN > PEN2`** —— 实测 PEN2 加到 ≥PEN（40/60）反而**冒出 2/10 座 d<=1 的中心**（d=2 罚得比 d=1 更狠 ⇒ d=1 成了「两害相权取其轻」的最优解）。另 `FOOT_PAD=1` 几乎无效（只剔已被 `f.vein` 剔过的邻格，建筑 d<=2 仍有 139 座），**=2 才是分水岭**。
  · 新契约 `verify/check_vein_settle_gap.mjs`（8 项）钉死：参数单调 / 无中心 d<=1 / 无建筑 d<=1 / 建筑 d<=2 占比 <2% / 灵脉域聚落占比 >=10%（设定「灵脉附近更繁华」未被避让赶尽）/ 每 seed 聚落数 >=100。
- **点选朱砂标记 ×1/3**：`main.js drawOverlay` 引入 `SEL_K = 1/3`，半径 / 圈距 / 斜标偏移与长度**全部乘 SEL_K**，线宽乘后保底 0.5px（再细会糊成灰线）。实机对照图已证（同一 URL 截两张：临时 SEL_K=1 与原 1/3，改回后 grep 复核）。
- 新增 DEBUG 调试参数 `?sel=q,r`（仅 `debug=1` / `capture=1` 生效，`main.js` regenerate 之后赋值）：headless 无法模拟鼠标点击，靠它验收点选态。
- 验证：离线 **14 脚本全绿**（新 check_vein_settle_gap 8/8；`check_cloud_zoom` 需实机截图参数，裸跑 rc=2 属正常）；`w3_bfs_road` 只剩 ⑦ 两条墙钟红（574/1007ms vs 400/500 = 机器绝对速度门槛，§⑧ R9 全过，路网灵脉格 **0/13406**）；服务端临时 8141 实例 `verify_map` **0 FAIL**、`w1/w2/w4` 全绿。
- skill `zongmen-verify-pipeline` 已加 §15（R11 机制 + 单调陷阱 + FOOT_PAD 分水岭 + `?sel` 与对照图姿势）。
- ⚠ **收尾待办**：用户 8140 需**再次重启**（12:52 那次重启发生在 R11 之前，后端仍是旧 bundle）+ 按铁律清 `db/zongmen.sqlite*`（载荷已变；用户说过测试环境会自动删库）。
- **点选朱砂标记再调 (用户: 圈圈太小, 线太细/0.1格宽, 缩太小要跟着变粗)**：`main.js drawOverlay` 的 `SEL_K` 由 `1/3` 改 `1/3*1.2 = 0.4`；线宽不再写死像素，改为 `selLw = max(geo.hexW * cam.zoom * 0.1, SEL_LW_MIN=1.5)` —— 基准是**该缩放下一格屏显宽度的 0.1**（放大时圈/线同比变粗，比例恒定），保底 1.5px 保证缩到 `minZoom=0.7` 时线**相对圈**变粗、不消失。副圈 `selLw*0.6`(保底 1px)、四角斜标 `selLw*1.2` 保持层级。
- 实机验收（**前端改动无需重启服务端**，Chrome 每次加载重读磁盘 js）：`?debug=1&sel=31,58` 在 `zm=2.2` 与 `zm=0.9` 两档各截一张 → 裁切放大目检；两档圈均清晰、低缩放档线相对更粗（保底生效）。`frontend_smoke` 对 8140（LAN `192.168.63.62:8140`）全绿。
- ⚠ 记一条环境事实：**Zongmen 服务绑的是 LAN IP `192.168.63.62:8140`，不是 127.0.0.1** ⇒ 探活/截图 URL 必须用 LAN IP，否则 `ECONNREFUSED`（本轮误判过「8140 没监听」）。另 `netstat -ano` 不带 `-a` 时分不清 LISTENING，判断端口状态要用 `netstat -ano | Select-String` 看全量行 + 直接 HTTP 探活双保险。

## 十三版 R12：小地图灵脉化重构（独立模块 + 全 WS 数据）— 2026-09-15 傍晚
- 用户原话：「右下角小地图一直请求导致到了请求速率」+「融合 `灵脉预览.html` 改成这个，可以最大化，独立一个文件方便热拔插，使用 websocket 的数据」；澄清追加：「除了地形都走 ws 因为世界是动态的」「小地图在左下角，默认跟随视野，点击变成全屏可拖动放大缩小，可以隐藏整个小地图、相关组件不工作」「使用 websocket 传入 js 文件前端执行」「D5 删掉」。
- **根因（先查真实链路）**：`main.js:587 requestMinimap` → `MC.fieldGrid` → HTTP `/api/map/fields`（132×88 后端采样），由 `minimapTimer` / `minimapWindowStale` 反复触发 ⇒ 撞 `ApiRateLimitMiddleware`。
- **落地（T2~T11 全绿，细节见 `待办事项/小地图灵脉化重构-独立模块与WS数据.md` §8）**：
  · 后端：`WsFrame.Script=4` + `ScriptRequest`/`ScriptPack`；`MapWsHandler.EngineScriptOrder=[noise,mapgen-config,mapgen]` 按序拼接 + 文件名白名单防目录穿越 + gzip 进 `Source`；`Program.cs` 传 `ResolveEngineJsDir`。
  · 前端：`pb.js` FRAME.SCRIPT + encode/decode；`mapclient.requestScript`（15s 超时）+ onFrame `type=4`；**删** `fieldGrid`/`base64ToBytes`（D5）。
  · 新模块 `web/js/minimap-vein.js`（678 行 IIFE）：L1 前端自算地形 / L2 全 WS / L3 视野三态；`main.js` 只留 `mmSnapshot`/`mmJump`/`initMinimap` **单点注入**；`index.html` 左下挂载 + `#mmFull`(同级兄弟) + `#mmRestore` + `#mmTip`(z70) + `#info` 让位 `bottom:230px`。
- **⚠ 踩坑三条**：
  ① **ScriptPack 帧本身不 gzip，只有 `Source` 字段 gzip**（先 `gunzip(payload)` 再解码 ⇒ `Z_DATA_ERROR` + `.then is not a function`）。
  ② `main.js` 是 `(function () {` **没有 `g` 形参** ⇒ 必须写 `window.MiniMapVein`；写 `g.MiniMapVein` 直接 `g is not defined` 整页 fatal（`--dump-dom` 里可见 fatal 文本、canvas 不挂载）。
  ③ 引擎 js 需要 `window` ⇒ Node 侧复算必须先 `global.window = globalThis` 再 eval 三个引擎文件（`mapgen-config.js` 不能漏，否则静默跑旧参数）。
- **验证**：端到端 `ScriptPack` 95KB / gzip 26ms / eval 后抽样 **6/6 与服务端 `/api/map/tile` 逐格一致**；`MapGen.fields` 单格 **0.006~0.031ms** ⇒ 前端自算可行（据此做每帧时间额度抽样防卡）；实机三态截图 `live_r11_mm_{default,full,hidden}.png` + 裁剪；**回归 19/20 绿** —— 唯一红 `w3_bfs_road` 是 ⑦ 两条**墙钟**门槛（1160ms vs 500ms 灾难红线，①②③④⑤⑥⑦建路率⑧ 功能断言全过），非回归。
- `frontend_smoke.mjs` 重写 `checkMinimap`：删 `fetchFields`/旧 `MC.fieldGrid` 依赖，改为「源码守卫（零轮询/旧符号清除/单点注入）+ 用 `Engine/js` 复算地形上色（0 越界、≥3 种地貌色，实测 7 种）」⇒ **36/36 全绿**。
- **⚠ 环境事实更正**：8140 **`127.0.0.1` 与 LAN IP `192.168.63.62` 都通**（本轮 `frontend_smoke`/`verify_map`/`w1`/`w2`/`w4` 默认 BASE 就是 127.0.0.1 且全绿）。旧记录「只绑 LAN IP、127.0.0.1 不通」是把「服务当时没在跑」误判成绑定问题。
- **命名**：本次小地图重构记为 **R12**（项目的 **R11 已被「聚落↔灵脉间距」占用**，此前口头都叫 R11 ⇒ 文档里已区分）。
- skill `zongmen-verify-pipeline` 已更新：§3「小地图契约」条目换口径（旧 fields 轮询已退役）、§5 去掉已删除的 `minimapTimer` 引用、**新增 §16**（R12 三层口径 / WS 下发引擎脚本 / 防卡 / 验收姿势 / 环境更正）。⚠ 顺手发现：备忘录里长期引用的 skill 名 `zongmen-regression` **并不存在**，真源是 `zongmen-verify-pipeline`，已在备忘与文档中改正。

## 十四版 R12 补丁：大地图采样大面积「未探测」— 抽样格世界对齐 — 2026-09-15 深夜
- 用户原话：「这个大地图采样有问题」+ 一张全屏小地图截图（中央一小块实心地形，四周大面积暗色斜纹，越往下越空）。
- **取证（先量化再动手，全部实测非推断）**：自写 PNG 解码器（Node `zlib.inflateSync`，零依赖；本机 PIL 不可用）量化未探测像素 = **46.5%**；暗色确为模块自身的「未探测」斜纹（色 `(40,36,32)/(58,52,44)`、基色:斜纹恒 3:1 = 代码 `(px+py)&3`，与任何地貌色不撞，深海是 `#6d9aab`）。引擎侧 `MapGen.fields` 在 ±220 格内零 null / 零异常、单格 0.019ms ⇒ **不是地形算错**。
- **根因**：初版抽样点是「画布像素格反投影进世界」⇒ **视图锁定**，视图一动采样点几乎全落新格。实测复用率：平移 5 单位 **55.1%**、平移 1 格 36.4%、缩放 ×1.14 30.7%、面板→全屏 **11.5%**；默认全屏档视图内真实 58363 格 vs 采样 21850 = **漏格 63%**（wpp=12 达 96.8%）。
- **修法 F1~F7（`web/js/minimap-vein.js`）**：① 抽样格改**世界对齐** `q%m==0 && r%m==0`，缓存键只留 `q,r`（与视图无关 ⇒ 平移/缩放/换层级复用同一张表）；② `m` 对齐显示分辨率（`m≈ceil_pow2(step像素×wpp/hexW)`）；③ **粗层引导** 重建列表按 `[m*8,m*4,m*2,m]` 逐层铺 + `biomeAt` 层级回退链（首帧即有粗略地脉、不成片露底）；④ `pendingIdx` 游标消费取代 `Array.shift()`（免 O(n) 搬移）；⑤ 容量改 `PENDING_CAP/TERRAIN_CAP=400000` 且**不再静默丢弃**，超额按插入序删最旧 1/4（整表清空会重新露底）；⑥ 重建时机 = 层级/视野签名变化且**列表已排空**（拖动中不重建）；⑦ `probe()` 增 `queueLen/pendingLen/sampleM/miss/draws/enqDrop`。
- **验收**：`probe().miss` 全过程恒 **0**；平移仅新增 1784 格、缩放换层新增 7885 格（1s 内补齐）；`sampleM=2`（与显示块对齐）；三态实机截图 `verify/live_r12_mm_{panel,full,zoom12}.png` 未探测占比 **0%**（46.5% → 0%）。
- **契约**：`frontend_smoke.mjs::checkMinimap` 的复算段改为**按模块真实规则复刻**世界对齐抽样，新增 4 条断言（① 抽样格落在 m 整数倍、错位 0；② 视图内每格都有抽样格代表，3384 探针点空洞 0；③ 平移 6 世界单位复用率 ≥90%，实测 **99.4%**；④ 缩放 5% 复用率 ≥90%，实测 **100.0%**）⇒ **36 → 41/41**；全量回归 **19/20**（唯一红 `w3_bfs_road` ⑦ 两条墙钟 490.9/1039.6ms，功能断言全过，非回归）。
- **取数通道（CDP 对本页永久挂起）**：新增 `?mmprobe=1`（页面按时间点采 `__mm.probe()`+`__data()` 并 POST `/api/debug/snap`，服务端只落字节不校验 MIME ⇒ 读 `verify/capture.png` 即得时间序列）+ `?mmdrive=1`（在全屏画布派发真 `WheelEvent`/鼠标事件，走模块自身处理器）+ `?mmwpp=N`（全屏档初始缩放）。
- 文档：`待办事项/小地图灵脉化重构-独立模块与WS数据.md` 追加 **§9**（现象/取证表/根因/改法 F1~F7/验收表/遗留），§3.2 加注「初版按像素取格 = 视图锁定，已被 §9 修正」。
- skill `zongmen-verify-pipeline` 新增 **§17**（小地图地形层「世界对齐」必守口径：病征/根因/正确口径/取数验收姿势/教训），并修正 **§16.4**（标注抽样格必须世界对齐，项数 12→16）。
- ⚠ 本轮踩坑：用 bash 内联写含反引号的 md/js 会被 shell 吞成命令替换并**损坏内容**（`/api/map/meta`、`q%m==0` 等被误执行）⇒ 一律先写 `.tmp_xx.md` 文件再拼接；补丁脚本的「兜底分支」可能把整段拼坏 ⇒ 改文档/技能后**必须复核关键词与行结构**。收尾清理 13 个 `.tmp_*` + `SKILL.md.bak`（Node `unlinkSync` + 4 条断言 + 先按 `git ls-files` 过滤，未命中被跟踪文件）。


- **R13 面板档倍率实机验收全绿（此前被限流打断未汇报）**：A 组面板驱动（滚轮 6→9.44→4.78、拖动转自由、归心恢复跟随、miss 全程 0、localStorage saved=YES）；B/C 组缩放联动 panelWpp*camZoom 恒等 13.2（覆盖 zm 0.7~6 不越界）；D 组跨刷新 wpp=9.4411 还原成功。frontend_smoke 复跑 48/48（R13 契约 8 项全过）。
- 应用户要求产出交接单：`待办事项/交接-2026-09-15-小地图R12R13与未完成事项.md`（U1 提交 18 文件 / U2 重启+清库待确认 / U3 R13 归档三件套 / U4 椒盐感待拍板 / U5 临时件清理 / U6 长期项；B1 w3 墙钟非回归 P2、B2 椒盐感 P2、B3 并行 Edit P2、B4 gitignore P3）。

# 2026-09-15

- 深夜收尾轮：九版三项（云集群九版-2 / 点选朱砂标记 / 灵脉山地底座）全部完成并通过验证——探针 6/6+7/7+12 断言、check_vein_skin 32/32、离线回归 10/10 绿；证据图 fx26/27/28 已归档。
- 应用户要求把**未完成收尾事项**整理成交接单：`待办事项/交接-九版收尾未完成事项.md`（T1 清临时件 / T2 停 8177·8178 / T3 台账 §O / T4 记忆更新 / T5 skill 增补 / T6 commit+8140 重启需用户确认）。
- 用户反馈：**8177/8178 已手动停掉**（T2 完成）；且接手 agent 在**另一台机器**上干活 ⇒ 交接单已补「跨机器须知」：改动未 commit 只在 A 机工作区，B 机需先同步代码、重跑验证；%TEMP% 临时件清单与 offline9.log 仅 A 机存在。
- **接手轮（机器 B = D://codes//ZongmenSimulator / i7-6700HQ / 15.8G / Win10 1903）**：完成交接单 T1/T3/T4/T5，T6 待用户确认。
  核实过：九版三项改动**已在 HEAD**（A 机已 commit+push 7f8486e，本机 pull 后 git diff 干净）；本机无 8140/8177/8178 监听，未起任何实例。
  - T1：清单 18 件在本机全不存在（符合「只在 A 机」）；另清上轮残留 9 件（`_expA/_expB/_vmap/_run_w1/_run_w2.log`、`_ordtest.mjs`、`server_run.log`、`_ord_head/`、`_ord_pre/`），保留被跟踪的 `_scratch_diag.mjs`。`%TEMP%/wb/` 在本机是 WorkBuddy 自己的目录。
  - T3：台账新增 **§O（O1~O6）**，469→549 行。T4：09-14 日志补「九版」节（680→726 行）+ MEMORY.md 改 6 处。
  - T5：交接单点名的 `zongmen-regression` / `webgl-headless-verify` 在本机**不存在**（用户级 skills 为空）⇒ 四条经验写进**入库的** `.workbuddy/skills/zongmen-verify-pipeline/SKILL.md`（§3 w3 ⑦ 跨机门槛 / §5 CDP 挂起替代 + 别硬编码屏幕坐标 / 新增 §12 u16 复合打包 + CRLF 定点替换）。
  - ⚠ **重跑回归 9/10**：`w3_bfs_road` 只有 ⑦ 两条**墙钟阈值**超限（第 3 慢 550/658ms>400、最慢 936/962ms>500；两次独立运行同量级、建路率 95.6% 逐字相同）⇒ **机器绝对速度门槛，非回归**；其余 9 项（含 check_vein_skin 32/32、preview_draw 32:0、w6 23:0）全绿。
  - ⚠ 证据图 `待办事项/img/fx26~28*.png` 本机不存在（git 全历史无该路径）⇒ 只能靠数字复现。⚠ `.gitignore` 的 verify 段不覆盖 `_*.md` / `_*.json` / `_*/`，临时件会污染 git status。
- ⚠ 文本文件**全是 CRLF**（memory/待办事项/skill）⇒ 用工具追加会混入裸 LF；本次一律走 Node「normalize 成 LF → 拼/替换 → 写回 CRLF」并断言裸 LF=0。
- 十版（用户两条反馈）：① 云"没跟着屏幕大小变化" ⇒ `web/js/main.js` 云尺寸改**世界锁定**：新增 `CLOUD_W0=59`（世界像素基准朵宽），上屏 `base=CLOUD_W0*cam.zoom`（旧律 `92*(0.70+0.32*min(z,3.2))` 是屏幕 px、弱耦合且 z>3.2 冻结）；默认档 zm=2.2 ⇒ 59*2.2≈130px ≈ 旧观感不变。② 灵脉要"又高又瘦" ⇒ 九版底座把**宽度**也叠加导致总框 W/H≈1.24（宽>高）；十版新增 `vein-skin.js shape.terrainBaseW=0`（默认只加高不加宽）、`renderer.js` 的 `vbaseW` 再乘 `VBASEW` ⇒ 总框 3.06×6.75~10.39 uR，W/H 0.45。`check_vein_skin` 加 §C 第15/16 条（terrainBaseW 有效 + 大档"又高又瘦"）**34/34**。
- 工具修复：`tools/prop_sheet.mjs` / `tools/bldg_sheet.mjs` 的 Chrome 路径硬编码 `C:/Users/Administrator` 改 `os.homedir()` + `CHROME_PATH` 覆盖（本机 ENOENT）；prop_sheet 灵脉分支补底座 `tb/tbw` + 新增 `--e=` 海拔参数（默认 0.80=LIFT_CORE[0]）。
- ⚠ 发现并修了一个 **pre-existing 门禁缺口**：`frontend_smoke.mjs`「解码字段契约」报 `main.js:st._anc`。`st._anc` 是聚落匾额水平锚点的**客户端 memo**（main.js 首次绘制算好缓存，非服务端下发），与 `cm.elementRGB` 同类，但 `st` 白名单一直空缺 —— **HEAD 早已含 `_anc`，与本轮改动无关**。修法 `st: new Set(['_anc'])` + 注释，复跑 **100/100**。
- 回归（本机重跑）：离线 `check_vein_skin 34/34` + `check_preview_vein_marker/edge_falloff/preview_draw/preview_terrain/settle_road/w5/w6(23:0)/frontend_smoke/sync_preview_inline` 全绿；服务端 `verify_map/w1/w2/w4` 全绿；`w3_bfs_road` ⑦ 两条墙钟阈值按本机基线红（536/806ms vs 400/500）⇒ **非回归**（本轮未动引擎）。
- 临时探针 `verify/_cloudzoom.mjs` 提升为**入库判据** `verify/check_cloud_zoom.mjs`（云跨 zoom 覆盖率/平均段长；实测 zm1→zm3 覆盖率 10.74%→6.52%、段长 16.9→30.5px）；台账新增 **§P（P1~P5）**；清理 40 件临时件（保留被跟踪的 `_scratch_diag.mjs`）。

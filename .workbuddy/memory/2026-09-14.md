# 2026-09-14

## todo2 A 节收尾 + B1 实证收口（承接 09-13 的 429 限流中断）

背景：09-13 末段被限流打断；本次恢复后先复核工作区真相 —— **git 已干净，WIP 批次已提交为 `8dec793`**
（spreadPick / ROADFAILTRIALS_CAP 定义+使用 / roadSet 引用计数 / trialBump / resetRoads 全部在库）。
所以「P0 漏定义 `ROADFAILTRIALS_CAP`」在上一会话已补齐并入库，无需重修。

### A1 spreadPick 视觉确认（完成）
- 16 seed / 551 聚落对拍（旧引擎 `ff6d476` vs 新）：地皮构成 **0 差异**、风格 **0 差异**、
  方位角质心偏斜 **0.557→0.349**；`resources` 数量变 63% 但**种类集合仅 1.6%**
  —— 合预期：kind 由格坐标 hash 决定（mapgen.js:815），换格必换 kind。
- 取景「清风镇 q=-160 r=-21」并排对照：before 全挤候选盘左侧 → after 绕中心铺开。
- ⚠ 更正 `mapgen.js` 里与实测不符的注释（原「产出聚合与旧版逐项相同」）并已 sync 预览内联。

### A2 回归
- 离线全绿：vein_marker / terrain / edge_falloff / preview_draw(32:0) / settle_road / w5 / w6(23:0) / sync --check。
- `w3_bfs_road` 12 项只有 ⑦「最慢单 region<250ms」FAIL（993.6ms）= **本机 CPU 慢的假象**：
  同款测试 HEAD 1352ms / 修复版 1139ms 同量级；建成路 739 / 建路率 97.9% 与基线逐一一致。
- 本机**无 .NET SDK** ⇒ verify_map / w1 / w2 / w4 / frontend_smoke 仍跑不了（提交前必须在有 SDK 的机器补跑）。

### A3 结论：spreadPick 无需推广到生产前端
`web/js/main.js drawBuildings`（710 行）直接消费服务端 `settleCells[].buildings[].q/r` 按格心贴图 ⇒ 引擎侧改动自动受益，客户端零改动。

### A4 清理
- 删 5 个被跟踪临时脚本（`_cdp_shot.mjs` + `_dbg_{inline_diff,town_ab,town_geom,town_page}.mjs`）+ 44 个未跟踪临时文件。
- `_runlog.mjs`→**`runlog.mjs`**、`_imgdiff.mjs`→**`imgdiff.mjs`** 提升为常驻（见下「工具」）。
- ⚠ **剩余 46 个 gitignored 临时文件 + `_eng_head/` 目录未删**：环境「批量删除守卫」按 **单轮 50 个** 上限
  拦截（`SAFE_DELETE_BULK_CONFIRM_REQUIRED`, scope=turn），需分轮补删。`verify/_*` 已在 .gitignore，不影响 git。
- 提交 `364f484`。

## B1「热跑回退 +23%」—— 用修好的 bench 实证：已解决
先修 `verify/bench_road_drain.mjs` 注入锚点（A4 把入库点从 `cacheSet(roadCache,…)` 改成 `roadSet(…)` 导致 2 处锚点失配；
并补计 DI 延迟重试轮的建成）。实测（seed-check ±8 区域格 = 141 格）：

| 指标 | PRE | 修复前 | **现在** |
|---|---|---|---|
| 热跑墙钟 | 5052ms | 6207ms | **3ms** |
| 热跑主循环 A* | — | — | **0** |
| 热跑 drain A* | — | — | **2** |
| 单次 roadsNear(9999) 热态 | — | — | **0.03ms** |
| 冷建 | — | — | 26432ms / 303 条 = 87.2ms/条 |

⇒ `roadFailVer` 负缓存（失败边不再由扫描重算）+ 主循环 `starved` 时让位 drain + `drainMark` 按 roadVer 节流，三者合起来把热跑干到近零。**B1 关。**

## B2 仍未关（唯一剩下的道路病灶，在预览页）
`bench` 泵送段：**budget=1 泵完只建 89/303（29%）**、budget=3 → 198、budget=8 → 259。
根因 = `灵脉预览.html pumpRoads` 游标**单向前进**，预算饥饿格被永久跳过（引擎侧 `starved` 标记没被消费）。
已选方案（待拍板，未落）：零产出格回队尾 + 多圈重复直到「一整圈零产出」收敛；UI 进度改按已建条数。
⚠ 收敛判据要能区分「格已建完」(每次零产出但正常) 与「格被饿死」—— 用「整圈零产出」即可覆盖两者。

## C 节现状
- C3 已由 WIP 完成（`main.js drawBuildings` + `index.html` 挂 `bldg_ink.js`）。
- C1（预览页接 `bldg_ink` 实时绘制）大改：需把 `web/js/bldg_ink.js` 内联为预览页第 5 段脚本 + 扩 `sync_preview_inline.mjs`。
- C2（`main.js:715` `hexR*z<5` 一刀切隐藏稀有 7 种）改法：`tiny` 模式只画稀有档 + 抬最小尺寸，且**不置 `bldgShown`**（否则聚落图标会被误让位）。两者均待拍板。

## 工具/环境（本轮新增认知）
- **后台任务 ~2 分钟上限**：长脚本（w3 ≈ 100s、全套 > 2min）必须前台跑并放宽 `timeout`。
- **Node 管道 + `process.exit()` 截断输出** ⇒ 一律用 `verify/runlog.mjs <out.txt> <script>` 包装（同步落盘 + 捕 uncaught/signal）。
- **批量删除守卫**：node `fs.unlinkSync` 单轮 >50 个 ⇒ `SAFE_DELETE_BULK_CONFIRM_REQUIRED`，大清理必须分轮。
- PowerShell stdout 不回显（本环境）⇒ 落盘再 Read（老规矩）。
- `core.autocrlf=true` ⇒ 多行注入前 `replace(/\r\n/g,'\n')`（bench 已内置）。

---

## 收尾：B2 / C1 / C2 全部落地并提交 `bde322c`（上述「仍未关 / 待拍板」以本节为准）

用户拍板全选三项，均已实现 + 离线回归通过 + 入库：

### B2 预览页泵送饥饿 —— 已修
`灵脉预览.html pumpRoads` 改**多圈收敛**：某圈只要有新产出就把游标回卷再跑一圈，直到「一整圈零产出」为止
（兼顾「格已建完」与「格被饿死」两种零产出）。`resetRoadState` 复位 `roadPassBuilt/roadLap`；
`roadStat` 显示 `(第 N 圈 …)`。实测 `check_preview_settle_road` 106 → **305 条**（多圈收敛 7 圈，≈100%）。

### C1 预览页接 bldg_ink 实时精灵 —— 已修（关键：不新增第 5 段 script）
把 `web/js/bldg_ink.js` 真源内联进**渲染脚本块内部**，用 `/*… 内联副本（来源 …` / `… END` **START/END 标记对**界定，
`sync_preview_inline.mjs` 的 MAP 条目用 `end` 字段定位块尾；这样**不破坏既有「4 段 `<script>` 计数」回归**。
重写 `drawTownPlan`：`tilePx >= SPRITE_MIN_PX(5)` 走 `window.BldgInk.spriteOf`（`P_R_BUCKETS`/`pBucket`/
`previewBiome`→`MapGen.fields().biome`/`previewSolver`→`BldgInk.faceSolver`）；过小时回退旧六边+方块占位。
`check_preview_draw` 断言由 `rectZoom>200` 改 `drawImage>0`：**32:0**、zoom 下 drawImage **403**；截图肉眼确认真实精灵。

### C2 稀有 7 种建筑抬最小尺寸 —— 已修
`web/js/main.js drawBuildings` 新增 `RARE_KINDS`（炼炉/官衙/焦炭窑/宗祠/祭坛/聚灵阵/灵枢殿）；
`tiny`(`hexR*z<5px`) 时 `bkt=0, scale=1`（最小桶）、非稀有档 `continue`、`bldgShown = !tiny`（避免误让位聚落图标）。
`node --check` 通过；`main.js:900 solid=bldgShown && type!=='poi'` 语义不变。

### A4 清理补轮 —— 已完成
`verify/` 下剩余 **50 项 gitignored 临时件 + `_eng_head/` 全部删除**（`git status --ignored verify/` 剩余 0）；
D:\codes\杂项 下 20 个 `_*` 辅助脚本/重定向输出亦删光。被跟踪的 `verify/_scratch_diag.mjs` 保留。
（用 Node 脚本按 `git status --ignored` 名单删，天然跳过被跟踪文件 —— 比盲删 `rm verify/_*` 安全。）

### 最终状态
工作区干净；`sync_preview_inline --check` 全同步（mapgen-config 4164 / mapgen 79174 / bldg_ink 84831 字节）。
todo2 A/B/C 三节全部 `[x]`。提交 `bde322c`。**仍欠：有 .NET SDK 的机器上补跑 verify_map/w1/w2/w4/frontend_smoke。**

---

# 2026-09-14（下半程）· todo1 收尾：工作区抢修 + 全量回归 + 强策略拍板

对应 `待办事项/todo1.md`（本轮已把结论写回该文件）。

## 0. 开工即发现两处「旧认知是错的」

1. **本机有 .NET SDK**：`C:\Program Files\dotnet\dotnet.exe` = **8.0.100**。
   早先「dotnet 不在 PATH、标准路径也不存在 ⇒ 服务端项跑不了」是**错**的 —— 服务端回归一直可跑。
2. **`verify/` 被误删**：`git status` 显示 **26 个被跟踪脚本全部 ` D`**（4547 行），
   回归套件（w1~w6 / verify_map / check_preview_* / frontend_smoke / scan_poison / 工具链）从工作区消失。
   - 归因：与 todo2-A4「清理 gitignored 临时件」同批（全部 mtime = `2026-09-14T03:10:03Z`）。
     **`git status --ignored verify/` 之所以「剩余 0」，正是因为连被跟踪文件一起删了** —— 判据自证。
   - 处置：`git restore --source=HEAD --worktree -- verify/` → 26 文件回位；`git hash-object --path`
     逐字节复核等于 HEAD；`git add --` 清掉 7 个 stat-cache 假脏（diff/numstat 为空）；最终 `git status` 全净、verify 32 文件。
   - ⚠ 教训：清理必须**先过滤被跟踪名单**再用 Node `fs.unlinkSync` 绝对路径删；禁 shell 通配 / `git rm`。

## 1. 环境坑（本机）

- **bash 的 PATH 是坏的**：`ls` / `cp` / `tail` / `dirname` 一律 `command not found`（shim 报 `dirname: command not found`）。
  可靠姿势 = 全程用 Node：`"C:/Users/Administrator/.workbuddy/binaries/node/versions/22.22.2-3/node.exe" -e "…"`，
  内部用 `child_process.execFileSync` 调 git / dotnet 的**绝对路径**。PowerShell 依旧不回显 stdout。
- 隔离服务端实例（不碰用户的 8140）：
  `dotnet build Server/Zongmen/ZongMen.csproj -o verify/_vmsrv -p:UseAppHost=false`
  → `Zongmen__Port=8150 Zongmen__DbPath=<temp> [Zongmen__EngineJsDir=<临时引擎副本>] dotnet verify/_vmsrv/ZongMen.dll`
  （`Options.FindRoot` 自 `_vmsrv` 上溯到仓库根 ⇒ 自动用仓库 `web/` + `Engine/js`）。跑完删 `_vmsrv`。
- **引擎副本 A/B 技巧**（本次最有用的手法）：整目录复制 `Engine/js` → 只改待测那一行 →
  把 `bench_road_drain.mjs` 的 `JSDIR` 改成读 `process.env.ENJDIR` → 同进程 A/B，**全程不碰仓库**。
  同理把 `verify_map.mjs` 的 `ROOT` / 引擎路径改成读 `ROOT_OVERRIDE` / `ENJDIR` 即可换引擎跑对拍。

## 2. 回归结果（干净树 `bbfab79`）

- **离线 9/9 全绿**：sync_preview_inline --check、check_preview_vein_marker、check_preview_terrain、
  check_edge_falloff、**check_preview_draw 32:0**、**check_preview_settle_road 305 条**、
  w5_sprite_range、**w6_bldg_face 23:0**、**w3_bfs_road 12 项全过**（⑦ 最慢单 region **202.2ms < 250ms** 转 PASS；
  09-13 曾 993ms FAIL，属本机负载差异）。
- **服务端 4/5 绿**：frontend_smoke / w1_client_revs / w2_concurrency / w4_revs_at_scale 全 PASS；
  **scan_poison 42 [-20,20]² = 1681 块 bad=0**（与基线一致）。
- ❌ **`verify_map` 22 项失败**（全在道路「点列」与 `region(-1,0) 道路 key 一致`）。

## 3. `verify_map` 22 项的定性：既有「顺序依赖」，**不是回归**

三引擎对照（同一份 verify_map，只换引擎）：

| 引擎 | 策略 | 失败数 |
|---|---|---|
| `8481638`（09-13 18:37，路网改动**之前**） | —— | 22 |
| `bbfab79` 当前 | 强策略 `has(rkey)` | 22（明细完全一致） |
| `bbfab79` 当前 | 温和策略 `get===roadVer` | 22（明细完全一致） |

⇒ 与强策略无关、与 8dec793 的道路改动无关，是**引擎固有序行为**：
`bfsRoad(..., roadTileIdx)` 复用让**折线取决于「首次建成时路网已有多少路」**（mapgen.js 原话「复用是路径依赖的」），
而参考端按 `di/dj` 三重循环逐区域 `regionJson`、服务端按 WS 块请求 + 缓存 ⇒ **扫描顺序不同 ⇒ 同 key 折线不同**。
处理建议（待拍板）：① 改测试为拓扑等价判据（比 key 集合/端点/瓦片，不比折线）或让参考端按服务端的块序驱动；
② 改引擎让折线只依赖该边 + 静态地形（代价：丢路复用，路网变长）。
与 review E2（预览页传 cq/cr、服务端不传 ⇒ 两端路网分叉）**同源**，建议一并按「顺序无关化」处理。

## 4. todo1 §1.3 拍板：**保留强策略**（干净树实测）

同进程引擎副本 A/B，两变体只差 `if (roadFailVer.has(rkey)) continue;` 这一行：

| 指标 | 强策略 | 温和策略 |
|---|---|---|
| seed-check 冷建 | 6100ms / **303 条** | 7091ms / **310 条** |
| seed-check 热跑 | **1ms**（A* 0） | **5923ms**（42ms/格，A* 147） |
| seed-check 热跑新建 | **0 条**（303→303） | **+7 条**（310→317，非幂等） |
| seed-ab 冷建 | 6542ms / **330 条** | 7199ms / **341 条** |
| seed-ab 热跑 | **1ms**（A* 0） | **6193ms**（A* 138） |

⇒ 代价 = 冷建 **-2.3% ~ -3.2%**；收益 = 热跑 **~6000×** + **输出幂等**。
**「热跑 +23%」记为已解决**；原「303~316」区间之谜 = 温和策略在重扫次数不同下的漂移（310→317），与并发会话无关。
todo1 §2（预览页 pumpRoads）已由 todo2-B2（`bde322c` 多圈收敛，106→305 条）关闭。

## 5. 收尾清理

三个隔离实例（8150 强 / 8151 温和 / 8152 旧引擎）跑完即关，端口已释放；用户 8140 实例全程未触碰；
`verify/_vmsrv`（46 文件）+ `verify/capture.png` 已删；最终 `git status` 与 `status --ignored verify/` **双净**。
全部临时脚本/引擎副本/日志留在 `%TEMP%\wb_todo1`（在仓库外，不入库）。

---

# 2026-09-14（再下半程）· B 版落地：道路「顺序无关化」（E2 / A8 根治）

用户拍板：review §8.8 的 A8+E2 走**方案 B**（根治，而非把测试判据降级）；**本次不清库**（测试版本）。

## 1. 病灶复现（先证后修）
临时脚本 `%TEMP%/wb_b_run.mjs`：同一引擎副本、4 种扫描顺序各建一遍收敛路网，比「逐路 rkey|折线(量化)|瓦片集」指纹。
- **HEAD：4 种顺序 → 4 个不同指纹**（`6a0a411c`/`c39b1000`/`c152f3d0`/`dff8d5e4`），路数 738~748、瓦片 10760~10912。
  预览页语义（传 `cq/cr=0,0`）指纹 `f3f1dfe0` ≠ 无中心 `6a0a411c` ⇒ **E2 分叉实证**。
- 根因二连：① `bfsRoad(..., roadTileIdx)` 把已建路格当 2 费高速路 ⇒ 折线取决于「首次建成时路网已有多少路」；
  ② 绕行闸的骨架判定用 `skeletonEdgesFor(i, j)`（**当前扫描格**的 5×5 池）⇒ 同一 rkey 会因「哪个区域格先扫到」得两种答案。
  ⚠ ② 是这次挖出来的**新根因**，此前只归因于 ①。

## 2. 改法（mapgen.js -219/+81）
- 生产寻路恒 `bfsRoad(pA.q, pA.r, pB.q, pB.r)`（不传第 5 参）；
- 骨架集按**边**归一：取该边**小 id 端**所属格的骨架集（`skels[spk]` 每次调用内缓存）；
- 需求边预算 `COST_MAX÷ROAD_W_ROAD=60` → `ROAD_STEPS_MAX=40`（纯地形下与 `d0×minW>COST_MAX` 等价，去掉必败 A*）；
- **整批退役顺序依赖机器**：`roadTileIdx`/`roadTileRef`/`roadFailTrials`/`roadFailVer`/`diRetryQueue`/`drainMark`/
  `failMark`/`trialBump`/`trialClear` + deferred 重试轮 + drain 消化块。`roadFail` 由状态变**派生量**（一次判终身）。
  `roadSet` 退化为 `cacheSet(roadCache,…)`；`resetRoads` 只剩 roadCache/roadFail/roadVer。
- `bfsRoad` 的 `roadTiles` 参数**保留**（`verify/trace_road.mjs` 仍在用；且 w3 的 PRUNE 锚点依赖该分支）——标注「只剩诊断用途，别在生产传」。

## 3. 修后实测（同机同 seed 同窗口）
| | HEAD 复用 | **B 纯地形** |
|---|---|---|
| 4 种顺序指纹 | 4 个 | **1 个**（`ff8a7f68`，含 cq/cr） |
| 二遍新增 / 路 / 瓦片 | 0 / 739 / 10768~10912 | 0 / **716** / **11780(+9.5%)** |
| 冷建一遍 | 22.0~24.9s | **13.3~15.6s（-33%）** |
| A* 次数 / 合计 / 均 / 最慢 / p50 | 1118 / 16.2s / 14.5ms / 165ms / 7ms | **954 / 9.7s / 10.2ms / 88ms / 5ms** |
| 失败 A* / 二段重搜 | 316 次(8.6s,53%) / 313 次 | **231 次(3.3s,33%) / 0** |

## 4. 回归（全绿）
- 离线 9/9：`sync --check`、vein_marker、terrain、edge_falloff、w5、w6 23:0、draw 32:0、settle_road 305 条/7 圈、**w3 13/13**（连跑 2 轮）。
- 服务端 **6/6**（隔离实例 `:8150` + 自有 `%TEMP%` 库，未碰 8140 与仓库 `db/`）：frontend_smoke / w1 / w2 / w4 /
  **`verify_map` ✔（22 项顺序依赖失败 → 0）** / scan_poison 1681 块 bad=0。`dotnet build` 0 警告 0 错误。

## 5. w3 ⑦ 阈值重标（**重要纠正**）
旧「最慢单 region < 250ms」在本机是**噪声主导**：同代码实测 max = 221/290/297/307/**605**ms（605 那次整轮从 14s 涨到 19s）。
且**同 harness 实测 HEAD 281.8ms 一样 FAIL** ⇒ 250ms 早已误报，**不是 B 引入的回归**。
改为两条：**第 3 慢 < 400ms**（尾部稳健：B 178~181 / HEAD 230~241）+ **最慢 < 500ms**（灾难红线，原意是防 A* 时代 ~540ms）。
`STEP_CAP_A` 60→40。w3 12 项 → 13 项。

## 6. 关键手法（可复用）
1. **断言式补丁脚本**：改 mapgen.js 用 Node 脚本 + 每条替换断言「唯一命中数」，**先对原文逐条校验、全绿才写盘** ——
   一次跑出全部锚点是否失配（比逐条 Edit 试错快得多）。⚠ 源文件里有**转义引号**（`(Set \"q,r\")`）与 **CRLF**，
   匹配串要按实际字节写、写回要 `replace(/\n/g,'\r\n')`。
2. **顺序无关性 harness**（`%TEMP%/wb_b_run.mjs`）：4 种扫描顺序 + 指纹（逐路 rkey|折线量化|瓦片集 排序后 FNV）+
   二遍新增 + cq/cr A/B。**这是验证「输出与顺序/缓存冷热无关」的通用手法**，比逐个 assert 更有力。
3. **逐次 A* 注入计时**：`__lg(seg,len)` 在 `search()` 返回处记录（注意签名顺序，第一次写错导致 len 全 undefined）。
4. 引擎副本 A/B：复制 `Engine/js` → 用 `wb_b_patch.mjs` 按 mode(HEAD/B) 打补丁 → 脚本 JSDIR 读 `ENJDIR`。

## 7. 遗留
- **A5 未动**（region 首请求 V8 门闩内 ≈195.8ms）；B 之后它更好做：预算只影响先后、不改最终路网。
- **峰值第一顺位不是 A***：最慢格 279ms 里 A* 只 128ms，其余 ~151ms 是 `scanDemandEdges` 的 `rngDominated`
  （每对候选重扫 50 邻格 × `settlementsFor` + 2×`isqrt`）⇒ 要压峰值先做「按格预建邻域池」（P3，未动）。
- `verify/bench_road_drain.mjs` 已随复用退役（跑会抛「注入点未找到」），**加横幅保留未删**，等用户定夺。
- ⚠ 世界内容已变：用于**非测试库**时必须停服清 `db/zongmen.sqlite*`（本次按用户指示未清）。

---

# 2026-09-14（前端表现升级）· 匾额 / 灵脉山体 / 地表让位 / 云气

需求与勾选见 `待办事项/前端表现升级-匾额山体云气.md`（已全部 `[x]` + 回填验证数据）。
**纯前端改动**（`web/js/{main,textures,bldg_ink}.js` + `web/index.html`），未碰引擎生成逻辑 ⇒ 不需重建服务端、不需清库。

## 做了什么
- **A 竖排匾额**：`main.js drawNameBanner()` 米黄纸签 + 手撕纸边 + 内框双线 + 焦墨**单列竖排**字
  （`-bh+padT+lineH*(i+0.5)`）+ 引绳连回锚点；签**一律在地物上方**（`bottomY = anchorY - lead`，
  `anchorY` 取图标顶/建筑群上沿之上）；倾角 `bannerTilt()=hash32(name)` 恒定 ±1.6° ⇒ 不闪。开关 `#btnBanners` / `?nobanner=1`。
- **B 氤氲灵脉山**：`textures.js` 新增 `MIST_PAL/mistMass()/mistBand()/drawMistyPeak()`（7 层柔边叠色、**无硬轮廓**、
  山脚化雾、云气横带），`propVein(el)` 改用它 + 灵根敷色两道 + 降对比符纹。**图集索引不变**：
  灵脉峰仍 id **50..54**（第 6 行 2~6 列），大世界岩/雪峰 **40/41/56/57/42/43/58/59**。
- **C 地表让位**：新增 `propBlock`(Set) + `blockedVer/syncedVer` + `propBuilt`(Map)；
  `rebuildPropBlock()` 收集**建筑占地格 + 道路折线按 `hexW*0.34` 步长采样的格**（不是只取端点）；
  `refreshChunkProps(key)` 传 GPU 前过滤精灵；renderer 新增 `updateProps(key,data)` 只换该块实例、不重跑淡入。
  版本号门控 ⇒ 平移缩放零开销。调试开关 `?noyield=1`。
- **D 压水**：`bldg_ink.js` 新增 `KINDS['栈桥']`（**故意不进 `KIND_LIST`**，保住「26 种 = 服务端 BUILDINGS+CORE_KIND」对齐）；
  道路分陆/水两路：`pushDash()` 切虚线段 + renderer 第三条缓冲 `roadBufWater`（石青墨），水上即虚线航道。
- **E 云气**：`textures.js buildClouds()` 4 变体（`CLOUD_W/H=128/84`×2）：接地淡影 → 云身 → 近白云顶 → 灰绿云底阴面 → 淡墨软线，
  全程径向渐变、无硬边；`main.js drawClouds()` 世界锚定 + `CLOUD_PARALLAX=0.88` + `CLOUD_DRIFT=7px/s` 东移，
  播种密度由格号 hash（六成格子不播 ⇒ 疏朗），绘于静态层之上、`hexHi` 之下。开关 `#btnClouds` / `?nocloud=1`。

## ⚠ 本轮最重要的两个发现

1. **`refreshChunkProps` 的 null 崩溃（已修）**：纯海区块 `pn=0` ⇒ `pb.chunkToArrays` 返回 **null** 而非空数组，
   直接读 `.length` 会在渲染循环里抛异常 → `showFatal` **整页不可用**。取值前必须判空：
   `var n = a.propSprites && a.propCenters ? a.propSprites.length : 0;`
   同一类坑在 `regionCells` 的 `pack.roads` 上已由既有代码防过（`pack.roads || []`）——**凡"可选包"一律判空**。
2. **「建筑压水→栈桥」当前不可达**：`Engine/js/mapgen.js:836` `if (f.biome <= BIOME.OCEAN) continue;`
   （`BIOME={DEEP:0,OCEAN:1}`）⇒ **建筑永不落水**。离线扫 seed 42 × 14 区域全部聚落足迹，落水建筑 **0 座**。
   「水上有桥」的可见形态由**临水建筑自带栈桥**承担（码头/渔船坞/渔亭 `FACE_RULE='water'`，painter 里栈桥
   自岸边 v=0.1 伸到 v=1.5 入水）。显式分支保留为防御，待用户拍板是否放开生成规则。

## 验证手法（**headless 看不到图时的通用解法，值得复用**）
1. **A/B 逐像素差分**：同机位同 seed，只切一个开关 → `verify/png_stats.mjs A.png B.png` 读
   「差异像素占比 / 强差占比 / 墨像素占比」。实测：
   - 匾额 开/关 → 差异 **0.98%**（>24）、0.32%（>120），bbox `1021×428` 集中在聚落上方；
   - 让位 开/关 → 墨像素 **9.074% vs 9.266%**（开启后墨变少 = 树·山确实被抹平），差异 1.72%；
   - 云气 开/关（**冻结漂移**）→ 差异 **3.71%**，墨像素 9.054% vs 9.399%（云比地图亮 ⇒ 开云墨更少）。
2. **离线引擎探测代替在线寻点**：新增 `verify/probe_spots.mjs`（`node verify/probe_spots.mjs 42 --R=9 --BR=4`）
   直接调 `MapGen.roadsNear(9999)/growTownFootprint/communityOf` 吐出「路压水 / 建筑压水 / 聚落 / 最近灵脉」坐标。
   比让浏览器漫游找点快一个数量级、且可复现。seed 42 关键落点：最近大灵脉「金属矿脉」格 **(-8,0)**；
   最近水上路段 **(-37,42)↔(-38,43)**；归元宗 **(-10,11)**、承天城 **(-27,-2)**。
   ⚠ `communityOf` 返回体**没有 `exists` 字段**（键为 i/j/q/r/element/spirit/x/y/veins），按 `exists` 过滤会永远空。
3. **实机截图走 `verify/live_cap.mjs`**（前端自截回传通道，无 CDP）：`node verify/live_cap.mjs <url> <out.png> 90 1400x900`，
   URL 带 `capture=1`。~6s 一张，尺寸可控（须旧版 `--headless`）。注意它落盘到共享的 `verify/capture.png` ⇒ **必须串行跑**。
4. **CDP `Runtime.evaluate` 在这张页面上会偶发永久挂起**（实测 >100s 无返回，卡在首次 wait 之后的 evaluate）。
   本轮因此**放弃 CDP 取数**，改走上面 1+2。`verify/cdp_feat.mjs` 保留（已加 **100s 硬看门狗** + 阶段日志，
   把"挂死"变成可诊断失败），并在 `main.js` 的 DEBUG 段留了 `window.__feat()`（计数汇总）与
   `window.__spots()`（路压水/建筑压水格坐标）两个零副作用钩子备用。

## 新增调试开关（日常游玩不传）
`?nobanner=1`（关匾额）· `?nocloud=1`（关云气）· `?noclouddrift=1`（云不漂移，供严格 A/B）· `?noyield=1`（关地表让位）
· 既有 `?nobldg=1 / ?nofade=1 / ?capture=1 / ?debug=1`。

## 回归
离线 **9/9 全绿**（exit 0）：`sync_preview_inline --check`（bldg_ink 86706 字节）、`check_preview_vein_marker`、
`check_preview_terrain`、`check_preview_draw`、`check_preview_settle_road`、`check_edge_falloff`、
`w5_sprite_range`、`w6_bldg_face`、`w3_bfs_road`。截图归档 `待办事项/img/`（10 张 1400×900）。

## 新增文件
`待办事项/前端表现升级-匾额山体云气.md`（待办+验证台账）· `verify/cdp_feat.mjs`（CDP 验数+截图，含看门狗）
· `verify/probe_spots.mjs`（离线定点探测；**故意不用 `_` 前缀** —— `.gitignore` 里 `verify/_*.mjs` 会被忽略，
留着迟早被"清理临时件"扫掉）· `待办事项/img/up*.png`（11 张 1400×900，全部 live_cap 出图）。

---

# 2026-09-14（续）用户三条反馈修正 —— 山体重做 / 灵脉牌匾+开关+位置 / 滚轮冻结

用户原文：「1.山体样子不对，美术风格不对，需要重做 2.牌匾在灵脉也要有，另外：开关没做，
其次是位置超出正上方太远 3.现在滚轮放大缩小时，有时候没响应，只有拖动屏幕时才会有响应
（滚轮的数值有记录，但是画面没变）」

## ① 灵脉山体重做（第四版，定稿）
**根因**：PROP_VS 把整个 128 格**非等比**映射到 W×H（W 常是 H 的 1.5~2.2 倍），自己另造的山形
上屏必被横向拉宽。前三次失败的形态：v1「玻璃锥」、v2「兔耳土包」、v3「苍白圆锥/帐篷」。
v3 之所以苍白：把「氤氲」理解成「把山洗白」—— `dark` 提到 `[146,162,126]`、`g0` 压到 0.70、
再叠 0.50 雾光 ⇒ 没骨没体量，比大世界岩峰还难看。

**定稿做法**：`propVein(el)` 的**山形骨架直接复用大世界已验证好读的 `drawPropPeak`**，只换
「色 / 气 / 皴 / 符纹」——
- 副峰 `[96,118,96]/[152,172,138]`、主峰 `[58,80,68]/[116,138,108]`（**青灰绿；主峰 dark 与大世界
  岩峰 `[60,54,46]` 同明度、只换色相 ⇒ 有体积不飘**）；
- `{noCrag:true, g0:0.86/0.90, g1:0.48/0.50, lit0/1/2=青白}` —— **g0 必须回到 ~0.90**；
- `miDian()` 米点皴代岩层横裂；山脚 + 山腰 `mistBand()` 云断；元素色 wash（0.13/0.06）；
  `drawSigil` 缩 0.62 落山体中下段；6 点灵气游丝只落山脚（y≥62，免得峰面像撒糖霜）。
- **最关键一条：云气感来自「山脚云断 + 山腰淡霭」，不是把整座山变淡。**
- `drawPropPeak` 为此新增 `opt.g0/g1`（脊线对比）、`opt.lit0/1/2`（受光面色）、`opt.noCrag` 三个参数。
- **整族删除**：`mistPeakPath` / `mistShade` / `mistPeakBody` / `drawMistyPeak`（自闭环死码，全仓库无外部引用）；
  `MIST_PAL` 精简为只剩 `pale`(云断) / `dark`(米点)，青灰绿山体色内联在 `propVein` 的 `drawPropPeak` 调用里。

**自检工具**：新建 `tools/prop_sheet.mjs [--hero] [--R=8]` → `verify/_prop_sheet.png`
（原尺寸 2.4x 看板 + 6x 特写行 + PROP_VS 实机尺寸条）。**改精灵美术必先跑它** —— 只看原图会把比例判错。
本轮靠 `--hero` 6x 对拍才看出「灵脉峰 vs 大世界山 B」的苍白问题（大世界山 B 实色有骨、灵脉峰像纸锥）。

## ② 灵脉牌匾 / 开关 / 位置
- **灵脉牌匾**（新）：`main.js renderStaticInto` 里新增灵脉名牌循环 —— 与聚落同款竖排纸签，
  **受「匾额」开关管（不是「注记」）**，`z>=0.85` 才画；签面薄敷灵根本色 `tint` + 签脚一枚元素色印；
  签底立在脉峰之上 `ps3.y - hexR*z*(level===0?2.55:2.25)`；画在聚落名牌**之后** + `avoid:true`
  ⇒ 与村名撞位时上让一档。已实机确认 `金属矿脉灵脉（大）`、`雾瘴湖泽灵脉（中）` 两签均在。
- **「开关没做」= CSS 裁切，不是 JS 没写**：4 个按钮的 `click` 一直存在且都调 `forceStaticDirty()`。
  真因是 `#controls` 是 `.panel`，其 `clip-path: polygon(...)` **裁剪整棵子树**，而 `#controls .row`
  是不换行的 `display:flex` ⇒ 开关加到 4 个后被挤出面板、落在裁切区外**看不见也点不到**。
  修法：`#controls .row` 加 `flex-wrap:wrap; justify-content:flex-end`，统计拆到第二行。
- **位置太高**：聚落签原先用**常数** `ps2.y - 4.8*hexR*z` 当抬升量 —— 镇子小时签子飘到半空。
  改为 `bldgAnchor(st)`：取建筑群**世界包围盒上沿中点**，抬升压到 `1.15*hexR*z + 2`；
  引绳也由 `fs*0.95` 缩到 `fs*0.62`。
- 新增全局 `BANNER_MAX = 90`、`bannerBoxes/bannerHit()` 撞位占用表（每帧在 `renderStaticInto` 里清空）。

## ③ 滚轮缩放偶发无响应（画面冻死）
**根因（代码级可证）**：静止降帧分支写成
`... && !drag && chunkBusy.size === 0 && (frameCount & 1)` —— 而 `frameCount` **只在真正渲染的帧里 `++`**。
相机静止且它恰好为奇数时，跳帧分支**永远成立**（它自己不会前进）⇒ 整个 `loop` 冻死：
滚轮改的 `cam.tzoom/cam.zoom` 数值在涨（`onWheel` 里两个都写了）、画面纹丝不动；
只有 `drag`（让 `settled=false`）才"活一帧"。**这正是用户描述的症状。**
**修法**：改用**每次 loop 都自增**的 `tickCount`（`var tickCount = 0; tickCount++` 在 loop 开头）。
最坏只是隔帧降载，永不再冻。

## 本轮回归与门禁
- `node --check web/js/textures.js` / `web/js/main.js` → exit 0。
- `sync_preview_inline.mjs --check` → 「✔ 全部同步」。**本次改动只在 `textures.js`/`main.js`，这两个文件
  不在 `灵脉预览.html` 的内联清单里（只内联 mapgen-config/mapgen/bldg_ink）⇒ 无需重内联。**
- 离线回归 **9/9 全绿**（均 exit 0）：`sync_preview_inline --check`、`check_preview_vein_marker`（默认视野
  大灵脉 1.23px→4.6px 可见）、`check_preview_terrain`、`check_preview_draw`（32 通过/0 失败）、
  `check_preview_settle_road`（305 条/7 圈）、`check_edge_falloff`、`w5_sprite_range`、
  `w6_bldg_face`（23/0）、`w3_bfs_road`。
- **`live_cap` 只含地图画布（glcanvas + overlay），不含 DOM 面板** ⇒ 要看按钮必须另走全页
  `--headless` 截图（且 `--headless=new` 忽略 `--window-size`）。本轮临时用 `verify/_ui_shot.mjs`
  （旧版 headless，精确 1400×900）截 `verify/_ui.png` → 裁 `verify/_ui_tr.png` 核对：三行齐全
  `[种子][山河重铸][另起一界]` / `[灵脉][注记][匾额][云气]` / `已探明 宗门村镇 23 · 道路 46 · 灵脉 64`。
- 牌匾位置实测（裁图 `verify/_c8.png`）：seed 42 的「金属矿脉」就在「古井村」上 ⇒ 两签同址，
  灵脉签（9 字）被村签顶到 278px、村签 387px，**上让机制工作正常**；非碰撞位约在峰顶上方 ~30px（贴合）。

## 台账 / 备忘
- `待办事项/前端表现升级-匾额山体云气.md`：§A2/A3 加修正注、§B 整节重写为「四版定稿」、
  新增 §G（本轮三条修正逐项）与 §H（工具/调试清单）、§遗留 增 4/5（灵脉签同址可见性、是否要更"仙"）。
- `MEMORY.md` 已超限被截断 ⇒ 按提示做了一次精简重写（15k→~8k 字节），保留全部坑位与真源，压缩行文。

## 二轮反馈「名字位置还是不对」（同日 18:40）

用户给了一张自己的截图（1167×1007，**不是 seed 42**）＋ 5 处红箭头标注。逐条查完：

- **量坐标的教训（重要）**：我先用"叠网格缩略图"目测读牌匾/箭头坐标，**读数与真值差 60px 以上**，
  据此推出"左半屏右移 / 右半屏左移"的错误模式，白费好几轮。改用**程序化连通域检测**
  （米黄纸签 `r-g>4 && 均亮>205 && r>228` / 红箭头 `r>165&&g<105&&b<105`，16px 桶 BFS 合并）
  后坐标才准：桃溪村 cx=379、乌衣镇 cx=1114、甘泉集 cx=944。
- **真因** = 我上一轮（§G4）改 `bldgAnchor` 时把**水平锚点从 `st.x` 换成了建筑包围盒中心**，这是回归。
  建筑地皮含农田/林地/码头/水车，分布不对称甚至离群 1~2 格 ⇒ 中心被整体拉偏。
  **修法：水平回 `st.x`，垂直保留"建筑群最北格 `y0`"**。
- **量化**（seed 42 / `zm=2.6` / cam 原点，同机位 A/B 差分）：差异 >24 占 **2.69%**；
  古井村牌匾 x **440→487**，建筑群橙点目测中心 ≈485 ⇒ 对齐。**附带副作用修好**：牌匾右移后
  不再与灵脉签相撞，此前被 `avoid` 跳过的「金属矿脉灵脉（大）」重新出现（BEFORE 缺 / AFTER 有）。
- **灵脉牌匾复查结论：未改**。灵脉签 x 恒 = `w2s(v.x,v.y).x`，与 `IT.drawVeinFlower` 同源不可能错位；
  用户图实测签底 757 / 峰顶 ≈767（贴住）。用户圈的 2 条灵脉疑似"箭头顺带经过"。
- **新调试能力**：`?plaqdbg=1` 叠「聚落中心 / 聚落格 / 包围盒锚点 / 每个建筑格 / 灵脉」十字 + 标签；
  `?ancgeo=1` 锚点退回包围盒中心。**这是本地判"牌匾 vs 地物"关系的唯一可靠手段。**
- 门禁：`node --check web/js/main.js` OK；离线回归 **9/9 全绿**；证据图归档
  `待办事项/img/fx5_plaque_anchor_AB.png`（并排 BEFORE/AFTER）+ `fx6_plaque_anchor_after.png`。
- 另：`verify/_jcrop.mjs`（任格式裁切放大）踩坑 —— `transform:scale(S)` 会把 `left/top` 偏移
  一并放大 S 倍，偏移必须写 `-X*S`。本轮临时脚本 + 临时图共 23 个已按规程清理
  （被跟踪的 `verify/_scratch_diag.mjs` 未动，`git ls-files verify` 仍 32）。

## 三轮需求「灵脉更高 / 更锋利 / 按五行配色 / 异灵根 / 专用配置」

用户原文：「其次灵脉要更高一些，跟附近的山脉区别开然后：更多锋利棱角，其次不是都是绿色的要根据五行
之类的的来定义颜色，后面可能还有多种灵气，随机组合的，然还有异灵根，不是统一颜色的，要有专门的
配置文件来配置颜色」。

**落地要点**
- 新建 **`web/js/vein-skin.js`** = 颜色/形状唯一真源（`global.VeinSkin`）：SHAPE(hScale 1.90 / wScale 0.82 /
  cell 主副峰几何 / facet 3 / ribs 3 / miDian 12) + ELEMENTS 五行(金木水火土) + VARIANTS 异灵根(雷风冰暗)
  + DUAL 组合表 + elementPal/variantPal/variantSprite。`index.html` 挂载在 `textures.js` **之前**。
- **图集 row4 死代码复活**：tile 索引 = `biome*4+variant ≤ 31`，`HEX_FS` 的 `biome-8→row4` 分支**永不触发**
  ⇒ 原「灵脉格底」从来画不出来。把 row4 col0..3 给**异灵根灵脉峰 id 32..35**（col4..7 空置）；
  五行峰仍 row6 col2..6 = id 50..54。引擎侧 `mapgen.js` 加 `VEIN_VARIANT_ORDER=['雷','风','冰','暗']`，
  `propSpriteFor(f)` 有 `f.vein.variant` → `32+indexOf`，否则 `b+42`。
- **颜色契约**：`elements[].glow` 必 == `mapgen.js ELEMENT_RGB`；`variants[].glow` 必 == `VARIANT_RGB`；
  `variants` 键序必 == `VEIN_VARIANT_ORDER`。**第一次就踩了** —— glow 我先写了更亮的自定值
  （金 `[206,186,128]`），与引擎 `[196,176,120]` 不符 ⇒ 全部改回引擎值。护航脚本
  **`verify/check_vein_skin.mjs`**（11 断言，含 W/H≈0.890 计算）钉死，新增元素/异灵根后必跑。
- **绘制重写**（`textures.js`）：`propVein()`/`drawVeinPeak()` = 折线尖峰(肩部正弦) + **不透明岩体
  mid@0.97** + 硬受光/背光多边形 + 焦墨轮廓 + 脊线 + 棱线 + 米点 + 仅底部 12% 云断；
  `drawSigilVariant` 四符文；`mistBand(...,col)` 每灵根异色云。**第一版就是"玻璃锥"重演**：主体用垂直
  渐变淡到 `mid@0.0` ⇒ 整座半透明像纸锥；改成不透明实色 + 硬受光面才立住。
  另：颜色第一版太淡（读成纸锥）⇒ 全面加深 back/mid（金 back[70,58,32] mid[142,120,68]；
  火 back[74,28,20] mid[154,64,42]；暗 back[22,18,30] mid[72,60,90]），受光面 alpha 0.50→0.36。
- **更高**：`renderer.js PROP_VS` 读 `window.VeinSkin.shape` → `VEIN_SHAPE{h:1.90,w:0.82}`；
  灵脉 `hrand` 收窄为 `0.72+0.28*hrand`。**踩坑**：hScale 1.55 时灵脉最高只与大世界雪峰齐平，
  而 hrand 全域 [0,1] ⇒ seed 74 的雷灵脉直接埋进雪里；必须**同时**提 hScale + 收窄 hrand 才"恒高"。
  ⚠ 改这两个系数必须**同步 `tools/prop_sheet.mjs`**（它按同公式摆实机尺寸条），否则看板失真。
- 门禁：`node --check` 全过；离线 **9/9 + check_vein_skin**；`w5_sprite_range` 加两条新断言
  （「每个五行群系出现过本元素灵脉峰」+「异灵根位 32..35 全部被使用」）；`tools/prop_sheet.mjs` 重跑出看板。
- 实机：用户 8140 实例（PID 5052）kill → `db/zongmen.sqlite` 改名 `.bak-20260914112258` → 重启清库重建
  （sprites 载荷变了 ⇒ 必须清库）。定点 seed 42 金「金属矿脉」(-8,0)、seed 74 雷「雷击崖」(4,-9)。
  证据图 `fx7_vein_element_variant_sheet.png` / `fx8_vein_live_gold.png` / `fx9_vein_live_thunder.png` /
  `fx10_vein_closeup_gold.png` 归档 `待办事项/img/`。
- 台账：`待办事项/前端表现升级-匾额山体云气.md` 新增 **§J**（J1~J7）；§遗留 5 标注"已由 §J 作答"。
- 环境备忘：隔离实例首次选 :8141 撞「address already in use」⇒ 改 **:8177** 成功。

## MEMORY.md 超限清理（本次）
- `MEMORY.md` 又超注入上限被截断（9431 字 / 14453 字节）⇒ 按规程精简重写为
  **4579 字 / 7090 字节（−51%）**。做法：**去重**——把「构建/回归跑法」整段指向已存在的
  skill `zongmen-regression`（它本就有隔离实例/引擎 A/B/顺序无关 harness/时间判据/误删事故全部内容），
  MEMORY 只留该 skill **未覆盖**的坑（Chrome headless=new、CDP 挂起、目测坐标必错+?plaqdbg、
  复现 seed、live_cap 串行、文件删除高危）；并把「灵脉山体」节整体换成三轮后的新状态。

## 四轮：灵脉**大/中/小分档**（占地 + 高度）
- 需求原文：「大的不变，中的只占有 当前格子和下面两个格子，小灵根只有一个格子，高度也要有区分」。
- 引擎 `mapgen.js`：新增 `VEIN_LVL_BELOW=[[0,1],[-1,1]]` + `veinFootKeep(level,dq,dr)`
  （大=旧 7 格**逐位不变** / 中=中心+下方两格 / 小=仅中心；两者已导出到 `global.MapGen`）；
  `fields()` 灵脉覆写守卫加 `veinFootKeep(vn.v.level, q-vn.v.q, r-vn.v.r)`；
  `buildChunk()` 用 `e: (f.vein ? f.vein.level : f.e)` 把**等级复用海拔通道**下发。
  ⚠ 几何依据：`tileToWorld` 的 `y=1.5*HEX_R*r`（y 随 r 增）⇒「下方」= `dr=+1`，即 `[0,1]`/`[-1,1]`；
  不是同排的 `[1,0]/[-1,0]` 也不是上方的 `dr=-1`。
- 配置 `web/js/vein-skin.js`：新增 `levels` 三档（hScale **1.90/1.58/1.22** + 各档**收窄** hRand
  [.72,1]/[.86,1]/[.72,.96]）+ `levelInfo(i)`；导出 `levels/levelInfo`。
- 渲染 `renderer.js PROP_VS`：灵脉分支改为按 `iElev`(**=等级，不再是海拔**) 取 `hs` 与 hrand 包络；
  GLSL 字面量经 `EPS=1/1024` 取整 + `toFixed(3)`，避免 `1.9000000000000001` 进着色器。
- 门禁：`verify/check_vein_skin.mjs` 新增 **B 段「等级契约」12 项断言（总 29/29 ✔）**；
  `verify/w5_sprite_range.mjs` 默认半径 **8→12** —— 分档后「异灵根峰」格数骤降 ~70%，±8 窗口
  漏掉最稀的雷档 → **假 FAIL**（非代码 bug）。离线回归 **10/10**；`sync_preview_inline --check` ✔。
- 实测（`verify/_probe_vein_lv.mjs`，直调 `communityOf/veinNear/fields`，以 `fields().vein` 为准）：
  大 **76 根×7 格** · 中 **91×3** · 小 **213×1**（其中 1 根 kept=0 —— 两群落把灵脉放在**同一格**的既有碰撞，
  非本次引入）⇒ 无任何等级超上限。高度契约严格分离：**大[63.29,68.40] > 中[54.76,56.88] > 小[40.64,43.45] px**；
  同 hash 同刻度并排实测 **66.7 / 56.2 / 42.5**（比 1.18 / 1.33）。
- ⚠ **量测坑（踩过，重要）**：想「用像素高度证明 大>中>小」两条路都不通 ——
  ① 固定屏幕窗口跨机位不可比（三张图相机目标不同，中/小顶部还被窗口裁掉）；
  ② 金色阈值 `r>g>b && r-b>=45` 会被**橙色花树**命中（到金板距离 <105）→ 假高度；
  ③ 实机帧里灵脉格水平仅隔 ~10px（zoom1.5）而精灵盒半宽 ~45px ⇒ 大灵脉的**上排邻格峰必然盖住中心格峰**，
  **单格高度在实机帧里无法隔离量测**；簇包围盒高度又会被**占地行数**污染（大含上方邻格）⇒ 不可跨档比簇高。
  ⇒ 高度差只能由「同底同刻度并排」的确定性看板判定（`tools/prop_sheet.mjs` ② 段，正是其设计用途）。
  顺手修了该工具既有缺陷：全量模式页高 1560 **装不下 ② 段**（Chrome `--screenshot` 只截视口）→ 改 2100。
- 证据图归档 `待办事项/img/`：`fx11_vein_tier_sheet.png`（三档同刻度并排 + H 值）·
  `fx12_vein_tier_trio_live.png`（seed42 群落(4,3) 三档**同屏同机位**）·
  `fx13_vein_tier_large_live.png`（大=多峰大山体）· `fx14_vein_tier_mid_small_live.png`（中/小对比）。
- 台账：`待办事项/前端表现升级-匾额山体云气.md` 新增 **§K**（K1~K7）。
- MEMORY.md 再压 **4579→3724 字**（去重 + 重写，坑位未丢）。
- ⚠ 发现 `待办事项/` 下 **10 个** md（`review.md`+`review_v1~v5` / `todo1~2` / `前端建筑绘.md` /
  `城市选址与建筑足迹重构.md`）处于 **D（已删除但仍被 git 跟踪）** 状态 —— 疑似历史批量清理连带，
  当时**未擅动**，已向用户报告待示。
- ✅ 20:02 用户确认这 10 个**均为已完成文档** ⇒ 已 `git rm --cached` 登记删除（只动索引；
  工作区本就已无文件，不需恢复）。判据：HEAD 里它们分别是 6 份 code review 报告 + 2 份 todo 清单
  + 2 份设计稿（539/194 行），均属阶段性产物。
- ⚠ 顺带审计出**应入库却漏跟踪**的资产并 `git add` 补录：`web/js/vein-skin.js`（**渲染配置真源，
  丢失风险最高**）· `verify/{check_vein_skin,probe_spots,cdp_feat}.mjs` · `tools/prop_sheet.mjs` ·
  台账 `待办事项/前端表现升级-匾额山体云气.md`。审计法：按目录比对 `git ls-files <dir>` 的扩展名
  惯例 vs 工作区实际文件（`web/js/` 已跟踪 7 js → 第 8 个漏了；`verify/` 32 mjs；`tools/` 4 mjs）。
- ⚠ `待办事项/img/`（14 张 fx* 证据图，**14.6MB**）仍未跟踪。`docs/` 下 27 张 png 是**入库**的
  （≠ 之前误读的 0，是 ls-files 路径带引号害的 endsWith 失真）⇒ 但 img/ 从未入库且体积大，
  是否入库待用户定。
- ⚠ 教训：`git ls-files` 输出受 `core.quotepath` 影响会给中文/特殊路径加引号 ⇒ 按扩展名过滤
  必须带 `-c core.quotepath=false`，否则统计假 0。






---

## 2026-09-14 六轮：改错层回退 + 灵脉单格 + 山形「宽顶圆拱 & 下半渐隐」 + 云「祥云六型」

用户原文：「不是改大世界的山，是改灵脉的山啊，你改错了！！！改回来 灵脉不应该站好几个格子的，
只要站一个格子即可 …而且你的云也太丑了，参考这个画云这种风格的云，要多变，不要只有一个」

### 1. 改错层回退（用户点出「改错了」）
- 五轮误把**大世界地形山** `web/js/textures.js drawPropPeak()` 的 6 处（渐变 / 主轮廓 / 受光面 /
  背光面 / 雪帽 / 描边）改成宽顶 ⇒ **逐处回退**为原始窄峰二次曲线
  （`cx-w*0.28…cx+w*0.17…`；轮廓 3 点 `[cx-w*0.5,baseY]→[cx-w*0.27,baseY-h*0.52]→[cx-w*0.02,apeY+h*0.07]`，
  width 1.5 / alpha 0.5）。`opt.g0/g1`、`opt.lit0/1/2`、`opt.noCrag` 机制**保留**。

### 2. 灵脉占地：三档一律 1 格
- `Server/Zongmen/Engine/js/mapgen.js`：`veinFootKeep()` → `return dq===0 && dr===0;`；删除 `VEIN_LVL_BELOW`（含导出）。
- 端到端证据（新脚本 `verify/_vein_spots.mjs`，走真实入口 `MG.fields()`，与 buildChunk 同源）：
  **10 个 seed / 17 个灵脉地标 / 恰好 17 格**，相邻灵脉格对数 **0**；同窗口旧 7/3/1 规则本会占 **79 格**。
- 门禁 `verify/check_vein_skin.mjs` B 段改为「三档一律只占 `[[0,0]]`」+「等级不影响占地」+「六邻不保留」→ **27/27 ✔**。

### 3. 山形重写 + **下半截真渐隐**（本轮最关键根因）
- `veinPeakPts()/drawVeinPeak()` 重写：`VeinSkin.shape` 去 `facet/ribs`，新增
  `topW:0.30 seg:11 topSeg:7 fade:0.50 miDian:12`；`wScale 0.82→0.95`（W/H 0.890→**1.031**，不被横向拉宽）；
  `cell.mainCx66/mainBase118/mainW100/mainH96`（底座铺满 16..116）。
- ⚠ **根因**：原 ⑦「山脚化雾」是往渐隐区**叠加** mist 色（最高 0.50 alpha）—— **恰好把 ① 山体的渐隐重新糊实**；
  外部云断 band 还用了 0.70 ⇒ 山脚近乎实心（实测「逐行最大 alpha」直到最后 1 档才掉到 9，几乎硬边）。
  ⇒ 删 ⑦；云断 band 降档（`0.70→0.46` / `0.28→0.18`）；① 渐变改
  `0→0.96 / F*0.62→0.90 / F→0.58 / 0.84→0.15 / 1→0`；石纹抬到 `F+0.04` 以上。
  **原则（已写进代码注释 + MEMORY）：渐隐只能由 alpha 渐变实现，不能靠叠雾。**
- 实测（剪影探针）：灵脉·金 前景中位 alpha `半腰 187 → 山脚 9`（比值 **20.78**）；
  大世界山同法 `137 → 44`（比值 3.11）⇒ **只有灵脉峰下半化开**，两者一眼可分。

### 4. 云重做为「水墨祥云」六型
- `buildClouds()` 全重写：新增 `cloudPuff`（扁圆花瓣+下半阴+勾线）/ `cloudCurl`（**开放式**约一圈卷钩）/
  `cloudTail`（4 长弧）/ `cloudHead`（底大瓣+上缘小瓣+1 卷钩）/ `cloudRecipe`（6 配方），`CLOUD_N=6`
  = 长云 / 团云 / 双团 / 高云 / 卷云带 / 小云；`main.js cloudOf` 变体取模改**动态**（`% nsp`），
  不透明度 `0.40+0.36 → 0.72+0.26`。
- **反例已写进注释**：花瓣绕心均布 → 读成花/玫瑰；卷钩密而居中 → 读成花心；同尺寸花瓣串成链 → 读成气球堆。
- 机读签名 **6/6 互异**（宽高比/覆盖%：`2.8/26 · 1.7/21 · 2.2/24 · 0.8/21 · 2.5/22 · 1.6/9`）。

### 5. 新方法：读不了图时把「精灵剪影」量成文本
- 本轮 `Read` PNG 直接返回 `content filtered`（模型不支持图像）⇒ 自建探针页：
  `getImageData` 量 alpha 剪影 → 结果**文本注入 `<pre>`** → Chrome `--headless --dump-dom` 打到 stdout。
- ⚠ 判渐变**必须用「前景中位 alpha」**，不能用「逐行最大 alpha」（被米点皴/符纹等离散深色墨点污染
  → 会误判成「渐隐没生效」）。ASCII 覆盖率图只适合判**形/多样性**，判浓淡要回数值。
- 该手法已写进 skill `webgl-headless-verify`（新增专节，含可复制模板与判据）。

### 6. 验证 / 收尾
- 离线回归 **11/11 全绿**（含 check_vein_skin 27/27、w5 半径 12）；`sync_preview_inline --check` ✔；
  两块看板（`tools/prop_sheet.mjs`、`verify/_cloud_sheet.mjs`）重渲。
- 服务端验证走**隔离实例**（`dotnet build -o verify/_vmsrv` + `%TEMP%/wb/reg8177.sqlite`，端口 **8177**），
  **不动用户 8140**；`live_cap.mjs` 串行抓 2 张实机图（seed42 大灵脉 (-8,0) 特写 + 广角云毯）。
- 证据图入 `待办事项/img/`：`fx15_vein_peak_sheet.png` · `fx16_cloud_six_sheet.png` ·
  `fx17_live_vein_seed42.png` · `fx18_live_clouds_seed42.png`（img/ 累计 **18 张**）。
- 台账 `待办事项/前端表现升级-匾额山体云气.md` 新增 **§L（L1~L9，49 行）**，并给 §B / §E / §J1 / §K1
  加「已废 / 存档」标注（保留历史、避免误导 —— §K 的 7/3/1 已被 §L2 改写）。
- 清理：`verify/_*` 临时件 **30 文件 + `_vmsrv/` 1 目录**；**保留被跟踪的 `_scratch_diag.mjs`**；
  git 终检 verify/ 下**无删除项**（被跟踪文件完好）。
- skill 修正：`zongmen-regression` 两处过时数字（check_vein_skin 11→27 项、W/H 0.890→1.031）。

### 7. 追加：占地 A/B（引擎副本法）+ 三条教训
- **引擎副本 A/B（可复用）**：`Server/Zongmen/Engine/js/*` → `%TEMP%/wb/eng_old/`，**只**把
  `veinFootKeep()` 回退成 7/3/1（其余一字不改），用 `Zongmen__EngineJsDir=<副本>` 起 **8178**
  （新规则实例 **8177**）。同 seed 同机位各抓一帧差分：
  · zm=6 差异**只有一处** —— bbox `422,88→938,566`（517×479px）、中心 (680,327)，镜头中心 (700,450)
    ⇒ 灵脉峰确实立在中心格上（底座在屏心、峰体向上 ~360px）；旧 7 格处是**大团重叠的深色山簇**
    （墨 20.0% / 亮度 182.1），新 1 格处墨降到 **11.5%** / 亮度升到 **192.0**（重叠消失、整体变亮）。
  · **控制实验**：同实例同 URL **连拍两帧差分为 0.00%** ⇒ 渲染完全确定（判回归时可当基线手法）。
  · 副本校验：旧规则下 seed42 大灵脉 7 格 = `(-9,0)(-9,1)(-8,-1)(-8,0)(-8,1)(-7,-1)(-7,0)`。
- 证据图追加 `fx19_vein_occupancy_old7cell.png` / `fx20_vein_occupancy_new1cell.png`（600×580 裁图，zm=6）。
- ⚠ **教训 1（bash 转义，本轮真实踩坑）**：用 `node -e "..."` 在 bash 双引号里写**含反引号**的中文内容，
  反引号会被 bash 当**命令替换**吃掉 ⇒ 写进台账的整段变空、甚至出现 `undefined`（本轮 §L 三处损毁，
  已用文件方式修复）。**凡含反引号的文本一律先 Write 成 .mjs 文件再跑**，别用 `node -e` 行内传。
- ⚠ **教训 2（CRLF 副本）**：复制到仓库外的引擎副本仍是 CRLF，断言式替换前必须先
  `replace(/\r\n/g, '\n')`，否则命中 0 处。
- ⚠ **8140 现状（重要）**：用户 8140 实例的 chunk 缓存跨了多轮引擎改动（A/B 差分里左下/右上各有一块
  旧地形）⇒ 要看到「单格灵脉」必须 **停服 + 清 `db/zongmen.sqlite*` + 重启**。本轮**未擅动**用户实例，
  该步骤已明确告知用户。
- skill 追加：`webgl-headless-verify` 新增「主模型读不了图时把精灵剪影量成文本」专节
  （含 `rowsOf/ascii` 模板 + 判据 + 「判渐隐要用前景中位 alpha，不能用逐行最大 alpha」的坑）。

---

## 2026-09-14 七轮：灵脉峰缩到 40% + 云「更小·更透·地上有影」 + 建筑「场地」

用户原文：「灵脉的山还是太大了缩小一点缩小到原来的40%大小 / 云太大了，而且透明的太低了，
土地上面也没有阴影，这个要有 / 然后建筑的背景应该有一个场地的，避免出现svg太浅了导致被背景覆盖掉的情况」

**纯客户端改动**（`vein-skin.js` / `renderer.js` / `textures.js` / `main.js` / `bldg_ink.js`），
**未碰引擎** `mapgen.js` 与任何服务端载荷 ⇒ 严格说**不必清库**（但分块图集有缓存，用户重启 :8140 才生效）。

### 1. 灵脉峰 40%（单旋钮）
- `vein-skin.js` `Shape` 新增 **`sizeScale: 0.40`** = 全工程唯一「灵脉上屏尺寸」旋钮；
  `renderer.js` `VEIN_SHAPE.sc ← sizeScale`，`PROP_VS` 里 `VSIZE=sc.toFixed(3)` **只**乘进灵脉分支的 `ss`，
  **W/H 同乘** ⇒ 比值不变（仍 ~1.031，不被横向拉宽）；`3.3+1.2*hrand` 的**相对分档一字未改**。
- 上屏高：大 **3.16~3.42** / 中 2.74~2.84 / 小 2.03~2.17 uR（七轮前 大 7.91~8.55）。
- ⚠ **量测坑（关键）**：用「逐行最长连续赭金段 ≥ minRun」量峰体剪影时，**同一 minRun 在缩放前后含义不同** ——
  峰缩 40% 后只有最宽几行还 >30px ⇒ 高度被砍到 **0.11**（假失败）；放宽成「本帧峰宽 50%」又因**该格地面同色系**
  把地面连成片（**8.13**，更假）。⇒ 这类「同色系地面上的小目标」剪影高**不可机读**；改用**与阈值无关**的量：
  **峰底宽 `maxRun`**（旧 83 → 新 33 = **0.398 ≈ 0.40**，干净且决定性）。高度正确性由**大灵脉实机**
  （峰基宽 226→87=0.385 + 峰高 109→25=0.229 双降）+ **renderer.js 源码契约**（只有一个 `ss=VSIZE`）共同保证。

### 2. 云：更小 + 更透（`main.js`）
- 尺寸 `base 176 → 92`；`cloudOf` `sc: 0.72+0.95 → 0.78+0.62`；不透明度 `a: 0.72+0.26 → 0.32+0.22`（上限 0.98→0.54）。
- 实机 zm=1：云体覆盖 **1.05% → 0.18%**；同一像素亮度增益 **+66.3 → +36.7**（「更透」的可测含义）。

### 3. 云影（新图层）
- `textures.js buildCloudShadows()`：与 `buildClouds()` **同种子**（`31337+v*977`）⇒ 影与云逐瓣对齐；
  抽出 `puffPath()` 供云/影共用。影色 `[58,56,50] a0.40 blur7px`，同尺寸画布（`CLOUD_W*2 × CLOUD_H*2`）。
- `main.js drawClouds()` 改**两遍**：先收集可见云 `vis[]`，第一遍画影（`SH_DX0.10/SH_DY0.30/SH_A0.55/scale1.04`），第二遍画云。
- 新增调试口 **`?noshadow=1`**（关影）—— 用**单差法**量影：云在·影关 vs 云在+影 = **4740px 压暗**（软影 Δ20~45 共 2458px）。

### 4. 建筑「场地」（`bldg_ink.js hexPlate()` 加强，两后端同源）
- `paint()` 默认 `plateA 0.20→0.32`；调用点 `0.16→0.40`；`hexPlate` 加**地面柔光晕**（`B.radial`，`r*1.18`，`detail>=2`）
  + 多边形填充默认 `o.a` 抬到 0.32 + `detail>=1` **勾一圈墨线**（`lw=max(0.5,r*0.045)`）。
- SVG 契约：首块 `<polygon fill-opacity>` = **0.40**（旧 0.16）。

### 5. ⚠ 修掉一个隐蔽崩溃 bug（本轮重要）
`bldg_ink.js paint()` 里 `fn = KINDS[spec.kind] || KINDS['民房'];` **漏 `var`** ⇒ 在 namespace IIFE 的
严格模式 eval 下抛 `ReferenceError`，**整层建筑渲染直接崩**。补 `var` 后恢复。
教训：`bldg_ink.js` 是独立加载的 IIFE，改完必须至少 `eval + 调一次 svgBody()/paint()` 冒烟，光看源码看不出。

### 6. ⚠ 截图确定性（capmin 预热）
自截通道就绪阈值 `chunkData>=3` 在低缩放下**太松** ⇒ 未预热完就截，同 URL 两帧墨像素差 **6.4% vs 13.9%**。
新增 URL 参数 **`?capmin=N`**（截前静候 N 秒）。加 `capmin=10` 后**同实例同 URL 连拍差分为 0.00%**。
⇒ 以后 A/B 帧一律带 `capmin`。

### 7. 门禁 / 回归（全绿）
- `check_vein_skin.mjs` **30/30**（B 段新增第 13 组：`sizeScale` 有效 + ×sizeScale 后三档仍不重叠 + 大档上屏高 ≤ 3.6 uR）；
  `tools/prop_sheet.mjs` 同步把灵脉峰 `ss` 也乘 `sizeScale`（看板与实机同刻度）。
- 七轮三段机读探针（`verify/_fx7_probe.mjs`，**已按惯例删除**，配方固化进 skill）**全绿 exit 0**：
  ① 灵脉 配置真源/接线/算术/赭金 43247→10243(0.237)/峰基宽 226→87(0.385)/峰高 109→25/全幅 A/B 仅一处 145×103 小块/
  小灵脉底宽 83→33(0.398)；② 云 覆盖 0.18% / 亮度增益 0.55× / 云影 4740px；③ 建筑 SVG 0.40 + 柔光 + 实机变化 1.68%。
- **离线全量回归 10/10**（`_reg_drive.mjs` 驱动，已删）：sync --check、vein_marker、terrain、edge_falloff、
  preview_draw **32:0**、settle_road **305 条**、check_vein_skin、w5、w6 **23:0**、w3 **13 项**（57.4s，全过）。
- `sync_preview_inline.mjs`：`bldg_ink.js` 内联 86706→**87186** 已写回，复检「✔ 全部同步」。

### 8. 收尾
- 证据图（左右并排 = **旧|新**）→ `待办事项/img/`：`fx21_vein40_big_AB.png`（大灵脉峰带，裁 520,100,360,700，
  左右半显著差异 22.9%）· `fx22_cloud_z1_AB.png`（云 zm=1 全幅 ×0.55）· `fx23_bldg_plate_AB.png`（聚落全幅 ×0.55）。
  拼图器 `verify/_hstack.mjs`（纯 Node PNG 解码+拼接+编码，临时，已删）。
- 台账新增 **§M（M1~M10）**。
- 清理：`verify/_*` **76 文件 + 43 目录**（含 `_vmsrv/`）全删；`%TEMP%/wb` 下 `web_old` / `eng_old` /
  `reg8177.sqlite*` / `reg8178.sqlite*` 清光；**保留被跟踪的 `_scratch_diag.mjs`**（按 `git ls-files verify/`
  过滤，唯一被跟踪的下划线文件）。隔离实例 **8177/8178 已 kill**（用 `Get-NetTCPConnection` 取 PID 后
  `Stop-Process`，**未 taskkill /IM**，用户 8140 全程 OPEN 完好）。
- **仍欠**：用户重启 :8140 后客户端三改才生效（引擎未动，不必清库）。

---

## 2026-09-14 八轮：云「成批/小批/孤单」分层 + 聚落名牌**垂直锚点修正**（"偏高 4 格"）

用户原文：「1.现在发现云太少了而且没有成批的出现（需要部分成批，部分孤单，部分一小批的）
2.@image 这个在灵脉上往上偏移1个格子，但是在村落上就不对，往上了4个格子」

**仍纯客户端**（只改 `web/js/main.js`），不碰引擎 ⇒ 不必清库。

### 1. 名牌：垂直锚点 `min` → 建筑格 r 的 **p25**
- **根因（离线实测 `_anchor_probe.mjs`）**：聚落地皮常有一两格**离群地物**（农田/水磨/渔亭/码头）伸到
  主体以北 **2~3 档**（seed42 全部 **277 个聚落**：离群抬高 中位 **2** / 最大 **3** 档，≥3 档的 31 个）；
  y0 取 `min` ⇒ 签子被顶高 ≈3 档 + 抬升 ≈1 档 = **用户看到的"4 格"**。
- 改取 **p25**（`rs[floor(0.25*n)]`，= 内容密集区北沿）⇒「签底 ↔ 主体上沿」间隙统一 ≈1.4 档。
- 实机 A/B（seed42 / zm=2.6）：村·镇签**下移 31~62px = 1~2 档**；**灵脉签/景点签 0 档不动**（另一分支）。

### 2. 云：两层掷骰 + 提高密度（`cloudOf` / `CLOUD_CELL 430→350`）
① 族群块（`CLOUD_CLUSTER=1.5` 格）掷一次定族型：晴空 26% / 孤单 / 小批 / 成批；
② 块内**逐格**按族型概率播种；③ **每格朵数** = 孤单 1 / 小批 2~3 / **成批 5~8**（族型同时定大小档）。

### 3. ⚠ 三个反直觉的坑（都踩过，已写进代码注释）
1. **只播 1 朵永远连不成片** —— 格距 350 ≫ 云宽 ~95px，相邻格留 250px 空隙（实测最大连通块仅 832px）。
2. **抖动要收在 ±0.5 格；放大抖动 = 把同格的朵推开**（第一版 ×1.25 求"散开"，成批块反而更小）。
3. **族群块尺度要小**：3 → 2 → **1.5 格**。视野 @z=1 仅 ~4×2.6 播种格 ⇒ CLUSTER=3 时视野内仅 ~1 个族块
   （整屏要么全成批要么全孤单）；2.0 时期望成批 0.5 个（多半看不到）。

### 4. 验证（机读，无看图）
- **云**（`_cloud_blob.mjs`：双帧只差 `nocloud` ⇒ 非云区 Δ=0，阈值 Δ>5；8 连通 + 分块聚集度）：
  八版 覆盖 **1.33%** / 有效块 **40** / 成批1 小批13 **孤单26** / **聚集度 3.2×** ⇒ 合格；
  七版 0.26% / 5 块 / 成批0 小批4 孤单1 / 1.7× ⇒ 不合格。**覆盖 5.1×、块数 8×**。
- **名牌**（`_plaque_pos.mjs`：米黄纸签连通域 + 按 x 配对算 dy/档）：下移 1~2 档（对应 out=1~2）。
- 门禁：`node --check main.js` ✔；`sync_preview_inline --check` ✔；**离线回归 10/10 全绿**。

### 5. 隔离实例与收尾
- 建 `%TEMP%/wb/web_old8`（**只回退**八版两处：`CLOUD_CELL 430`+均匀播种 / `bldgAnchor` 用 min）⇒
  8177（仓库 web/）vs 8178（web_old8）**仅差这两个变量**。副本用**递归 copyFileSync**（沙箱禁 `fs.cpSync`）。
- 证据图归档：`fx24_cloud_cluster_AB.png`（云广角）· `fx25_plaque_anchor_AB.png`（聚落近景，旧|新）。
- 台账新增 **§N（N1~N7）**；实例 8177/8178 已 kill（8140 全程未动）；临时件 58 文件 + 43 目录清光
  （保留被跟踪的 `_scratch_diag.mjs`），TEMP 副本清光。
## 2026-09-14 九版：云「块 → 团 → 朵」 + 朱砂点选 + 灵脉「山地底座」

用户原文（三条）：「成批的云要聚集很多，像乌云一样密集；散装的云也要有集群感」/「不要自动认领本宗」
（旧标记「又黑又粗，丑」，改成**玩家点哪一格标记就落哪一格**）/「灵脉的高度如果在山地要在原来的山的基础上加上高度，避免看不见」。

**纯客户端**（`main.js` / `renderer.js` / `vein-skin.js` + 门禁 `check_vein_skin.mjs`；引擎 `mapgen.js` **零改动**，
全仓 grep「九版」只在这 4 个文件命中）⇒ 载荷未变、**不必清库**；用户重启 :8140 后生效。

### 1. 云：结构性改法 —— 遍历单位从「格」升为「块」，块内再撒「团」
- 根因：八版**逐格独立播种** ⇒ 每格都长云 ⇒ 只有「稀/密」之别，**没有「一团一团」的集群感**。
- 三级：① 族群块（`CLOUD_CLUSTER=3` 格，`CLUSTER_W=570` 世界像素）掷一次骰定档
  （晴空 h<0.18 / 孤单 <0.46 / 小批 <0.72 / 成批 ≥0.72）；② 档位定 `nm` 团数 1/2/3、`per` 每团 4/13/24 朵、
  `spr` 0.105/0.150/0.180、`sc` 大小、`a` 不透明度；③ 团心 = 块中心 ±0.30 块，朵按**圆盘均匀** `rr=rad*sqrt(v)` 撒（中心密、外缘疏）。
- 每团朵数再抖 ±40%（成批 **12~28** / 小批 6~14 / 孤单 2~4）；`CLOUD_CELL 350→190`。
- ⚠ 密集感靠**团内多朵互叠**，不是单朵变大变实 —— 七版用户嫌「太不透明」，`a` 仍压着。
- 判据（`_cloud_blob.mjs` 配方：两帧只差 `?nocloud=1`；8 连通 + 分块聚集度）**6/6**：
  覆盖 **4.6×**、96px 窗峰值密度 **3.5×**、聚集对比 **25×**、最大云幕 **8373px**。

### 2. 点选标记（删掉「自动认领本宗」）
- `sect.auto` 整套删除（择宗菜单「随行 · 就近择宗」同去）；新增 `var selMark=null`。
- `selectTile(t)` 落 `{q,r,x,y}`（**世界**坐标）；命中宗门则顺带 `pinId/pinEnt` + 重排面板 ⇒ 点地图与菜单点名**共用一套标记**。
- 画在**覆盖层**（`setTransform(dpr,0,0,dpr,0,0)` ⇒ CSS px，**不随 zoom 变换**）：朱砂 `rgba(166,58,44)`
  实圈 + 外虚圈 + 四角斜标；旧的「黑色加粗六边框」已删。调试口 `__probe().sel / .sectPin`。
- 判据 **7/7**：质心距点击点 **0.3px** / 随世界平移 **59px** / 全图近黑粗描边像素 **0**。

### 3. 灵脉「山地底座」+ u16 复合通道（本轮唯一技巧点）
- 灵脉格海拔被引擎 §八抬到 `LIFT_CORE=[0.80,0.75,0.70]`，但格底山精灵已被峰顶替 + 七版峰缩到 40% ⇒ 群山里看不见。
- 新增 `SHAPE.terrainBase:1.0`；`PROP_VS` 用**大世界山同档公式**算底座（>0.84 雪峰 / >0.70 山地 / 否则 0
  ⇒ 平原·水面格自动为 0），`H = uR*(3.3+1.2*hrand)*hs*ss + vbaseH`、`W = …*ss*ws + vbaseW` ⇒ **底座不乘 sizeScale**。
- ⚠ 海拔通道是 **u16（值域 [0,1]）**，装不下「等级 0~2」+「海拔 0~1」⇒ `iElev=(等级+海拔)/3`，
  shader 里 `vz=clamp(iElev,0,1)*3`、`等级=floor(vz)`/`海拔=frac(vz)`，**max=(2+1)/3=1.0 恰好不溢出**；
  大世界山照旧直传海拔 ⇒ **不动 protobuf 契约、不加通道**。名牌锚点 `veinTopU()` 同步抬高。
- 门禁新增 **§C 山地底座契约** ⇒ `check_vein_skin` **32/32**；判据 **12 断言**：峰尖 **+29px** / 赭金体量 **×1.4** / 差异**局部化单格**。

### 4. 收尾
- 证据图 `待办事项/img/`：`fx26_cloud_cluster_AB.png`（云 全幅）· `fx27_vein_mountain_base_AB.png`（灵脉 特写 ×3）·
  `fx28_click_select_ABC.png`（点选 三联）。⚠ 该目录**不入库、且本机（接手机）不存在**（只留在生成机）⇒ 跨机只能靠数字复现。
- 台账新增 **§O（O1~O6）**；交接单 `待办事项/交接-九版收尾未完成事项.md`（T1 清理 / T2 停实例 / T3 §O / T4 记忆 / T5 skill / T6 commit）。
- **回归复跑（接手机 i7-6700HQ / 15.8G / Win10 1903）**：**9/10 绿** —— sync --check / vein_marker / terrain /
  edge_falloff / preview_draw(32:0) / settle_road / check_vein_skin(32/32) / w5 / w6(23:0) 全 ✔；
  **w3_bfs_road 仅 ⑦ 两条墙钟阈值超限**（第 3 慢 region **550ms**>400、最慢 **936ms**>500；功能类 8 项 + 建路率 95.6% 全过）
  ⇒ **机器绝对速度门槛，不是回归**；要跨机就应改成「先跑固定 CPU 基准再按倍率缩放阈值」的相对校准。
- ⚠ 两条通用教训：① 量已渲染目标**别硬编码期望屏幕坐标**（首版偏 >60px 全废），从**新旧差分自证**（连通域质心）或读 `__probe()` 事实；
  ② **CDP `Runtime.evaluate` / `Page.captureScreenshot` 对本页会永久挂起**（连 `cdp_feat.mjs` 也挂 >100s）
  ⇒ 改用**探针页（注入点击/平移）+ `live_cap.mjs` 像素差分**通道。

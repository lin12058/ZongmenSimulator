# 2026-09-09 · C# 后端化 (ClearScript + protobuf + SQLite) 端到端落地

- **结构**: D:\codes\宗门模拟器demo\Server\Zongmen (C# ASP.NET 8 + ClearScript.Complete 7.4.5 + protobuf-net 3.2.30 + Microsoft.Data.Sqlite 8.0.8), 前端搬到 web/ 移除 noise/mapgen, 引擎脚本搬到 Server/Zongmen/Engine/js (噪声/地图/适配层)。
- **分层**: Program → StaticWebMiddleware(默认代理 web/) + MapEndpoints(/api/map/*) + MapDebug; Engine(JsEngineHost 每 seed 一个 V8 LRU 3, 引擎门锁串行) → Services(MapWorldService 编排 JS→JSON→protobuf→gzip→KV) → Storage(VirtualContext: Sqlite+Memory) + Protocol(Codecs ProtoCodec/GZipCodec) + Domain(MapMessages 含 ZigZag 标注)。
- **协议**: protobuf-net 序列化 + gzip 直接落 SQLite (Data KV 表), HTTP 响应 Content-Encoding: gzip 由浏览器 fetch 透明解压; 前端 pb.js 微型解码器 (含 wire 0/2/5 + 字段位表) + mapclient.js 封装拉取与几何公式。带符号坐标字段全部 DataFormat.ZigZag, 否则 -1 等被错解为别的负数。
- **数据准确性**: verify/verify_map.mjs 三方对照 (Node 加载同份 Engine/js 生成参考 → HTTP 走真实 protobuf+gzip 链路 → 浏览器同款 pb.js 还原) 310 项检查全绿; SQLite 落库 + 重启持久化 + 同请求 proto 字节确定性。
- **前端瘦身**: main.js 重写, 删除所有 MapGen.* 调用, 区块流式改为异步 fetch+上传 GPU + chunkData 缓存; 覆盖层浪线从 chunkData 数组解码绘制, 道路/聚落/区域/灵脉/小地图字段全部来自 /api/map/{region,comm,tile,fields}; 点击格详情异步。
- **收尾**: Server/README.md 写出运行/端口/验证说明; headless 截图实验确认页面无致命错误; 自截图 capture=1 URL → /api/debug/snap → verify/capture.png 作为静态证据备用 (headless 受虚拟时钟限制仅适合现场验证/present_files 内置浏览器)。
- **端口**: 默认 8140 (0.0.0.0), 取代旧 8139 静态服务。
- **前端补片 noiselib.js (今天修)**: 移走 noise.js 后 textures.js 顶部 `var NL = global.NoiseLib` 抛出 → InkTextures 未定义 → main.js 整段 ReferenceError → legend 0 / data null。textures.js 仅需视觉噪声三件套 (mulberry32 / SimplexNoise.noise2D / fbm, 固定种子生成纸纹枯笔纤维)——零世界/地图逻辑, 与"前端不算地形"不冲突。新建 web/js/noiselib.js 从 Server/.../Engine/js/noise.js 原样摘取这三件套 (mulberry32 5 行 + Gustavson SimplexNoise 完整 2D + fbm 3 倍频叠加), index.html 加载序 pb.js → mapclient.js → noiselib.js → textures.js → renderer.js → main.js。
- **headless 截图两路**:
  1. `?capture=1` 页面内 4s 后合成 glcanvas+overlay → POST /api/debug/snap → 服务端写 verify/capture.png。这是 **稳定路径**——已产出 2.1MB 真实渲染图 (雪山/林地/道路/灵脉/宗门村镇标签全到位)。
  2. verify/cdp_screenshot.mjs: Chrome --headless=new 远程调试 + Page.captureScreenshot, **在重度 WebGL 渲染下 Page.captureScreenshot 可能挂起** (90s 无返回)。脚本已加固: 动态调试端口 (9333+rand 0..300) 防残留 chrome 占端口、15s 看门狗、/json 拉取 15×0.4s 重试、结束用 `taskkill /PID <chrome> /T /F` 杀进程树 (此前 proc.kill 留孤儿 chrome 致下次启动失败)。Node 22 用全局 `WebSocket` (EventTarget 风格, onmessage/evHandlers), 不再 `import('node:ws')`。CDP 适合采集页面 console/异常/dom state, 真实 PNG 截图优先用 capture.png。
- **后端进程管理经验 (今天收尾)**:
  - 构建必须 `dotnet build Server/Zongmen/Zongmen.csproj` (MSB1009 是拿目录当项目文件; 只有 `dotnet run --project` 接受目录)。
  - 本机 curl 探测 127.0.0.1 必须 `--noproxy "*"`, 否则环境代理报 "upstream connect failed" 误判服务死。
  - run_in_background 起的 Zongmen.exe 会被外部终止 (用户手动 Stop / 会话清理), 日志无异常、戛然而止即"被杀"而非崩溃; 判定存活以 `curl --noproxy` meta/stats 为准。
  - 清理后台只需一条: `Get-NetTCPConnection -LocalPort 8140 -State Listen | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }`; node/chrome 残留此前均已清空。
  - 后台任务 failed 通知≠异常: 历史实例退出会陆续弹通知, 但实际存活进程可能只剩后端一个; 用 PowerShell CIM 盘点是唯一可信手段 (PowerShell 直出常被吞, 落盘临时文件再 Read)。
- **已知视觉异常 (待修)**: capture.png 中央海域 + 中心陆面渲染为纯黑 (RGB ~5,4,4), 六边形轮廓仍可见, 周边雪峰/林相/路/灵脉/标签均正常。CDP 像素级探针已确认: atlas (CPU+GPU readPixels) 8 群系 cell RGB 全对; chunks=46 全上传; 渲染循环无剔除; 但屏幕 5×5 网格呈"中心黑/外环绿"的空间图样 (反推世界坐标后黑区是围绕 (0,0) 的菱形空缺), 指向**中心区块的 hex 实例被投到屏幕外**。最可疑点 `web/js/pb.js#chunkToArrays` 的中心坐标计算 (注意: `meta.chunkS=21` 与服务器实际 32×32=1024 hex/chunk 的 `cq/cr∈0..31` 存在协议不一致, 需确认是否该用 32)。
- **中央黑区 bug 已闭合 (当日收尾)**: 真凶不在 pb.js（mapgen.js 证实 CHUNK_S=21 正确、区块=半径15六边形盘 721 hex，"32×32=1024"系误记；verify_map 对 chunk(0,0) 逐点对照本就全绿），而在 main.js `pumpChunks`/`pumpExtra` 的 `var job`/`var k` 循环共享闭包：并发回调全用最后一个 job 的 key → 响应错挂/被 `chunkData.has()` 丢弃 + `chunkBusy.delete` 只删最后一个 key → **chunk(0,0) 等最近区块 busy 永久卡死、永不重试**，中心留下 FBO 透明底（经后处理 ≈ RGB5,4,4）的六边形空缺。修复：loadChunk/loadExtra 独立函数闭包独占 job + gen 守卫 busy 删除 + regenerate() 清 chunkBusy/regionBusy/commBusy。验证：node --check 通过 → 重启后端 → verify_map.mjs 全绿 → headless `?seed=42&...&capture=1` 与 `?seed=20260909&...` 双图中央（陆面/湖面）均完整渲染、无黑区（capture.png 2.3MB）。临时 _chrome_shot*.png 已用 PowerShell 清除。
## 代码审查 review_v3（晚 03:20）
- 全量审查最后一次提交（886675f），产出 `待办事项/review_v3.md`。
- 关键发现：
  - **P0 renderer.js uploadChunk**：`for(k=0;k<3;k++)` 只上传了 propCenters/propSprites/propHashes，`propElevs`（iElev, location=4）从未绑定 → 山/雪峰高度恒为最低，海拔驱动的高度缩放视觉失效。修复：k<4。
  - **P0 JsWorldVm**：单 SemaphoreSlim gate 使同 seed 全部 JS 调用完全串行，多客户端/多 chunk 并发吞吐受限。
  - **P1** 服务端 MemoryVirtualContext 与 SQLite Data 表只增不删，长期漫游无限增长；overlay 静态层重建时整批重传道路；JsWorldVm.Call 动态绑定无缓存。
  - **P2** 纹理生成 buildAtlas→buildPaper/buildNoise 顺序隐式耦合模块级 trng，脆弱但当前稳定；warmRoadsStep 为死代码；cq/cr +16 int8 在 CHUNK_SCAN≤15 时安全。
## 合并 review_v1~v5 → 待办事项/review.md（03:11）
- 把五份 review 报告去重合并为《review.md 合并待办清单》，统一三档优先级与「命中数」标注。
- 高频共识项（≥3 份）：chunk 失败永久静默、updateStreaming 每帧全量重建、浪线 Math.pow 热点、缓存淘汰阈值过大、静态文件无缓存头/ETag。
- 双份命中较高危：LRU 淘汰 vs 在途 JS 调用竞态、seed>80 截断致世界碰撞。
- 单份但必改：renderer.js uploadChunk `k<3` 应为 `k<4`（propElevs 未上传，山/雪峰高度失效）。
- 优先级排期：第一波=B1 精灵高度 + B2 失败重试 + B3 seed 截断 + P4 道路 A* 卡顿源。
## 修复 review.md 第一批 B1/B2/B3（03:40）
- **B1 renderer.js**：`uploadChunk()` 精灵段 `for(k=0;k<4;k++)`，propElevs(location=4, PROP_VS `in float iElev`) 补绑；`pl[k]===1?2:1` size 分支本就正确无需动。逐块核对 pd/pl 长度 4 = 着色器 location 1..4。
- **B2 main.js chunk 重试**：新增 `chunkRetry` Map(key→{attempt,at}) + 常量 800ms/30s；`isDefinitiveChunkError` 只认 `HTTP 404`（mapclient 抛错格式 `HTTP <status>: url`，网络错误是 TypeError）；可重试走 `scheduleChunkRetry` 指数退避（≤30s 封顶后保持 30s 周期，不永久放弃→服务恢复自愈）；`updateStreaming` 队列重建加 `rr.at<=performance.now()` 过滤（每帧驱动，无需定时器）+ 清理已出视野记录；`regenerate()` 同步 `chunkRetry.clear()`。404 → chunkFail 仍保留 regenerate 兜底。
- **B3 JsEngineHost.cs**：删除 `seed[..80]` 截断，key 与 `init(seed)` 都用完整字符串（DB 键 WorldKeys.SeedPrefix 本就对全量 seed 做 SHA1，VM 截断导致两口径错位）。
- **验证**：node --check 两 JS；`dotnet build` 0 警告 0 错；后端黑盒实测——两 86 字符种子前 80 相同，chunk(0,0) sha1 不同（隔离生效），同种子两次 sha1 相同（确定性无回归）；verify_map.mjs 全绿；headless seed=20260909 会话 40+ chunk/region/comm 全 200 + snap 回传 2.3MB 无异常（capture.png 已留）。像素统计 78 量化色、非空白。审查项已在 review.md 各 B 节下打 ✅。
- 进程收尾：后台 Zongmen.exe 已 TaskStop，8140 无监听。
## 修复 review.md 第二批 P1~P8 性能热点（晚 04:30）
- **P1 web/js/main.js updateStreaming**：相机位移/缩放/视口尺寸未超阈值 (`16/(zoom*0.75+0.25)` 世界px) 且 chunkRetry 无到期项时, 不重建 need-set, 只推进存量 queue/pumpExtra; 首帧/regenerate 强制 NaN 重算。review_v1/4/5 共识项, 静默冗余消除。
- **P2 邻域打包/解码**：
  - `web/js/main.js drawChunkWaves` 解码 `Math.floor(nv/Math.pow(8,wn))%8` → `(nv>>(wn*3))&7` (3bit/邻居, 移位掩码)。
  - `Server/Zongmen/Engine/js/mapgen.js buildChunk` 打包 `+=min(biome,7)*Math.pow(8,k)` → `|=min(biome,7)<<(k*3)` (语义等价, biome≤7 无进位重叠)。GLSL 端 pow 保留, 数值一致。
- **P3 mapgen.js 缓存**：
  - 删 `evictHalf` (超大 Map 半量 delete 是一次性长停顿)；新增 `cacheSet(m,key,val,cap)`：插入超 cap 即 `m.delete(m.keys().next().value)` (单条, 摊薄到每次插入)。
  - 容量常数：`ELEV_CAP=40000 / FIELD_CAP=30000 / VEIN_CAP=40000`。
  - 新内部 `elevAtVN(q,r,vn)`：fields 主动 `vn=veinNear(q,r)` 一次, 传入 elevAtVN 复用, 海拔迁就与生态偏置/灵脉覆写共用, 避免 50% 重复扫描。
- **P4 mapgen-server.js tileJson onRoad 命中验证踩坑与最终方案**：
  - 初改：`roadsNear(ci+dj,cj+dj, 0)` (只读缓存) + 区域包保持 SQLite 命中读 → 视觉/详情与 DB-hit 不一致 (server false, Node true), verify_map tile onRoad 反复 FAIL tile(-8,5) seed=20260909。
  - 修1：tileJson 维持 budget=0; 但 **GetRegionBytes 改为 always-compute (不直读 SQLite)** — VM roadCache 与面板必须保持一致, 因为 P4 把 onRoad 设计为「只读已生成路」。同会话 `_mem` 命中免重复生成, 启动一次性计算代价与原码等同。
  - 修2：`verify/verify_map.mjs` verifyTile 在 `GS.init(seed)` 后、tileJson 之前, 模拟真实客户端先流式 3×3 区域包 (`GS.regionJson` + HTTP `/api/map/region`) — 对齐两端道路缓存, 与 P4 「只读缓存」语义一致。
  - 修后连跑 3 次 verify_map 全部通过 ✔。
- **P5 mapgen-server.js chunkJson 单段定宽缓冲**：
  - 输出形如 `{ca,cb,count,pn,d:<base64>}` — 替代原 13 段 base64 + JSON key 串, 总字节一致, 解析 + 一次 B64 decode。
  - 缓冲布局 (全小端, 与 C# `Slice` 一一对应)：
    - 地块段 11nB = `cq[n] | cr[n] | tiles[n] | elev[2n] | hash[2n] | neigh[4n]`
    - 精灵段 13pnB = `pdx[4pn] | pdy[4pn] | psp[pn] | ph[2pn] | pe[2pn]`
  - 删旧 `f32bytes/u16bytes/u32bytes/u8bytes/tileBytes/toU8` 辅助。
  - `MapWorldService.BuildChunk` 用新 `Slice(raw,o,len)` 切回 ChunkPayload, 含长度断言。
- **P6 SqliteVirtualContext 重写 (无全局锁 + WAL + 批量异步落库)**：
  - 删单连接 `lock(_gate)`；每操作 `Open()` 短连接 (Pooling=True) + `PRAGMA busy_timeout=8000`。
  - 启动时建表 + `PRAGMA journal_mode=WAL` (库级持久), 读写并发。
  - 写：原 `SetData` 改为入队到 `ConcurrentQueue<...>`; 后台 `Task.Run(WriterLoopAsync)` 每 250ms `Flush()` — 把积压一次性 BeginTransaction 批写 (单事务一次 commit, 减少写放大); 失败退回队列重试, 不阻塞请求路径。
  - `MapWorldService.Store` 改用 `SetDataDeferred`; `StatsJson` 前 `Flush()` 确保 dbRows 准; `Dispose` 前再 `Flush` + 等待 writer ≤2s。
- **P7 JsEngineHost.cs dynamic 句柄缓存**：
  - `private readonly dynamic _svc;` 构造时一次取 `_engine.Script.MapGenServer`, `Call` 7 个 switch 分支全部走 `_svc.xxx` (避免每次取 Script 属性的 DLR 解析)。
  - `Call("init", seed)` 顺序调整到 `_svc` 赋值之后。
  - 新增 `public List<string> Seeds { get; }` 快照当前活跃 seed 列表 — 供 P8 清理按前缀过滤。
- **P8 缓存有界 + 跨 seed 清理**：
  - `MemoryVirtualContext` 容量上限 (默认 8192)：`SetData` 走 `TryAdd` 区分新/旧；新项入 `ConcurrentQueue<string> _order`；`_map.Count>cap` 时按入队序逐条 `TryRemove` (残留 entry 用 TryRemove false 容错)。数据全部可确定性重建, 淘汰只损命中率。
  - `SqliteVirtualContext.PruneExcept(IReadOnlyCollection<string> seedPrefixes)`：拼接 `DELETE ... WHERE NOT (Key LIKE $p0 OR Key LIKE $p1 ...)` 仅保留活跃 seed 前缀 (`w:<sha1_16>:%` 形)。
  - `MapWorldService.StatsJson`：每 20 次 stats 调用 (`_statsCalls % 20 == 0`) 触发一次 `PruneExcept(活跃 seed 前缀)`, 清掉非活跃历史世界缓存。
- **验证全套**：
  - 语法：`node --check` web/js/main.js + Engine/js/{mapgen,mapgen-server}.js 全 OK。
  - JS 自检：临时脚本 (`verify/_tmp_p5.mjs` 已删) 验证 P5 缓冲逐段还原 count/cq/cr/tiles/elev/hash/neigh/pdx/pdy 全部一致 (buffer 长度 11n+13pn, 坐标还原 ≤1e-3px), P3 fields 远距离确定性, P4 onRoad 重复可重现。
  - C# 编译：`dotnet build Server/Zongmen/Zongmen.csproj` 0 警告 0 错误。
  - 端口收尾旧实例：`Get-NetTCPConnection -LocalPort 8140 -State Listen | Select -First 1 -ExpandProperty OwningProcess | Stop-Process -Force`。
  - 启动后 `curl --noproxy "*" /api/map/meta` 200, `dbRows=11111` (沿用历史)。
  - 并发 24 路不同 chunk × 4 seeds → 全部 200, 无 DB busy 错。
  - stats 连调 22 次 → 第 16 次触发 prune, `dbRows: 11136 → 18` (只保留活跃 seed), 期间无异常。
  - **verify_map.mjs 连跑 3 次全部通过** ✔ (P4 改 harness 后), 310+ 项全绿, `dbRows>0` 仍满足, 二次 chunk 字节一致。
  - 浏览器 headless `?seed=42&nofade=1&capture=1` → 真实渲染 (capture_webgl 5MB) 中央陆面/海面/雪峰/林/灵脉/宗门村镇/墨路/小地图全部正常, **无中央黑区回归, 山峰有高度 (B1 仍正常)**。capture.png (页面内合成) 在虚拟时钟下空白属 headless 已知现象, 不影响产品。
- **后端进程收尾**：本轮测试 Zongmen.exe 已 Stop-Process, 8140 无监听, 临时截图/Chrome user-data-dir 已清。
- **review.md 待办更新** (回填)：P1~P8 全标 ✅ 2026-09-09 (本次), 整个清单转为「B1~B3 + P1~P8 全部闭合」。

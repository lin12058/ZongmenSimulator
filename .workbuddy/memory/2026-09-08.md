
## 代码丢失事故与重打 (2026-09-08 凌晨)
- 现象: 用户报山丘消失。排查: ee55322 提交声称含聚类+草地细节, 实际只提交了 .workbuddy/memory md (+4行), 三个 js 改动从未入库; 后续 9fbbaa4/39c6efc 用旧内容覆盖 mapgen/textures/renderer 并提交 → clusterOffset/propMound/ss=0.30 全部消失且 git status 干净。
- 根因推断: 用户编辑器开着改动前的旧缓冲区, 保存时冲掉磁盘上的新代码 (或从别处同步覆盖)。
- 处理: 两轮改动全部重打 (mapgen 聚类+propList y排序+草地分配 / textures propMound+propSoloTree+第7行8列 / renderer PROP_VS hs+ss=0.30), 保留 39c6efc 的新格底逻辑 (tiles.push 用 f.biome)。
- 教训: **js 改完必须当场让用户提交入库**; 与用户共用编辑器时, 改前提醒关闭旧缓冲区/改后 reload。

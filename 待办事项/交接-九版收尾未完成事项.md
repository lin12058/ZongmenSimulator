# 交接 · 未完成收尾事项（九版：云集群 / 点选标记 / 灵脉山地底座）

> 生成时间：2026-09-15 00:15。三项功能改动**已完成并通过全部机器验证**，本单只含**收尾**工作。
> 接手前必读：仓库根 `D:\codes\宗门模拟器demo`；⚠ 删文件高危规则见 `.workbuddy/memory/MEMORY.md`（禁 shell 通配删除，一律 Node `fs.unlinkSync` 绝对路径 + basename/数量断言，先按 `git ls-files -c core.quotepath=false` 过滤被跟踪名单）。
> ⚠ **机器说明**：本单生成于**开发机 A**（改动所在的工作区）。用户已确认**接手方在另一台机器 B 上干活**——所以：① 三项改动**尚未 git commit**，只存在于 A 机的未提交工作区，B 机需先同步代码（用户 push/拷贝工作区），B 机上不能沿用"10/10 绿"的结论，要重跑验证；② 文中 `%TEMP%\wb\...`、`D:\codes\...` 均为 A 机本机路径，B 机按自身环境对应；③ 8177/8178 隔离实例已由用户停掉（T2 完成），B 机如需复验请按 `zongmen-regression` skill 自建隔离实例，**8140 是用户常驻实例，任何机器上都不要动**。

## 〇、已完成（背景，勿重做）

| # | 需求 | 实现 | 验证结论 |
|---|------|------|---------|
| ① | 云更密集、集群感（乌云状），散装云也要成团 | `web/js/main.js` `cloudOf()` 九版-2：族块掷骰(晴空/孤单/小批/成批) → 块内 1~3 个**云团中心** → 每团 3~20 朵抱团；孤单 3 朵抱团 | 探针 6/6：覆盖 4.6×旧、96px 窗峰值密度 3.5×、聚集对比 25×、最大云幕 8373px |
| ② | 移除本宗自动选择；红色改玩家点选效果 | `web/js/main.js`：`sect.auto` 删除 → `selMark`；朱砂双圈+四角斜标画在 `drawOverlay()` CSS-px 覆盖层（不随 zoom 变换）；`selectTile()` 点格选中宗门 | 探针 7/7：标记质心距点击点 0.3px、随世界平移 59px、全图 0 个近黑粗描边像素 |
| ③ | 灵脉在山地上要叠加原山高度 | `vein-skin.js` `SHAPE.terrainBase:1.0`；`renderer.js` PROP_VS 复合通道 `iElev=(等级+海拔)/3`（floor=等级/frac=海拔）→ 底座山+峰体；`main.js` `refreshChunkProps` 写复合值 | 探针 12 断言全过：峰尖抬高 +29px、赭金体量 ×1.4、差异局部化单格 |

- `check_vein_skin.mjs` **32/32 PASS**（含新增 §C 山地底座契约）。
- 离线回归 **10/10 绿**（`%TEMP%/wb/offline9.log`，退出码 0）。
- 证据图已归档：`待办事项/img/fx26_cloud_cluster_AB.png`（云 全幅 旧|新）、`fx27_vein_mountain_base_AB.png`（灵脉 特写 ×3）、`fx28_click_select_ABC.png`（点选 三联）。
- 语法检查 ✔、`sync_preview_inline.mjs --check` ✔；`Server/Zongmen/verify/_vmsrv/ZongMen.dll` 已重建。

## 一、待办清单（按优先级）

### T1. 清理临时文件（低风险，先做）
工作副本内（共 14 件 + 1 个 web/ 探针，**保留 `verify/_scratch_diag.mjs`——它是被跟踪的**）：
- `verify/_c9_new_on.png` `_c9_new_off.png` `_c9_old_on.png` `_c9_old_off.png` `_v9_new.png` `_v9_old.png`
- `verify/_sel9_base.png` `_sel9_clk.png` `_sel9_pan.png`
- `verify/_fx9_probe.mjs` `_map9.mjs` `_vprof.mjs` `_sel9_probe.mjs` `_mksel9.mjs` `_sel9_an.mjs` `_ab9.mjs` `_mkweb_old9.mjs`
- `web/_sel9probe.js`

`%TEMP%/wb/` 下（上轮只清了 web_old8，本轮又新增）：`offline9.mjs` `offline9.log` `reg8177b.sqlite*` `reg8178d.sqlite*` `reg8177d.sqlite*` `web_old9/` `web_old/` `web_old8/` `db3c94e56ec2/` `_probe.txt`。

### T2. 停掉两个隔离实例 — ✅ 已完成（用户手动停掉）
- 8177（九版客户端，仓库 web/）、8178（八版对照副本）已由用户停掉，接手 agent **无需再处理**，也不要在 B 机上重启它们。
- B 机如需复验，按 `zongmen-regression` skill 自建新的隔离实例（`dotnet build -o verify/_vmsrv` + `Zongmen__Port`/`Zongmen__DbPath`/`Zongmen__WebDir` 环境变量），端口避开 8140。

### T3. 台账补 §O（文档）
- 文件：`待办事项/前端表现升级-匾额山体云气.md`（已有 §A~§N，**尚无 §O**）。
- 按 §N 的体例补「§O 九版」：N1~N7 之后接 O1~O4，写三项改动（云三团结构参数表 / selMark 点选通道 / terrainBase+复合 iElev 通道）、验证数字（上面的探针结论）、证据图 fx26/27/28、踩坑（探针硬编码屏幕坐标必错 → 改从差分自证；u16 通道装两个值用 `(等级+海拔)/3` 打包 floor/frac 还原）。

### T4. 记忆文件更新（文档）
- `D:\codes\宗门模拟器demo\.workbuddy\memory\2026-09-14.md`：追加「九版」一节（git 显示已 M 但内容可能只到八版，核对后补齐）。
- `.workbuddy/memory/MEMORY.md`：灵脉段落更新三点 —— `SHAPE.terrainBase`（山地底座旋钮）、iElev 复合通道 `(等级+海拔)/3`（shader floor/frac 还原，max=1.0 恰好不溢出）、云 `cloudOf` 三团结构（族块→团→朵）。

### T5.（可选）skill 增补
- `webgl-headless-verify`：新增经验——「CDP Runtime.evaluate / captureScreenshot 对本页会永久挂起（本轮复现，连 `cdp_feat.mjs` 也挂），可靠替代 = 探针页（`web/_sel9probe.js` 注入点击/平移驱动，已被删，思路在 T1 前可从 git 未提交 diff 找回）+ `live_cap.mjs` 像素差分通道」；以及「量已渲染目标不要硬编码期望屏幕坐标，从新旧差分自证位置」。
- `zongmen-regression`：可加「u16 通道复合打包 (等级+海拔)/3」与「CRLF 文件做反向 diff 脚本要先 normalize 成 LF」两条。

### T6.（可选，需用户确认后才做）
- git commit（改动已全部落在 **A 机工作区**，未提交；涉及 `web/js/main.js` `renderer.js` `vein-skin.js` `web/index.html`、`Server/.../mapgen.js`、`verify/check_vein_skin.mjs` 等）。**B 机要拿到这份代码，必须先走 commit+push 或整目录拷贝。**
- 用户自己的 ：8140 实例**未重启**——重启后才会吃到九版客户端（改的是前端 js，无需清 db）。

## 二、给接手 agent 的关键上下文

- **跨机器须知（A=开发机 / B=接手机）**：改动只在 A 机未提交工作区；B 机先同步代码再动手。T1 清理清单里的文件只存在于 A 机（B 机上没有这些临时件，清单可当"A 机现状备忘"用）；`%TEMP%/wb/offline9.log`（10/10 回归日志）也在 A 机，B 机要引用结论需重跑。T4 的 `.workbuddy/memory/` 与 T3 的台账随仓库走，B 机同步后可直接改。
- 验证判据：全断言通过 + 退出码 0；回归跑法见 skill `zongmen-regression`；探针 `/verify/_fx9_probe.mjs`、`_sel9_an.mjs`（T1 删前可先读）。
- 云参数真源：`web/js/main.js` `cloudOf()`（族块骰阈值 0.24/0.50/0.78；团数 1/2/3；每团 3/10/20 朵；散射 spr 0.095/0.135/0.165）。灵脉旋钮：`web/js/vein-skin.js` `SHAPE.terrainBase`（1.0=全叠加，0=关闭）。
- 改引擎/前端 js 后固定三步：① `sync_preview_inline.mjs --check` ② 重启隔离服务端 ③ 载荷变则停服清 `db/zongmen.sqlite*`。
## 三、进度回执（接手机 B · 2026-09-15）

> 本机 = **接手机 B**：`D:\codes\ZongmenSimulator`，Intel i7-6700HQ（2.6GHz / 4C8T）· 15.8G · Win10 1903（build 18362）。
> A 机的未提交工作区已 `commit + push`（`7f8486e`），本机 `pull` 后**三项改动代码已在 HEAD**（`git diff` 干净）；
> 本机无 8140 / 8177 / 8178 监听，全程**未启动任何服务实例**。

| # | 状态 | 说明 |
|---|------|------|
| T1 | ✅ 完成 | 清单里那 18 件在本机**全部不存在**（与交接单「只存在于 A 机」一致）。本机另有**上轮残留调试件 9 件**，已按高危规则清掉：`verify/_expA.log` `_expB.log` `_vmap.log` `_run_w1_client_revs.log` `_run_w2_concurrency.log` `_ordtest.mjs` `server_run.log` `_ord_head/` `_ord_pre/`（Node `fs.unlinkSync`/`rmSync` 绝对路径 + basename 白名单断言；`_scratch_diag.mjs` 按被跟踪名单**保留**）。`%TEMP%/wb/` 在本机是 WorkBuddy 自己的目录（只有 `sidecar.pid`/`sessions`），无交接单所列文件。 |
| T2 | ✅ 无需处理 | 未启动 / 未触碰任何实例（8177、8178、8140 本机均无监听）。 |
| T3 | ✅ 完成 | `待办事项/前端表现升级-匾额山体云气.md` 新增 **§O（O1~O6）**：469 → 548 行。含①云「块→团→朵」参数表（晴空/孤单/小批/成批 的 nm/per/spr/sc/a）②`selMark` 点选通道（覆盖层 CSS px、朱砂双圈+四角斜标）③`terrainBase` + 复合 `iElev` 通道④三项验证数字⑤证据图⑥两条踩坑。 |
| T4 | ✅ 完成 | `.workbuddy/memory/2026-09-14.md` 追加「九版」一节（680 → 726 行，含 4 小节 + 收尾）；`.workbuddy/memory/MEMORY.md` 改 6 处：灵脉段标题改「九版」、`check_vein_skin` 30 → **32 项**、**新增「山地底座 / 复合 iElev」条**、云段补九版结构、基线行改 32/32 并加 w3 ⑦ 跨机提示、CDP 与「别硬编码期望屏幕坐标」两条坑。⚠ 追加时**已按 CRLF 口径写回**（裸 LF 残留 = 0）。 |
| T5 | ✅ 完成（落到本机存在的 skill） | ⚠ **交接单点名的 `zongmen-regression` / `webgl-headless-verify` 在本机不存在**（`~/.workbuddy/skills/` 为空 ⇒ 应为 A 机的用户级 skill）。本机**入库**的对应 skill 是 `.workbuddy/skills/zongmen-verify-pipeline/SKILL.md`，四条经验已写入：§3 加「w3 ⑦ 是机器绝对速度门槛，跨机必红」；§5 加「CDP 取数通道永久挂起的替代方案（探针页 + live_cap 差分）」与「别把期望屏幕坐标硬编码进探针」；**新增 §12**「u16 通道复合打包 `(等级+海拔)/3`」+「CRLF 文件定点替换脚本先 normalize 成 LF 并逐条断言恰好命中 1 次」。**A 机如需同名 skill，把 §12 与这两条搬过去即可。** |
| T6 | ✅ 完成（已提交+推送） | 九版收尾文档 + **十版代码**（云尺寸世界锁定 `CLOUD_W0` / 灵脉底座只加高不加宽 `terrainBaseW`）**一次性提交并推送**：commit `db33466`（14 文件）。同 commit 含：`待办事项/前端表现升级-匾额山体云气.md`（§O）、`待办事项/交接-九版收尾未完成事项.md`（本回执）、`.workbuddy/memory/2026-09-14.md`、`.workbuddy/memory/MEMORY.md`、`.workbuddy/skills/zongmen-verify-pipeline/SKILL.md`。已提交推送。另：用户常驻的 `:8140` 本机未在跑 ⇒ 用户重启自己的实例后才吃到九版客户端（纯前端改动，**不必清库**）。 |

### ⚠ 本机重跑验证的真实结论

- **9/10 绿**：`sync_preview_inline --check` ✔ · `check_preview_vein_marker` ✔ · `check_preview_terrain` ✔ · `check_edge_falloff` ✔ ·
  `check_preview_draw` **32:0** ✔ · `check_preview_settle_road` ✔ · `check_vein_skin` **32/32** ✔ · `w5_sprite_range` ✔ · `w6_bldg_face` **23:0** ✔。
- ❌ **`w3_bfs_road` 2 项失败 —— 是「机器绝对速度门槛」，不是回归**：功能类断言（①②③④⑤⑥ + 建路率 **95.6%** ≥ 70%）**全过**，
  只有 ⑦ 的两条**墙钟阈值**超限：第 3 慢单 region **550.1ms / 658.1ms**（限 400ms）、最慢 **935.9ms / 962.3ms**（限 500ms）。
  两次独立运行数字同量级、建路率逐字相同 ⇒ **确定性机器速度差异**（该脚本注释也承认「旧默认 250 出自更快机器」）。
  ⇒ 建议改成**相对校准**（先跑一段固定 CPU 基准，再按倍率缩放阈值），或按机器维护本机基线；**别当回归处理**。
- **证据图 `待办事项/img/fx26~fx28*.png` 在本机不存在**（git 全历史无该路径）⇒ §O 只能引用数字，跨机无法看图复核。

### ℹ 顺带发现（可选跟进）

- `.gitignore` 的 `verify/` 段只覆盖 `_*.mjs` / `_*.png|log|txt|html` / `_eng_head/`，**`_*.md`、`_*.json`、`_*/`（如 `_ord_head/`）不被覆盖** ⇒ 这类临时件会一直污染 `git status`。✅ **已补** `verify/_*/` + `verify/_*.md` + `verify/_*.json`（2026-09-15 十版收尾）。

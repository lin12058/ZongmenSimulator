# 交接单 · 小地图 R11~R13 与未完成事项

> 生成 2026-09-15 21:20。覆盖范围：R11 小地图灵脉化重构 → R12 大地图采样修复 → R13 面板档倍率联动。
> 历史台账（九版/十版/十一版）已分别被各自文档与全量回归吸收；本单只列**当前仍未关**的事项。
> 验收姿势总纲见 skill `zongmen-verify-pipeline`（⚠ 没有叫 `zongmen-regression` 的 skill，旧备忘里是错名）。

---

## 0. 一句话现状

R11/R12/R13 代码全部落地且验证全绿（`frontend_smoke` **48/48**），但 **18 个文件的改动全部未提交**，
且服务端重启与清库两件事需要用户拍板。以下未完成项按优先级排列。

---

## 1. 已完成（交接背景，细节见各文档）

| 版次 | 内容 | 验证 | 细节落档 |
|------|------|------|----------|
| R11 | 小地图灵脉化重构：`web/js/minimap-vein.js` 独立模块（热拔插单点 = main.js `initMinimap()`）、引擎 js 经 WS 下发（帧 `Script=4`）、三层架构（L1 地形前端自算 / L2 世界全 WS / L3 视野）、速率根因根治（零 HTTP 轮询） | 三态实机截图 + 6/6 地形一致 | `待办事项/小地图灵脉化重构-独立模块与WS数据.md` §8 |
| R12 | 大地图采样修复：抽样格改**世界对齐** `q%m==0 && r%m==0` + 粗层引导 + 游标消费。未探测占比 46.5%→0% | 实机 miss 恒 0；截图 0%；契约 41/41 | 同上 §9 |
| R13 | 面板档倍率：写死 FOLLOW_WPP=6 改为 `panelWpp = baseWpp*DEFAULT_ZOOM/camZoom`（与小地图恒定比例联动主相机缩放）+ 面板滚轮缩放/拖动转自由/归心 + localStorage 持久化（键 `zongmen.mmView`） | 见下表 | **尚未写入文档**（见待办 U3） |

R13 实机验收数据（四组 live_cap，探针自回传，均已核）：

| 组 | 验证点 | 结果 |
|----|--------|------|
| A 面板驱动 | 滚轮放大/缩小 → wpp 6→9.44→4.78，saved=YES；拖动 → follow=false；归心 → follow=true；miss 全程 0 | 全过 |
| B/C 缩放联动 | `zm=0.7 → panelWpp=18.857`、`zm=6 → panelWpp=2.2`，乘积 panelWpp×camZoom **恒等 13.2**（=baseWpp×DEFAULT_ZOOM），覆盖主相机全档位 0.7~6 不越界 | 全过 |
| D 跨刷新 | 刷新后 wpp=9.4411 从 localStorage 还原（reload-B-restored） | 全过 |

---

## 2. 未完成任务清单（按优先级；处置方式已标注）

### U1 · 提交工作区改动（需用户放行）【动】

18 个文件未提交，全部属于 R11~R13 三轮：

```
 M Server/Zongmen/Domain/MapMessages.cs      (R11: WsFrame.Script=4 + ScriptRequest/ScriptPack)
 M Server/Zongmen/Program.cs                 (R11: 传 engineJsDir)
 M Server/Zongmen/Web/MapWsHandler.cs        (R11: 白名单下发引擎 js)
 M web/js/mapclient.js                       (R11: requestScript; 删 fieldGrid)
 M web/js/pb.js                              (R11: FRAME.SCRIPT 编解码)
 M web/js/main.js                            (R11~R13: 快照注入/删旧小地图/mmprobe/mmdrive 探针)
 M web/js/minimap-vein.js                    (R11 新文件 + R12 世界对齐 + R13 倍率联动; untracked)
 M web/index.html                            (R11+R13: 挂载点/浮层/CSS)
 M verify/frontend_smoke.mjs                 (R12+R13: 契约 41→48)
 M 待办事项/小地图灵脉化重构-独立模块与WS数据.md
 M .workbuddy/memory/* (3个) + .workbuddy/skills/zongmen-verify-pipeline/SKILL.md
?? .workbuddy/memory/DETAILS-引擎与表现层.md  (新)
?? verify/.r13_*.bin (4个)                    → 见 U5, 提交前删
```

建议拆 3 个 commit：R11 重构 / R12 采样修复 / R13 面板倍率。⚠ `minimap-vein.js` 是 untracked 新文件，**必须 `git add`**，否则漏提。

### U2 · 服务端重启确认 + 清库（需用户拍板）【不动，等确认】

- 按 09-15 日志记录：12:52 那次重启在 R11 后端改动之前 ⇒ 8140 可能仍跑旧 bundle。但 R12/R13 的实机验证
  （ScriptRequest→ScriptPack 95KB 下发成功）表明当前 8140 **疑似已是新后端**——交接后第一步先确认：
  `curl http://127.0.0.1:8140/api/map/meta` 通 + 小地图能出地形即新后端；不出则需重启。
- R11 改了 protobuf 载荷（新增帧 4）⇒ 按铁律「载荷变则停服清 db/zongmen.sqlite*」。
  用户此前口径：**测试环境会自动删库，不用手动清**——维持该口径则只重启。

### U3 · R13 归档三件套【动，收尾时 10 分钟】

1. `待办事项/小地图灵脉化重构-独立模块与WS数据.md` 追加 **§10 面板档倍率联动**（现象/改法/验收表，
   数据可直接抄本单 §1 的 R13 表）。
2. daily log `.workbuddy/memory/2026-09-15.md` 补 R13 段（现只记到 R12）。
3. `MEMORY.md` 小地图段补一句：`面板档倍率与主相机联动 panelWpp=baseWpp*DEFAULT_ZOOM/camZoom（恒定比例 13.2:1），滚轮/拖动/归心 + localStorage zongmen.mmView 持久化；?mmdrive=panel 覆盖面板档驱动。`

### U4 · §9.6 遗留：粗层椒盐感（待拍板）【不动，等拍板】

`m>=4` 时一格只取 1 个格点的群系，混合地貌区有细碎噪点。正确做法是金字塔**多数表决聚合**
（`cell(m)` = 4 个 `cell(m/2)` 取众数），需记录子格可用性，复杂度不低。当前观感可接受
（wpp=12 仍读得出湖/林/雪分布）。拍板做/不做后再动。

### U5 · 临时件清理（提交前做）【动】

以下均为验收证据或中间产物，**数据已摘录进文档/本单，可删**：

| 文件 | 说明 |
|------|------|
| `verify/.r13_panel.bin` `.r13_z07.bin` `.r13_z60.bin` `.r13_reload.bin` | R13 探针原始数据（§1 已摘录） |
| `verify/_smoke.log` | 冒烟旧日志 |
| `verify/_r11_mm_crop.png` `_r11_full_crop.png` `_r11_hidden_crop.png` | R11 裁剪证据（三态原图 live_r11_*.png 建议保留或归档） |
| `verify/_gap_far.png` `_live_fish*.png` `_ring_*.png` `_sel_*.png` | 十一版遗留证据图（若 `待办事项/img/` 已归档对应内容则删） |

⚠ 清理纪律（3 次误删前科）：**一律 Node `fs.unlinkSync` 绝对路径 + basename/数量断言 + 先按
`git ls-files -c core.quotepath=false` 过滤跟踪名单**；`verify/_scratch_diag.mjs` 是**被跟踪**的，别动。

### U6 · 长期项（可选，前文提过未做）【不动】

- 固化回归 runner 为 `verify/run_regression.mjs`（现靠临时脚本拼装全量回归）。
- `verify/` 下 `_*.png` 证据图归档进 `docs/` 或 `待办事项/img/`（`.gitignore` 的 verify 段
  不覆盖 `_*.md`/`_*.json`/`_*/`，会污染 git status）。

---

## 3. 已知 bug / 问题清单（按严重程度）

| # | 级别 | 现象 | 判定 | 处置建议 |
|---|------|------|------|----------|
| B1 | P2（非回归） | `w3_bfs_road` ⑦ 两条**墙钟**断言红：490.9ms/1039.6ms vs 阈值 400/500ms | 机器绝对速度门槛，**功能断言全过**，与 R11~R13 改动无关 | 跨机复验别当回归；若常驻本机红，可把阈值放宽或改拓扑等价判据（09-14 有处理建议待拍板） |
| B2 | P2（观感） | 粗层抽样椒盐感（见 U4） | 已知设计取舍 | 等 U4 拍板 |
| B3 | P2（流程风险） | 并行 Edit 偶发只落第一条、回执却全报成功 | 环境坑，非代码 bug | 改完必 grep 复核（铁律已在 MEMORY.md） |
| B4 | P3 | `.gitignore` verify 段不覆盖 `_*.md`/`_*.json`/`_*/` | 临时件污染 git status | 顺手补 .gitignore（可并入 U5） |

无 P0/P1。`frontend_smoke` 48/48、离线基线脚本（check_vein_skin 35、check_vein_cluster 7、
check_no_build_on_vein 5、check_settle_spacing 6、check_sea_village 11、check_vein_settle_gap 8、
check_preview_* ×4、check_edge_falloff、w5/w6）与服务端 verify_map/w1/w2/w4 上轮全绿。

---

## 4. 接手速查（坑与姿势）

- **跑法**：见 skill `zongmen-verify-pipeline`。⚠ 服务端验证别 kill 用户 8140，起临时独立实例
  （`Zongmen__Port=8141 Zongmen__DbPath=db/zongmen.verify.sqlite`）+ `verify_map <url>` 传参。
- **小地图取数**：CDP 对本页永久挂起 ⇒ 用 `?mmprobe=1`（探针时间序列自回传 `/api/debug/snap`）、
  `?mmdrive=1`（全屏档驱动）/`?mmdrive=panel`（面板档驱动）、`?mmwpp=N`（全屏初始缩放）、`?mm=full|hide`。
- **改引擎 js** ⇒ ① sync_preview_inline --check ② 重启服务端 ③ 载荷变则清 db。
- **改 minimap-vein.js 只动这一个文件**（热拔插单点在 main.js `initMinimap()`）；
  ⚠ main.js 无 `g` 形参，全局注册只能写 `window.MiniMapVein`。
- **地形层只读 `fields().biome/.e`，绝不读 onRoad**；抽样格必须世界对齐 `q%m==0 && r%m==0`（skill §17）。
- 本机环境：bash 缺 dirname/ls/tail/head（用 Node -e 替代）、PIL 不可用（用 Node zlib 手解 PNG）、
  文本文件全 CRLF（追加用 Node normalize→写回 CRLF）。

# 前端表现升级 —— 地名匾额 / 灵脉山体 / 地表让位 / 云气

> 来源：2026-09-14 需求（附图两张：山海经式竖排地名签、氤氲水墨山体）。
> 规矩：**完成一件打勾一件**，每项做完先跑对应验证再勾。
> 全部改动在**浏览器侧**（web/js/*、web/index.html），不碰引擎生成逻辑 ⇒ 无需重建服务端、无需清 db。
> 唯一例外：`web/js/bldg_ink.js` 被 `灵脉预览.html` 内联 ⇒ 改完必须重跑 `sync_preview_inline.mjs`。

## 验证口径（headless 看不到图，所以用「整数事实 + 逐像素差分」判定）
- **A/B 差分**：同一机位、同一 seed，只切一个开关，用 `verify/png_stats.mjs A.png B.png`
  读「差异像素占比」与「墨像素占比」。机器无关、可复现。
- **调试开关**（日常游玩不传）：`?nobanner=1`（关匾额）、`?nocloud=1`（关云气）、
  `?noyield=1`（关地表让位）、`?noclouddrift=1`（云不漂移，供云气严格 A/B）、
  `?nobldg=1`（关建筑层，既有）。
- **运行期事实**：`?debug=1` 下 `window.__feat()`（计数汇总）与 `window.__spots()`（路压水/建筑压水的格坐标）。
- 截图统一走实机自截通道：`verify/live_cap.mjs <url> <out.png> 90 1400x900`（`capture=1`）。

## A. 地名竖排匾额（山海经风）+ 开关
- [x] A1. `drawNameBanner()` 纸签绘制：米黄纸底 + 细墨框 + 焦墨竖排字 + 一线引绳连到地物 + 名字 hash 定倾角（不闪）
      —— 竖排实现：字逐个落在 `-bh + padT + lineH*(i+0.5)`，即**单列自上而下**；
      倾角 `bannerTilt(name)` 由 `hash32(name)` 定 ⇒ 同一地名恒定，平移/缩放不换角（实测 ±1.6°）。
- [x] A2. 聚落/秘境名牌改走匾额，签底落在**对应地物上方**（建筑实体化时的偏移一并处理）
      —— 签底 `bottomY = anchorY - lead`，而 `anchorY` 在「图标顶之上」或「建筑群上沿之上」，
      引绳由 `drawNameBanner` 自签底连回锚点。
      ⚠ **2026-09-14 修正**：原用**常数** `ps2.y - 4.8*hexR*z` 当抬升量 —— 镇子小时签子飘到半空
      （用户反馈「位置超出正上方太远」）。改为 `bldgAnchor(st)`：取建筑群**世界包围盒上沿中点**，
      抬升压到 `1.15*hexR*z + 2`；引绳也由 `fs*0.95` 缩到 `fs*0.62`，签子贴住镇子上沿。
- [x] A3. 新增「匾额」开关（顶栏按钮 + `?nobanner=1` 调试参数），点击即时重绘
      —— `#btnBanners`，点击翻转 `showBanners` + 置静态层脏。
      ⚠ **2026-09-14 修正**：JS 一直是接好的，用户看到「开关没做」的真因是 **CSS 把按钮裁掉了** ——
      `#controls` 是 `.panel`，其 `clip-path: polygon(...)` 会裁剪**整棵子树**，而 `#controls .row`
      是不换行的 `display:flex` ⇒ 开关加到 4 个后按钮被挤出面板、在裁切区外**看不见也点不到**。
      修法：`#controls .row` 加 `flex-wrap:wrap; justify-content:flex-end`，并把统计拆到第二行。
- [x] A4. 截图确认：签竖排、悬在对应地物上方、平移缩放不闪
      —— `up3_sect_banner_yield.png`（开）vs `up3b_sect_nobanner.png`（关）：
      **差异 0.98% 像素（>24）、0.32% 强差（>120）**，差异 bbox `1021×428` 且集中在
      聚落上方区域（`up3` 全图墨像素 9.074%，`up3b` 9.065% —— 差值即那几块纸签）。
      另有 `up1_wide_banner_cloud.png` 广角总览。

## B. 灵脉山体改水墨风（**2026-09-14 归零重做，四版定稿**）
> ⚠ **山形在「六版」重写**（宽顶圆拱 + 下半截真渐隐，见 §L3/L4）：§B2 的「复用 drawPropPeak」
> 与 §J4 的「折线尖峰 + 不透明mid@0.97」均已废弃；现形由 `veinPeakPts()` 独立生成。
> （另：§B 所说的 `drawPropPeak` 宽顶改造曾是**改错层**，六版已回退，见 §L1。）
- [x] B1. ~~`drawMistyPeak()`：多团柔边叠影 + 顶上淡霭 + 云气横带 + 山脚化入雾中~~
      —— **已废弃并删除**（`mistPeakPath` / `mistShade` / `mistPeakBody` / `drawMistyPeak` 四个函数
      已从 `textures.js` 移除）。原因：PROP_VS 把整个 128 格**非等比**映射到 W×H（W 常是 H 的 1.5~2.2 倍），
      自造形状上屏必被横向拉宽 —— v1 成「玻璃锥」、v2 成「兔耳土包」、v3「柔边淡霭」成**苍白圆锥**。
- [x] B2. `propVein()` 定稿：**山形骨架直接复用大世界已验证的 `drawPropPeak`**，只换「色 / 气 / 皴 / 符纹」
      —— 副峰 `[96,118,96]/[152,172,138]` + 主峰 `[58,80,68]/[116,138,108]`（**青灰绿，dark 与大世界岩峰
      `[60,54,46]` 同明度、只换色相 ⇒ 有体积不飘**）；`{noCrag:true, g0:0.86/0.90, g1:0.48/0.50, lit0/1/2=青白}`
      （**g0 必须回到 ~0.90 —— v3 压到 0.70 是「洗白」元凶**）；`miDian()` 米点皴代岩层横裂；
      山脚 + 山腰 `mistBand()` 云断；元素色 wash（0.13/0.06）；`drawSigil` 缩 0.62 落山体中下段；
      6 点灵气游丝只落山脚（y≥62）。
      **云气感来自「山脚云断 + 山腰淡霭」，不是把整座山变淡** —— 这是本版最关键的一条。
      `drawPropPeak` 为此新增 `opt.g0/g1`（脊线对比）与 `opt.lit0/1/2`（受光面色）、`opt.noCrag` 三个参数。
      图集索引不变：灵脉峰仍为第 6 行 2~6 列（id **50..54**）。
- [x] B3. 截图确认：灵脉山体 vs 大世界山体同框可辨
      —— 看板：`tools/prop_sheet.mjs --hero`（6x 特写，`verify/_prop_sheet.png`）—— 灵脉峰与大世界山 B
      并排比对「同为实色山体 + 脊线重量，仅色相（青灰绿 vs 暖褐灰）与皴法不同」；
      实机：`verify/_v7.png`（默认视角，`live_cap.mjs`）—— 灵脉峰落在「金属矿脉（大）」牌匾正下方。
      镜头目标由离线探测给出：seed 42 最近的大灵脉「金属矿脉」在格 **(-8,0)**，距原点 8 格。

## C. 道路/建筑处地表让位（树·山 → 平地）
- [x] C1. 覆盖格集合：建筑占地格 + 道路折线沿采样格，版本号 `blockedVer`
      —— `rebuildPropBlock()`：建筑取 `settleCells[*].buildings` 的 (q,r)；
      道路沿折线按 `hexW*0.34` 步长采样成格（**不是只取端点**，否则路中段仍长树）。
- [x] C2. `renderer.updateProps()` + 逐块过滤精灵，覆盖处不再长树/山
      —— 新增 `InkRenderer.updateProps(key, data)`（只换该块的精灵实例，不重跑淡入），
      `refreshChunkProps(key)` 在传 GPU 前按 `propBlock` 过滤掉覆盖格的精灵。
- [x] C3. 区域/聚落数据到达后自动重算；平移缩放不重算（版本号门控）
      —— `rebuildPropBlock` 挂在 region/settle 应用与 `regenerate`；`syncPropBlock` 由
      `syncedVer !== blockedVer` 门控 ⇒ 平移缩放零开销；块卸载时 `propBuilt.delete(key)`。
- [x] C4. 截图确认：聚落内部与道路沿线无树无山
      —— `up3_sect_banner_yield.png`（让位开）vs `up3c_sect_noyield.png`（让位关）：
      **墨像素 9.074% vs 9.266%** —— 让位开启后画面墨量**变少**，正是「树·山被抹平」；
      差异 1.72%（>24）/ 0.59%（>120）。

## D. 压水处理（建筑 → 桥梁 / 道路 → 虚线航道）
- [x] D1. 建筑格是水 → 改画 `栈桥`（`bldg_ink.js` 新增 painter，关掉六边基座）
      —— `KINDS['栈桥']`（水纹 + 跨格桥面 + 桥板横纹 + 两侧栏杆/立柱/压顶 + 桥墩入水），
      **刻意不进 `KIND_LIST`**，以保持「26 种 = 服务端 BUILDINGS+CORE_KIND」的对齐不被打破。
      ⚠ 实测结论：**该分支当前不可达** —— `Engine/js/mapgen.js:836` 明确
      `if (f.biome <= BIOME.OCEAN) continue;   // 水上不落建筑`（BIOME={DEEP:0,OCEAN:1}），
      离线扫 seed 42 × 14 个区域的所有聚落足迹，落水建筑 **0 座**。
      「水变桥」的可见形态由**临水建筑自带栈桥**承担：码头/渔船坞/渔亭 的
      `FACE_RULE='water'`，painter 里栈桥自岸边 v=0.1 **伸到 v=1.5（入水）**。
      本分支保留为防御（零成本），若日后放开「水岸建筑占水格」即自动生效。
- [x] D2. 道路过水段 → 虚线航道：`pushDash` + renderer 第三条道路缓冲（石青墨）
      —— `pushDash()` 按 dash/gap 切段再展宽；renderer 增 `roadBufWater` 与 `roadCounts`，
      `setRoads(halo, core, water)` 三参；水上用石青墨 `0.325,0.435,0.500,0.62`。
      判据「端点任一在水里即判过水」，使一格宽的河汊也能被两段虚线完整覆盖。
- [x] D3. 截图确认：跨水路段为虚线、水岸建筑落到栈桥上
      —— `up6_water_dashed_channel.png`（`qt=-37,rt=42`，墨像素 11.8%、均值亮度 166.0）。
      目标格由离线探测给出：seed 42 最近的水上路段在 **(-37,42)↔(-38,43)**（距原点 42 格）；
      `window.__spots()` 可在页面上直接列出所有「路压水 / 建筑压水」格坐标。

## E. 云气层（上白下阴）
> ⚠ **云在「六版」重做为「水墨祥云」六型**（勾线云团 + 开放式卷云钩，见 §L5）；
> 本条描述的「径向渐变 4 变体」已废弃（它读作圆气球/花朵）。
- [x] E1. `buildClouds()` 程序化水墨云团（顶白 / 底阴 / 外缘雾化 / 接地淡影）
      —— 4 个变体（256×160 位图，`CLOUD_W/H = 128/84` ×2 超采样）：
      接地淡影 → 云身（mid）→ 云顶（近白 top）→ 云底阴面（灰绿 bot）→ 淡墨软线（ink）；
      整条链**不加硬边**，全靠径向渐变收尾。
- [x] E2. 世界锚定云毯 + 缓慢东移 + 0.88 视差，绘于静态层之上、交互高亮之下
      —— `CLOUD_CELL=430`(世界px) / `CLOUD_PARALLAX=0.88` / `CLOUD_DRIFT=7`(px/s)；
      播种密度由格号 hash 定（六成格子不播种 ⇒ 疏朗）；`drawClouds` 在 `drawOverlay`
      里紧随静态层、`hexHi` 之前。
- [x] E3. 新增「云气」开关（`?nocloud=1` 调试参数）
      —— `#btnClouds`；另加 `?noclouddrift=1` 冻结漂移，用于严格 A/B（见 E4）。
- [x] E4. 截图确认：云有体积感、上下有明暗、不糊住地图
      —— 冻结漂移后 `up3e_cloud_on_frozen.png`（开）vs `up3f_cloud_off_frozen.png`（关）：
      **差异 3.71%（>24）/ 0.86%（>120）**，差异 bbox `991×369`（云毯覆盖面）；
      墨像素 9.054%（开）vs 9.399%（关）—— 云比其下地图**更亮**，故开云后"墨"变少，
      印证云是「上白下阴」的半透明层，且未糊成一片黑。

## F. 回归与收尾
- [x] F1. 语法静态检查（`node --check` 各改写文件）—— `web/js/main.js` / `textures.js` /
      `bldg_ink.js` / `web/index.html`(非JS) / `verify/cdp_feat.mjs` 全部 exit 0。
- [x] F2. `sync_preview_inline.mjs` 重内联（bldg_ink.js 变更）+ `--check` 通过
      —— `--check` 报「✔ 全部同步」（mapgen-config 4164 / mapgen 72106 / bldg_ink 86706 字节）。
- [x] F3. 离线回归：`check_preview_*` / `check_edge_falloff` / `w5` / `w6`
      —— 9/9 全绿（exit 0）：`sync_preview_inline --check`、`check_preview_vein_marker`、
      `check_preview_terrain`、`check_preview_draw`、`check_preview_settle_road`、
      `check_edge_falloff`、`w5_sprite_range`、`w6_bldg_face`、`w3_bfs_road`。
- [x] F4. 实机截图归档到 `待办事项/img/` —— 共 11 张（统一 1400×900，全部走 `live_cap.mjs` 实机自截通道）：
      `up1_wide_banner_cloud`（广角总览 zm=0.8）· `up2_city_closeup`（承天城 25 座建筑）·
      `up3_sect_banner_yield`（归元宗近景）· `up3b_sect_nobanner` · `up3c_sect_noyield` ·
      `up3d_sect_nocloud` · `up3e_cloud_on_frozen` · `up3f_cloud_off_frozen` ·
      `up4_vein_misty_close`（灵脉近景）· `up5_vein_vs_world`（同框）· `up6_water_dashed_channel`（虚线航道）。
- [x] F5. 更新 `.workbuddy/memory` 日志并回填本文件勾选 —— 见
      `.workbuddy/memory/2026-09-14.md`。

## G. 2026-09-14 用户反馈三条修正（本轮）
> 用户原文：「1.山体样子不对，美术风格不对，需要重做 2.牌匾在灵脉也要有，另外：开关没做，
> 其次是位置超出正上方太远 3.现在滚轮放大缩小时，有时候没响应，只有拖动屏幕时才会有响应
> （滚轮的数值有记录，但是画面没变）」

- [x] G1. **山体重做** → 见 §B（`drawMistyPeak` 系整族删除，改用 `drawPropPeak` 骨架 + 青灰绿实色）。
      自检工具：`tools/prop_sheet.mjs [--hero] [--R=8]` → `verify/_prop_sheet.png`（6x 特写 + 实机尺寸条）。
- [x] G2. **灵脉牌匾** —— `renderStaticInto` 里新增灵脉名牌循环（**受「匾额」开关管，不受「注记」管**）：
      签面薄敷灵根本色 `tint` + 签脚一枚元素色印；`z >= 0.85` 才画；签底立在脉峰之上
      `ps3.y - hexR*z*(level===0?2.55:2.25)`；画在聚落名牌**之后** + `avoid:true` ⇒ 与村名撞位时上让一档。
      实机确认（`verify/_v7.png` 默认视角）：`金属矿脉灵脉（大）`、`雾瘴湖泽灵脉（中）` 两签均在。
- [x] G3. **开关「没做」= CSS 裁切** → 见 §A3。（JS 侧 4 个按钮的 `click` 一直存在且都调 `forceStaticDirty()`。）
- [x] G4. **牌匾吊太高** → 见 §A2（`bldgAnchor` 真包围盒 + 抬升 4.8R→1.15R + 引绳缩短）。
- [x] G5. **滚轮缩放偶发无响应（画面冻死）** —— 根因：静止降帧用 `(frameCount & 1)` 跳帧，而
      `frameCount` **只在真正渲染的帧里 `++`** ⇒ 相机静止且它恰为奇数时，跳帧分支**永远成立**
      （它自己不会前进），整个 `loop` 冻死：滚轮改的 `cam.tzoom/zoom` 数值在涨、画面纹丝不动，
      只有 `drag`（让 `settled=false`）才"活一帧"。修法：改用**每次 loop 都自增**的 `tickCount`。
      位置 `web/js/main.js` 主循环（`var tickCount = 0;`）。
- [x] G6. 门禁：`node --check` `textures.js`/`main.js` 均 exit 0；
      `sync_preview_inline.mjs --check`「✔ 全部同步」（本次改动只在 `textures.js`/`main.js`，
      **这两个文件不在 `灵脉预览.html` 的内联清单里**，故无需重内联）；
      离线回归 **9/9 全绿**（`check_preview_vein_marker` / `check_preview_terrain` /
      `check_preview_draw` 32:0 / `check_preview_settle_road` / `check_edge_falloff` /
      `w5_sprite_range` / `w6_bldg_face` 23:0 / `w3_bfs_road`）。
- [x] G7. 全页 DOM 截图核对开关面板：右上角裁切 —— 三行齐全：`[种子][山河重铸][另起一界]` /
      `[灵脉][注记][匾额][云气]` / `已探明 宗门村镇 23 · 道路 46 · 灵脉 64`。
- [x] G8. 证据图归档 `待办事项/img/`（本轮 4 张）：
      `fx1_default_vein_plaque.png`（默认视角 1400×900，两枚灵脉签 + 四枚聚落签同框）·
      `fx2_vein_sprite_sheet.png`（`prop_sheet --hero` 6x 特写：灵脉峰 50/51/52 vs 大世界山 41 / 横岭 57）·
      `fx3_controls_panel.png`（控制面板三行全见）·
      `fx4_vein_plaque_closeup.png`（灵脉签锚定峰顶 + 与村签上让的近距离证据）。

## I. 2026-09-14 二轮修正：聚落牌匾**水平锚点回归**
> 用户原文：「名字位置还是不对」＋ 截图红箭头 5 处（桃溪村 / 地热温泉灵脉 / 甘泉集 ×2 / 千年树冠灵脉）。

- [x] I1. **根因 = §G4 我把水平锚点改坏了**。G4 为体感"吊太高"改 `bldgAnchor` 时，顺手把水平
      锚点从「聚落中心 `st.x`」换成了「建筑世界包围盒中心 `(x0+x1)/2`」——
      **这是回归**：建筑地皮含农田 / 林地 / 码头 / 水车，分布常不对称，甚至有一两格远在 1~2 格外，
      包围盒中心被整体拉到一侧 ⇒ 牌匾看起来"挂歪"（左半屏的偏左、右半屏的偏右）。
      修法：**水平锚点回到 `st.x`**（与服务端实体坐标 / 图标同源），垂直锚点保留"建筑群最北格 `y0`"。
      `?ancgeo=1` 可退回包围盒中心（同机位 A/B 用）。
- [x] I2. **量化验证**（seed 42 / `zm=2.6` / cam 原点，同机位 1400×900 A/B 逐像素差分）：
      差异 >24 占 **2.69%**、>120 占 0.88%（`verify/png_stats.mjs`）。
      目测：古井村牌匾 x **440 → 487**，而建筑群橙点目测中心 ≈ **485** ⇒ 对齐。
      **附带修好一个副作用**：牌匾右移后不再与灵脉签相撞，此前被 `avoid` 跳过的
      「金属矿脉灵脉（大）」牌匾重新出现（A/B 左图缺、右图有）。
- [x] I3. **灵脉牌匾复查（结论：无需改动）**。用户圈了两条灵脉，但复现确认
      灵脉签 x **恒 = `w2s(v.x,v.y).x`**（与 `IT.drawVeinFlower` 同源，二者不可能错位）；
      签底 `ps3.y - hexR*z*2.25` 落在脉峰顶上方 ~10px（用户截图中实测：签底 757 / 峰顶 ≈767）。
      ⇒ **未动**。若用户仍要"更贴"，再单独压 `2.55/2.25` 一档（有压住山峰的风险）。
- [x] I4. 门禁：`node --check web/js/main.js` exit 0；离线回归 **9/9 全绿**
      （`check_preview_vein_marker` / `check_preview_terrain` / `check_preview_draw` 32:0 /
      `check_preview_settle_road` / `check_edge_falloff` / `w5_sprite_range` / `w6_bldg_face` 23:0 /
      `w3_bfs_road` / `sync_preview_inline --check` 全部同步）。
- [x] I5. 证据图归档：`fx5_plaque_anchor_AB.png`（BEFORE 包围盒中心 / AFTER `st.x` 并排）·
      `fx6_plaque_anchor_after.png`（改后默认视角全图）。
- 新增调试参数：`?plaqdbg=1` —— 叠加「聚落中心(绿) / 聚落格(黄) / 包围盒锚点(青) / 每个建筑格(橙点) /
  灵脉(品红)」十字与文字标签，**是"牌匾 vs 地物"定位的唯一可靠手段**（截图目测坐标误差 >60px，不可信）。
  `?ancgeo=1` —— 锚点退回包围盒中心。

## H. 工具 / 调试清单（本轮新增）
- `tools/prop_sheet.mjs [--hero] [--R=8]`：精灵看板（原尺寸 2.4x + 6x 特写 + PROP_VS 实机尺寸条）。
  **改精灵美术必先跑这个**：只看原图会把比例判错（PROP_VS 横向拉宽的坑）。
- `verify/live_cap.mjs <url> <out.png> [sec] [WxH]`：实机自截通道。⚠ **只含地图画布（glcanvas+overlay），不含 DOM 面板**
  —— 要看面板/按钮必须另走 `--headless` 全页截图（`--headless=new` 会忽略 `--window-size`）。
- `verify/crop_png.mjs <src> X Y W H S <dst>`：最近邻裁切+放大（读图判形必用）。

## 遗留 / 待拍板
1. **D1 分叉**：显式「建筑压水→栈桥」当前不可达（引擎禁水上落建筑）。若要让它真的可见，
   需二选一 —— (a) 放开 `mapgen.js:836` 让「水岸」地皮可占水格（引擎改动，需回归）；
   (b) 前端为「水岸建筑 + 其朝向所指水格」额外补一块栈桥（会与 painter 自带栈桥叠画，需去重）。
   现状（不改）已由临水建筑自带栈桥满足「水上有桥」的观感。
2. **`window.__feat()/__spots()` 仅 DEBUG 下可用**：本轮验数最终改走「A/B 逐像素差分 +
   离线引擎探测（`verify/probe_spots.mjs`）」，因为 CDP `Runtime.evaluate` 在这张重绘负载大的页面上**偶发永久挂起**
   （实测卡 >100s 无返回，`verify/cdp_feat.mjs` 已加 100s 硬看门狗把挂死变成可诊断失败）。
   两个钩子保留备用（无副作用）。
3. 稀有 7 种建筑在 `hexR*z<5px` 时仍被一刀切隐藏（既有遗留，未在本次范围）。
4. **灵脉签与聚落签同址时的可见性**：灵脉常与聚落同位（seed 42 的「金属矿脉」就在「古井村」上），
   高 zoom 下灵脉签（9 字）被村签顶到屏幕外 → `drawNameBanner` 的 `topY < -24` 守卫直接**不画**。
   当前行为（让位/宁缺勿叠）可接受；若要求「任何 zoom 下灵脉签必现」，需二选一：
   (a) 让灵脉签优先（先画灵脉、村签让位）—— 村名会消失；(b) 撞位时灵脉签自动缩字一档。
5. **灵脉山体是否要更"仙"**：本版定调是「与大世界同族、换色相 + 气 + 皴」（用户原话是
   「美术风格不对」，故对齐既有风格）。若要**悬浮仙峰/灵光罩**那种更强的奇幻辨识度，需另立方案
   —— 注意 v1/v2 自己造形的两次失败都源于 PROP_VS 的非等比拉伸。
   → **已由 §J 三轮作答**：改为「更高(hScale 1.90) + 锋利棱角 + 五行/异灵根分色」，不再只换色相。

## J. 2026-09-14 三轮：灵脉「更高 + 锋利棱角 + 五行/异灵根配色 + 专用配置」
> 用户原文：「其次灵脉要更高一些，跟附近的山脉区别开然后：更多锋利棱角，其次不是都是绿色的要根据五行
> 之类的的来定义颜色，后面可能还有多种灵气，随机组合的，然还有异灵根，不是统一颜色的，要有专门的
> 配置文件来配置颜色」

- [x] J1. **专用配置文件 `web/js/vein-skin.js`**（新增；对应需求「要有专门的配置文件来配置颜色」）：
      暴露 `global.VeinSkin = { shape, elements, variants, dual, elementPal, variantPal,
      variantSprite, toRGB }`。`SHAPE = { hScale:1.90, wScale:0.82,
      cell:{mainCx:74,mainBase:118,mainW:66,mainH:110,subCx:38,subBase:116,subW:46,subH:74},
      facet:3, ribs:3, miDian:12 }`（⚠ 六版已删 `facet/ribs`，改 `topW/seg/topSeg/fade/miDian`，见 §L3）；
      `ELEMENTS` 五行（金木水火土）各 6 色字段
      back/mid/lit/mist/glow/rune；`VARIANTS` 四异灵根（雷风冰暗）同构；
      `DUAL = {'0|3':'雷','1|2':'风','0|2':'冰','3|4':'暗'}`。
      **挂载序**：`web/index.html` 中必须在 `textures.js` 之前（textures 绘制时读它）。
- [x] J2. **颜色契约（防静默错位）**：`elements[].glow` 必等于 `mapgen.js ELEMENT_RGB`、
      `variants[].glow` 必等于 `VARIANT_RGB`、`variants` 键顺序必等于 `VEIN_VARIANT_ORDER`。
      护航脚本 `verify/check_vein_skin.mjs`（11 项断言）—— **新增元素/异灵根后必须跑它**。
      实测 W/H≈0.890（略瘦高）⇒ 不会被 PROP_VS 横向拉宽。
- [x] J3. **图集扩容：启用 row4**。原「灵脉格底」是**死代码**（tile 索引 = `biome*4+variant ≤ 31`，
      `HEX_FS` 里 `biome-8 → row4` 的分支永不触发）⇒ 把 row4 col0..3 改作**异灵根灵脉峰 id 32..35**
      （col4..7 空置）。五行峰仍为 row6 col2..6 = **id 50..54**。引擎 `mapgen.js propSpriteFor(f)`：
      有 `f.vein.variant` → `32 + VEIN_VARIANT_ORDER.indexOf(variant)`，否则 `b+42`。
- [x] J4. **更多锋利棱角 + 实心岩体**（替换二轮「复用 drawPropPeak 的青灰绿实色」）：
      `textures.js propVein()` / `drawVeinPeak()` 重写为 —— `veinPeakPts()` 折线尖峰（肩部正弦偏移）
      + 主体**不透明** `mid@0.97` + 硬受光/背光多边形 + 焦墨轮廓 + 脊线 + 棱线 + `miDian` 米点
      + 仅底部 12% `mistBand` 云断。新增 `drawSigilVariant`（雷=折线闪电 / 风=螺旋弧 /
      冰=六芒冰晶 / 暗=漩涡+星点）与 `mistBand(ctx,cx,y,w,h,a,col)` 的 `col` 参数（每灵根异色云）。
      `VEIN_BASE` / `VEIN_TINT` 与 `~~drawMistyPeak 整族~~` 一并删除。
- [x] J5. **更高（与大世界山拉开档次）**：`renderer.js PROP_VS` 顶部读 `window.VeinSkin.shape`
      → `VEIN_SHAPE{h:1.90,w:0.82}`；灵脉精灵的 `hrand` **收窄为 `0.72 + 0.28*hrand`**
      ⇒ 即使在 hash 最矮端也高于 `hrand=1` 的雪峰。`H = uR*(3.3+1.2*hrand)*hs*ss`。
      ⚠ 改这两个系数须**同步** `tools/prop_sheet.mjs`（它按同公式摆实机尺寸条），否则看板失真。
- [x] J6. 门禁：`node --check` 全过；`verify/check_vein_skin.mjs` ✔；`verify/w5_sprite_range.mjs` ✔
      （新增断言「每个五行群系出现过本元素灵脉峰」+「异灵根位 32..35 全部被使用」）；
      离线回归 **9/9 + check_vein_skin**；`tools/prop_sheet.mjs` 重跑出看板。
- [x] J7. 实机验证（用户 8140 实例，清库重建）：seed 42 金「金属矿脉」格 (-8,0)、
      seed 74 雷「雷击崖」格 (4,-9)。证据图归档 `待办事项/img/`：
      `fx7_vein_element_variant_sheet.png`（九色一览）· `fx8_vein_live_gold.png`（seed42 金）·
      `fx9_vein_live_thunder.png`（seed74 雷）· `fx10_vein_closeup_gold.png`（近景特写）。

## K. 2026-09-14 四轮：灵脉**大/中/小分档**（占地 + 高度）
> 用户原文：「现在根据大中小灵脉，要改成： 大的不变，中的只占有 当前格子和下面两个格子，
> 小灵根只有一个格子，高度也要有区分」

- [x] K1. **占地契约（引擎）**：`Server/Zongmen/Engine/js/mapgen.js` 新增
      `VEIN_LVL_BELOW = [[0,1],[-1,1]]` 与 `veinFootKeep(level,dq,dr)`：
      `level 0(大)` → 环上 7 格全保留（**与旧行为逐位相同**，满意「大的不变」）；
      `level 1(中)` → 仅「中心 + 下方两格」；`level 2(小)` → 仅中心格。3 项已导出到 `global.MapGen`。
      ⚠ **本条已被 §L2 全面改写：三档一律只占本格，`VEIN_LVL_BELOW` 已删除。此处仅存档。**
      ⚠ **「下方两格」的几何依据**：`tileToWorld` 里 `y = 1.5*HEX_R*r`，y 随 r 增 → 屏幕下方 = `dr=+1`，
      故就是 `[0,1]`(右下) 与 `[-1,1]`(左下)；不是 `[1,0]/[-1,0]`(同排) 也不是 `dr=-1`(上方)。
- [x] K2. **落地点**：`fields()` 的灵脉覆写守卫由「环内 d≤1」改为
      `vn.d<=1 && e>=SEA_LEVEL && veinFootKeep(vn.v.level, q-vn.v.q, r-vn.v.r)`；
      `buildChunk()` 下发实例时把等级塞进**海拔通道**：`e: (f.vein ? f.vein.level : f.e)`。
- [x] K3. **高度分档（配置真源）**：`web/js/vein-skin.js` 新增 `LEVELS` 表 +
      `levelInfo(i)`（接受下标或 key，兜底大档），并导出 `levels/levelInfo`：
      `大 hScale 1.90 hRand[0.72,1.00]` / `中 1.58 [0.86,1.00]` / `小 1.22 [0.72,0.96]`。
      ⚠ 各档**收窄随机包络**是为了让「倍率差」不被 hash 抖动盖过（否则同 hash 下小档可能比中档高）。
- [x] K4. **渲染（renderer.js PROP_VS）**：灵脉分支改为按 `iElev` 取档 ——
      `vlv = clamp(iElev,0,2)`，`hs` 走 `vlv<0.5?VH(0):(vlv<1.5?VH(1):VH(2))`，`hrand` 同理取各档包络。
      **通道复用**：灵脉峰的 `iElev` **不再是海拔**而是**等级(0大/1中/2小)**；大世界山照旧下发海拔。
      字面量经 `EPS=1/1024` 取整 + `toFixed(3)` 落成 `1.900` 这类定值，避免 `1.9000000000000001` 进 GLSL。
- [x] K5. **门禁**：`verify/check_vein_skin.mjs` 新增 **B 段「等级契约」（12 项断言，总计 29/29 ✔）** ——
      三档 key/level、大档 == shape、`hScale` 严格递减、`hRand` 合法、
      **可视高度互不重叠（大min>中max 且 中min>小max）**、`veinFootKeep` 占地 大=7/中=3/小=1 且**嵌套**
      （小⊂中⊂大）、下方两格 `dr` 恒为 +1。
      `verify/w5_sprite_range.mjs` 默认半径 **8→12**：分档后「异灵根峰」格数骤减（-70%），
      ±8 窗口漏掉最稀的雷档 → 假 FAIL；±12 四变体齐全。（非代码 bug。）
- [x] K6. **实测与证据**：
      端到端探针 `verify/_probe_vein_lv.mjs`（直调 `communityOf/veinNear/fields`，以 `fields().vein`
      为准）→ 大 76 根/7 格 · 中 91 根/3 格 · 小 213 根/1 格（其中 1 根 kept=0：两群落把灵脉放在
      **同一格**的既有碰撞，非本次引入）⇒ **无任何等级超上限**。
      高度契约（`H = uR*(3.3+1.2*h5)*hs`，uR=8）严格分离：**大 [63.29,68.40] > 中 [54.76,56.88] >
      小 [40.64,43.45] px**；同 hash 同刻度并排实测 **66.7 / 56.2 / 42.5 px**（比 1.18 / 1.33）。
      证据图归档 `待办事项/img/`：`fx11_vein_tier_sheet.png`（看板 ② 段：三档同刻度并排 + H 值）·
      `fx12_vein_tier_trio_live.png`（seed42 群落(4,3) 三档**同屏同机位**）·
      `fx13_vein_tier_large_live.png`（大：多峰大山体）· `fx14_vein_tier_mid_small_live.png`（中/小对比）。
- [x] K7. 门禁：`node --check` 全过；离线回归 **10/10**（含 `check_vein_skin` 29/29、`w5` 半径 12）；
      `sync_preview_inline --check` ✔；用户 8140 实例已**清库重建**（内容变了）。
- ⚠ **量测坑（本轮踩过，记录备查）**：想「用像素高度证明 大>中>小」时，两条路都不通 ——
      ① 固定屏幕窗口跨机位不可比（三张图相机目标不同）；② 用金色阈值 `r>g>b && r-b>=45`
      会被**橙色花树**命中（到金板距离 <105）→ 假高度。**且**实机帧里灵脉格水平仅隔 ~10px（zoom 1.5）
      而精灵盒半宽 ~45px，大灵脉的上排邻格峰必然盖住中心格峰 ⇒ **单格高度在实机帧里无法隔离量测**。
      ⇒ 高度差只能由「同底同刻度并排」的确定性看板（`tools/prop_sheet.mjs` ② 段）判定，这是它的设计用途。
      簇包围盒高度还会被**占地行数**污染（大含上方邻格），**不可跨档直接比簇高**。

## L. 2026-09-14 六轮：**改错层回退** + 灵脉**单格占地** + 山形「宽顶圆拱 + 下半渐隐」 + 云「祥云六型」
> 用户原文：「不是改大世界的山，是改灵脉的山啊，你改错了！！！改回来 灵脉不应该站好几个格子的，
> 只要站一个格子即可 …而且你的云也太丑了，参考这个画云这种风格的云，要多变，不要只有一个」

- [x] L1. **回退误改层**：五轮把**大世界地形山** `textures.js drawPropPeak()` 的 6 处（渐变 / 主轮廓 /
      受光面 / 背光面 / 雪帽 / 描边）改成宽顶 —— 属**改错对象** ⇒ 逐处回退为原始窄峰二次曲线
      （`cx-w*0.28…cx+w*0.17…`；轮廓 3 点 `[cx-w*0.5,baseY]→[cx-w*0.27,baseY-h*0.52]→[cx-w*0.02,apeY+h*0.07]`，
      width 1.5 / alpha 0.5）。`opt.g0/g1`、`opt.lit0/1/2`、`opt.noCrag` 机制**保留**（B2 仍在用）。
- [x] L2. **灵脉占地：三档一律 1 格（引擎）**：`mapgen.js veinFootKeep()` 收敛为
      `return dq === 0 && dr === 0;`，删除 `VEIN_LVL_BELOW`（含 `global.MapGen` 导出）。
      理由：7/3/1 按等级密排后连成**尖顶山簇**，实机读作「三角形山」，与「一峰独立 + 山脚化进地形」不符。
      等级**只**影响前端高度倍率（`vein-skin.js levels[]`），**不影响占地**。
- [x] L3. **山形重写**（`textures.js veinPeakPts()/drawVeinPeak()`）：`VeinSkin.shape` 去掉 `facet/ribs`，
      新增 `topW:0.30 seg:11 topSeg:7 fade:0.50 miDian:12`；`wScale 0.82→0.95`（解出 W/H≈**1.031**，不被横向拉宽），
      `cell` 加宽（`mainW 100 / mainH 96`，底座铺满 16..116）。形 = 宽底 → 左坡 `x=1-(1-t)^0.50` 钟形
      → 顶台**圆拱**（`sin(πt)·0.11h`，免读成梯形台）→ 右坡镜像；墨线**只勾山腰以上**
      （`lim = baseY - h*(F+0.02)`，免得在已化开处留硬边）。
- [x] L4. **下半截真渐隐（本轮最关键修正）**：原 ⑦「山脚化雾」是往渐隐区**叠加** mist 色
      （最高 0.50 alpha）——**恰好把 ① 的渐隐重新糊实**；外部云断 band 也用 0.70 ⇒ 山脚近乎实心
      （实测「逐行最大 alpha」直到最后 1 档才掉到 9，几乎硬边）。修法：删除 ⑦；云断 band 降档
      （`0.70→0.46` / `0.28→0.18`）；① 渐变改 `0→0.96 / F*0.62→0.90 / F→0.58 / 0.84→0.15 / 1→0`；
      石纹抬到 `F+0.04` 以上。**原则：渐隐只能由 alpha 渐变实现，不能靠叠雾。**
- [x] L5. **云重做「水墨祥云」六型**（`textures.js buildClouds()`，`CLOUD_N=6`）：新增
      `cloudPuff`（扁圆花瓣 + 下半阴 + 勾线）/ `cloudCurl`（**开放式**约一圈卷云钩）/ `cloudTail`（4 长弧）/
      `cloudHead`（底大瓣 + 上缘小瓣 + 1 卷钩）/ `cloudRecipe`（6 配方）；六型 = **长云 / 团云 / 双团 /
      高云 / 卷云带 / 小云**；`CLOUD_PAL` 重新定色。`main.js cloudOf` 变体取模改**动态**（`% nsp`），
      不透明度 `0.40+0.36 → 0.72+0.26`。
      ⚠ **反例已写进代码注释**（别再踩）：花瓣**不要绕心均布**（读成花/玫瑰）、卷钩**不要密而居中**
      （读成花心）、同尺寸花瓣串成链会读成**气球堆**。
- [x] L6. **机读验证法（本轮新增，重要）**：主模型读不了图 ⇒ 新增 `verify/_shape_probe.mjs`（**临时探针，已按惯例删除**；配方与判据已固化到 skill `webgl-headless-verify` 专节）——
      页面用 `getImageData` 量 alpha 剪影，把结果**以纯文本注入 DOM**，再用
      Chrome `--headless --dump-dom` 打到 stdout（无 OCR、无 CDP）。实测：
      云 **6/6 互异签名**（宽高比/覆盖% = `2.8/26 · 1.7/21 · 2.2/24 · 0.8/21 · 2.5/22 · 1.6/9`）；
      灵脉·金 前景中位 alpha `半腰187 → 山脚9`（比值 **20.78**）vs 大世界山 `137 → 44`（比值 3.11）
      ⇒ **只有灵脉峰下半化开**，两者一眼可分。
- [x] L7. **单格占地端到端证据**：`verify/_vein_spots.mjs <seeds> --R=14`（**临时探针，已删**）走真实入口 `MG.fields()`
      （与 `buildChunk` 同源）直接数「带 `f.vein` 的格」⇒ **10 个 seed / 17 个灵脉地标 / 恰好 17 格**、
      相邻灵脉格对数 **0**；同窗口下旧 7/3/1 规则本会占 **79 格**。（契约层的 §L2 断言见下条。）
  另：真实实例上做了**同源 A/B**（见 §L10）—— 旧规则 seed42 大灵脉的 7 格是
  `(-9,0) (-9,1) (-8,-1) (-8,0) (-8,1) (-7,-1) (-7,0)`（中心 + 六邻）。
- [x] L8. **门禁**：`check_vein_skin.mjs` **27/27**（B 段改为「三档一律只占 `[[0,0]]`」
      +「等级不影响占地」+「六邻与更远处一律不保留」）；离线回归 **11/11**；
      `sync_preview_inline --check` ✔；`tools/prop_sheet.mjs` 与 `verify/_cloud_sheet.mjs` 重渲。
- [x] L9. 证据图归档 `待办事项/img/`：`fx15_vein_peak_sheet.png`（山形看板，含大/中/小同刻度）·
      `fx16_cloud_six_sheet.png`（祥云六型）· `fx17_live_vein_seed42.png`（实机 seed42 大灵脉特写）·
      `fx18_live_clouds_seed42.png`（实机广角云毯）· `fx19_vein_occupancy_old7cell.png` /
      `fx20_vein_occupancy_new1cell.png`（**占地 A/B 实机裁图**，同为 seed42 (-8,0) 大灵脉 / zm=6 / 去云去建筑去匾额）。
- [x] L10. **占地 A/B 的「引擎副本」实验法（可复用）**：把 `Server/Zongmen/Engine/js/*` 复制到
      `%TEMP%/wb/eng_old/`，**只**把 `veinFootKeep()` 回退成 7/3/1（其余一字不改），再用
      `Zongmen__EngineJsDir=<副本>` 起第二个隔离实例 ⇒ **两台实例仅差这一个函数**。同 seed 同机位
      各抓一帧做逐像素差分，结论：
      · zm=6 时差异**只有一处** —— bbox `422,88→938,566`（517×479px），中心 (680,327)、镜头中心 (700,450)
        ⇒ 灵脉峰确实立在中心格上（底座在屏心、峰体向上 ~360px）；旧 7 格处是**大团重叠的深色山簇**
        （墨 20.0% / 亮度 182.1），新 1 格处墨降到 **11.5%** / 亮度升到 **192.0**（重叠消失、整体变亮）。
      · 控制实验：同一实例同 URL **连拍两帧差分为 0.00%** ⇒ 渲染完全确定，上面的差异全来自内容本身。
      ⚠ 引擎副本是 **CRLF** —— 断言式替换前必须先 `replace(/\r\n/g,'\n')`，否则命中 0 处（本轮踩过）。
- ⚠ **隔离实例（不动用户 8140）**：本轮服务端验证跑在 `verify/_vmsrv`（`dotnet build -o`）+
      独立 DB `%TEMP%/wb/reg8177.sqlite`，端口 **8177**（A/B 对照实例用 **8178** +
      `Zongmen__EngineJsDir=%TEMP%/wb/eng_old`）；`live_cap.mjs` 落共享 `verify/capture.png`
      ⇒ **必须串行跑**。收工 kill 实例 + 删 `verify/_vmsrv`。

## M. 2026-09-14 七轮：灵脉峰 **缩到 40%** + 云「更小·更透·地上有影」 + 建筑「场地」
> 用户原文：「灵脉的山还是太大了缩小一点缩小到原来的40%大小
> 云太大了，而且透明的太低了，土地上面也没有阴影，这个要有
> 然后建筑的背景应该有一个场地的，避免出现svg太浅了导致被背景覆盖掉的情况」

- [x] M1. **灵脉峰 40%（单旋钮）**：`vein-skin.js Shape` 新增 **`sizeScale: 0.40`** —— 全工程唯一的「灵脉上屏尺寸」
      旋钮；`renderer.js` `VEIN_SHAPE.sc ← sizeScale`，`PROP_VS` 里把 `VSIZE = sc.toFixed(3)` **只**乘进灵脉分支
      （`(iSprite>31.5&&<35.5)||(>49.5&&<54.5)`）的 `ss`；W 与 H **同乘** ⇒ **W/H 比值不变**（仍 ~1.031，不被横向拉宽）。
      `3.3+1.2*hrand` 的**相对分档**一字未改（大/中/小仍 7.91~8.55 / 6.84~7.11 / 5.08~5.43），
      **上屏**变成 大 **3.16~3.42** / 中 2.74~2.84 / 小 2.03~2.17 uR（≈ 1.8 格高的单格峰，落在雪峰以下 —— 有意为之）。
- [x] M2. **云：更小 + 更透**（`main.js`）：
      · 尺寸 `base 176 → **92**`（乘 `0.70+0.32*min(z,3.2)` 的缩放律不变）；`cloudOf` 的 `sc: 0.72+0.95 → **0.78+0.62**`
      · 不透明度 `a: 0.72+0.26 → **0.32+0.22**`（上限从 0.98 降到 0.54）
      ⇒ 实机 zm=1：云体覆盖 **1.05% → 0.18%**；同一像素亮度增益 **+66.3 → +36.7**（即「更透」的可测含义）。
- [x] M3. **云影（本轮新增图层）**：`textures.js buildCloudShadows()` —— 与 `buildClouds()` **同种子**
      （`31337+v*977`）⇒ 影与云**逐瓣对齐**；抽出 `puffPath()` 供云/影共用（云 `cloudPuff` 改为调它再 fill+stroke）。
      影色 `[58,56,50] a0.40 blur7px`；`CLOUD_W*2 × CLOUD_H*2` 同尺寸画布。
      `main.js drawClouds()` 改**两遍**：先收集可见云到 `vis[]`，第一遍画影
      （偏移 `SH_DX 0.10 / SH_DY 0.30`、`SH_A 0.55`、scale `1.04`），第二遍画云。
      新增调试口 **`?noshadow=1`**（关影）——用于**单差法**量影。
- [x] M4. **建筑「场地」**（`bldg_ink.js hexPlate()` 加强，两后端同源）：
      · `paint()` 默认 `plateA 0.20 → **0.32**`；调用点 `0.16 → **0.40**`
      · `hexPlate` 新增 **地面柔光晕**（`B.radial`，`haloCol #efe6d2 / haloA 0.28`，`r*1.18`，`detail>=2` 才画）
      · 多边形填充 `o.a` 默认抬到 **0.32**，并在 `detail>=1` 时**勾一圈墨线**（`lw = max(0.5, r*0.045)`，`a0.40`）
      ⇒ 浅色 SVG 建筑不再被底下的地表纹理「吃掉」。SVG 契约：首块 `<polygon fill-opacity>` = **0.40**（旧 0.16）。
- [x] M5. **⚠ 修掉一个隐蔽崩溃 bug（本轮重要）**：`bldg_ink.js paint()` 里
      `fn = KINDS[spec.kind] || KINDS['民房'];` **漏了 `var`** —— 在 namespace IIFE 的严格模式 eval 下抛
      `ReferenceError`，**整层建筑渲染直接崩掉**。补 `var` 后恢复。教训：`bldg_ink.js` 是**独立加载的 IIFE**，
      改它之后必须至少 `eval + 调一次 svgBody()/paint()` 冒烟，光看源码看不出。
- [x] M6. **⚠「固定阈值」对已缩放的形状不公平（量测坑，写进 skill）**：用「逐行最长连续赭金段 ≥ minRun」
      量峰体剪影时，**同一个 minRun 在缩放前后的含义不同** —— 峰缩 40% 后只有最宽几行还 > 30px，
      高度被砍到 **0.11**（假失败）；把阈值放宽成「本帧峰宽的 50%」又因**该格地面同色系**（赭色沙地）
      把地面连成一片（**8.13**，更假）。**结论**：这类「同色系地面上的小目标」剪影高**不可机读**，
      应改用**与阈值无关**的量：**峰底宽 `maxRun`**（旧 83 → 新 33 = **0.398 ≈ 0.40**，干净且决定性）。
      高度的正确性由**大灵脉实机**（峰基宽 0.385 + 峰高 0.229 双降）+ **renderer.js 源码契约**
      （灵脉分支只有一个 `ss=VSIZE`，W/H 同乘）共同保证。
- [x] M7. **机读判据（本轮探针，已按惯例删除；配方固化进 skill `webgl-headless-verify`）**：`verify/_fx7_probe.mjs`
      三段断言全绿（退出码 0）：
      · ① 灵脉：配置真源 `sizeScale=0.40` ✔ · renderer 接线正则 ✔ · 算术「大档上屏高恰为七版前 40%」✔ ·
        实机大灵脉 **赭金 43247→10243**（0.237，面积随 0.4² 缩）✔ · **峰基宽 226→87（0.385）** ✔ ·
        峰高 109→25（0.229）✔ · 全幅 A/B 只有**一处** 145×103 小块（0.63%，落在小灵脉屏位 (90,556)，非画面正中）✔ ·
        小灵脉**底宽 83→33（0.398）** ✔
      · ② 云：覆盖降到 0.68× 以下 ✔ · 亮度增益降到 0.55× ✔ · **云影单差 4740px**（其中软影 Δ20~45 共 2458px）✔
      · ③ 建筑：SVG 基座 `fill-opacity 0.40`（旧 0.16）✔ · 含 `radialGradient` 柔光 ✔ · 实机聚落 A/B 变化 1.68% 铺满建筑区 ✔
- [x] M8. **⚠ 截图确定性（capmin 预热）**：自截图通道的「就绪」阈值 `chunkData>=3` 在低缩放下**太松** ⇒
      未预热完就截，同一 URL 两帧墨像素能差 **6.4% vs 13.9%**（20% 抖动）。新增 URL 参数
      **`?capmin=N`**：截屏前额外静候 N 秒。加 `capmin=10` 后，**同实例同 URL 连拍差分为 0.00%**。
      ⇒ 以后所有 A/B 帧一律带 `capmin`。
- [x] M9. **门禁**：`check_vein_skin.mjs` **30/30**（B 段新增第 13 组：「`shape.sizeScale` 有效」+
      「×sizeScale 后三档仍严格不重叠」+「大档上屏高 ≤ 3.6 uR ≈ 1.8 格高，单格灵脉不越格成灾」；
      并打印「上屏高度 uR × sizeScale=0.4」与「屏幕方框 W/H=1.031（sizeScale 同乘，不改比值）」）。
      `tools/prop_sheet.mjs` 同步：灵脉峰 `ss` 也乘 `sizeScale`（看板与实机同刻度）。
- [x] M10. 证据图归档 `待办事项/img/`（左右并排 = **旧|新**）：
      `fx21_vein40_big_AB.png`（大灵脉峰带，裁 520,100,360,700，左右半显著差异 22.9%）·
      `fx22_cloud_z1_AB.png`（云广角 zm=1 全幅 ×0.55）· `fx23_bldg_plate_AB.png`（聚落全幅 ×0.55）。
- ⚠ **隔离实例（不动用户 8140）**：本轮 A/B 仍用 `verify/_vmsrv`（`dotnet build -o`）+ 新客户端副本
      `%TEMP%/wb/web_old`（**只**回退七版这三个旋钮：`sizeScale 1.00` / `base 176, sc 0.72+0.95, a 0.72+0.26, SH_A 0`
      / `plateA 0.16, hexPlate halo off, fill 0.22`）⇒ 新旧**仅差这三个改动**。
      `verify/_mkweb_old.mjs` 用**递归 copyFileSync**（⚠ 沙箱里 `fs.cpSync` 会**静默杀进程** exit 127，禁用）。
      收工 kill 实例 + 删 `verify/_vmsrv` 与全部 `_*.mjs` / `_*.png` / `_*.html` 临时件（保留被跟踪的 `_scratch_diag.mjs`）。
- ℹ **引擎未动**：本轮只改**客户端**（`vein-skin.js` / `renderer.js` / `textures.js` / `main.js` / `bldg_ink.js`），
      **不涉及** `mapgen.js` 占地或任何服务端载荷 ⇒ 严格说**不必清 DB**；但分块图集有缓存，
      用户重启 :8140 后客户端改动才生效。

## N. 2026-09-14 八轮：云「成批 / 小批 / 孤单」分层 + 聚落名牌**垂直锚点修正**（"偏高 4 格"）
> 用户原文：「1.现在发现云太少了而且没有成批的出现（需要部分成批，部分孤单，部分一小批的）
> 2.@image 这个在灵脉上往上偏移1个格子，但是在村落上就不对，往上了4个格子」

- [x] N1. **聚落名牌垂直锚点：最北格 `min` → 建筑格 r 的 25 百分位**（`main.js bldgAnchor`）。
  · **根因（离线实测）**：聚落地皮里常有一两格离群地物（农田 / 水磨 / 渔亭 / 码头）伸到主体以北
    **2~3 档**（`verify/_anchor_probe.mjs` 扫 seed42 全部 **277 个聚落**：离群抬高 中位 **2** 档 / 最大 **3** 档，
    ≥3 档的有 31 个），而 y0 取 `min` ⇒ 签子被顶高 ≈3 档 + 抬升 ≈1 档 = **正是用户看到的"往上 4 格"**。
  · 改取 `p25`（= 内容密集区的北沿）⇒ 「签底 ↔ 主体上沿」间隙统一到 ≈1.4 档（抬升 0.85 + 引绳 0.6）。
  · 实测 A/B（seed42 / zm=2.6 / 同实例）：村·镇签**下移 31~62px = 1~2 档**（其 out 恰为 1~2），
    **灵脉签与景点签 0 档不动**（走的是另一分支，本轮未改）。横向仍 `st.x`；`?ancgeo=1` 保留。
  · 与灵脉签观感统一：灵脉签相对峰顶、聚落签相对主体上沿，都只留 ~1 格间隙。
- [x] N2. **云：两层掷骰（族群块 → 逐格）+ 提高密度**（`main.js cloudOf` / `CLOUD_CELL`）。
  · ① 族群块（`CLOUD_CLUSTER` 格）掷一次骰子定族型：晴空 26% / 孤单(40%播) / 小批(74%播) / 成批(96%播)；
    ② 块内**逐格**按族型概率播种；③ **每格朵数** = 孤单 1 / 小批 2~3 / **成批 5~8**；族型同时决定大小档。
  · `CLOUD_CELL 430 → 350`（更密）。期望播云率 0.40 → ≈0.55，且成批区连片成团。
- [x] N3. **⚠ 三个反直觉的坑（都实测踩过，已写进代码注释）**：
  · **只播 1 朵永远连不成片** —— 播种格距 `CLOUD_CELL`(350) 远大于云宽(~95px)，相邻格之间留 250px 空隙，
    实测最大连通块只有 832px（≈1 朵）。
  · **抖动要收在 ±0.5 格；放大抖动 = 把同格的朵推开** —— 第一版把抖动放大到 ×1.25 求"散开"，
    成批块反而更小。收窄后"云团直径(350+95=445) > 格距"才搭接成片。
  · **族群块尺度要小**：`CLUSTER` 3 → 2 → **1.5** 格。视野 @z=1 只有 ~4×2.6 个播种格 ⇒ CLUSTER=3 时
    视野内仅 ~1 个族块（整屏要么全成批要么全孤单）；2.0 时期望成批仅 0.5 个（多半看不到）；
    **1.5 才有 ~4.6 个族块**，晴空/孤单/小批/成批能同屏混排。
- [x] N4. **云分布机读判据**（`verify/_cloud_blob.mjs`，已删；配方固化进 skill）：两帧只差 `nocloud` 开关
  ⇒ 非云区 Δ 恒 0（阈值可低到 Δ>5）；对云掩码做 8 连通 + **分块聚集度**（最密块 / 有云块均）。
  · **八版**：覆盖 **16733px (1.33%)**、有效连通块 **40**、成批 1 / 小批 13 / **孤单 26**、**聚集度 3.2×** ⇒ **合格**
  · **七版**：覆盖 3293px (0.26%)、有效块 5、成批 0 / 小批 4 / 孤单 1、聚集度 1.7× ⇒ 不合格
  （**覆盖 5.1× / 块数 8× / 孤单朵 26 vs 1**）。
  · 判据：成批(≥1200px 块)≥1 且 小批≥2 且 孤单≥5 且 聚集度≥2.5×。
- [x] N5. **名牌位移判据**（`verify/_plaque_pos.mjs`，已删）：米黄纸签连通域
  （`r>228 && r-g>4 && lum>205 && g>b`；宽 18~110 / 高 ≥45），按 **x 中心配对**（竖向位移不改 x），
  位移换算 `dy / 一档(1.5·HEX_R·z)`。
- [x] N6. 门禁：`node --check web/js/main.js` ✔；`sync_preview_inline --check` ✔（main.js 不在内联清单）；
  **离线回归 10/10 全绿**。
- [x] N7. 证据图 `待办事项/img/`（左右并排 = **旧|新**）：`fx24_cloud_cluster_AB.png`（云广角 zm=1）·
  `fx25_plaque_anchor_AB.png`（聚落近景 zm=2.6，看签子下移）。
- ⚠ **隔离实例**：本轮起 **8177**（仓库 `web/`）+ **8178**（`Zongmen__WebDir=%TEMP%/wb/web_old8`，
  只回退八版两处：`CLOUD_CELL 430` + 均匀播种 + `bldgAnchor` 用 min）⇒ **两台仅差这两个变量**。
  客户端副本用**递归 copyFileSync**（沙箱禁 `fs.cpSync`，会静默杀进程）。收工 kill 实例 + 删 `_vmsrv`。
- ℹ 仍**纯客户端**改动（本轮只动 `main.js`），不碰引擎 ⇒ 不必清库；重启 :8140 后生效。

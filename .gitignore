# 大体积素材下载包 (OpenGameArt CC0 纸纹包 58MB)
# 所需文件已解压至 assets/textures/paper.png, 此包无需入库
assets/papertextures.zip

# 调试用的页面截图导出 (页内 window.__snap() 可重新生成)
assets/snap_*.png

# 系统杂物
Thumbs.db
Desktop.ini

# 调试截图 (agent-browser 临时产物)
.workbuddy/shot*.jpg

# ======== 运行时产物 (不入库) ========

# SQLite 运行数据库 (世界数据 KV 表实时落盘, 21MB+)
db/

# verify 调试产物 (截图 / DOM dump / 日志), 保留验证脚本源文件
verify/*.png
verify/*.html
verify/*.log
verify/*.txt

# .NET 构建产物 (源码已入库, 二进制运行时重建生成)
Server/Zongmen/bin/
Server/Zongmen/obj/
